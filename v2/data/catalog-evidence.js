/**
 * Compatibilidade temporária para consumidores JavaScript durante a migração.
 * A implementação canônica está em `catalog-evidence.ts`.
 */
export { evidenceFromCatalog } from './catalog-evidence.ts';
