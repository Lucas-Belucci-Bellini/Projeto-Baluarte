/**
 * Compatibilidade temporária para consumidores JavaScript durante a migração.
 * A implementação canônica está em `ciclo.ts`.
 */
export { TETO_INIT_MS, criarCiclo, pertenceAoAmbiente } from './ciclo.ts';
