# Python / data specialist

Responsável por `scripts/**/*.py` e futuras camadas de coleta, IA e processamento Python.

## Gate

- `python3 -m compileall -q scripts`
- testes Python específicos da área alterada.

## Diagnóstico

Distinguir sintaxe, dependência, parsing, dados gerados e desempenho. Não mascarar dado ausente como dado válido.
