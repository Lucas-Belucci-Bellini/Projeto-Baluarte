# JavaScript / V2-JSDoc specialist

Responsável por `src/`, `v2/core/` e módulos nativos JS.

Gate: `npm test` + `npm run tipos:v2`.

Diagnosticar por arquivo e código TS. Corrigir contratos JSDoc reais antes de considerar qualquer relaxamento de `strict`, `checkJs` ou `noImplicitAny`.

Nunca usar `@ts-nocheck` para esconder uma falha.
