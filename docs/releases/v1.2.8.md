# Release 1.2.8 — Piloto Wiki Zomboid e Evidence

**Status:** `PUBLICADA — validação concluída`

**Versão web:** `1.2.8`

**Versão do Launcher:** `1.2.8`

**Tag do desktop:** `desktop-v1.2.8`

**Tag pública:** `v1.2.8`

**Commit de código:** `77dbfff135c788903c7f87a6618b38063f097a59`.

**Main documental posterior:** será registrada após esta reconciliação.

## Escopo

A candidata 1.2.8 promove o próximo marco do roadmap V2: o piloto local de schema Wiki Zomboid conectado à Evidence Layer. O contrato TypeScript `v2/data/wiki-zomboid.ts` valida nome, autor, categoria, Workshop ID, URL, revisão da fonte e data de captura. O módulo `wiki-zomboid` expõe catálogo bounded, resumo, consulta por Workshop ID e anexação opcional de Evidence por `ctx.talvez('evidence', { versao: 1 })`.

O harness V2 registra sete módulos, 20 rotas internas e seis itens de navegação. A nova superfície `/wiki-zomboid` é um piloto local e read-only; as rotas públicas V1 `/zomboid` e `/zomboid-admin`, os wrappers JavaScript, o shell V1 e o router de produção não são substituídos.

## Segurança e limites

O piloto usa somente dados locais já existentes no repositório. Não raspa a Steam Workshop, não faz fetch automático, não escreve banco, não ativa Supabase, DDL, migrations, RLS, Auth de produção, OpenClaw, WhatsApp ou ações externas. Entradas de Evidence começam em `pending`, com proveniência e revisão explícitas; nenhum dado é promovido a fato verificado automaticamente.

A versão não concede autoridade client-side, não introduz permissões novas e mantém `runtimeAuthority: not-authorized` e `publicPromotionAllowed: false`. O fallback sem Evidence continua funcionando.

## Validação antes da publicação

O slice funcional passou no ambiente local com teste focal `4/4`, `npm test` `1254/1254`, typechecks TypeScript/V2, build, integração browser `48/48`, smoke `99/99`, caminho crítico `15/15` e runner oficial com 21 gates verdes. Rust local código 101 permanece documentado como `blocked-known` pela limitação de toolchain; não foi mascarado.

O commit de versionamento `77dbfff135c788903c7f87a6618b38063f097a59` repetiu os gates locais e os oito workflows remotos aplicáveis terminaram verdes. Como a validação foi concluída, as tags e a release foram criadas.

| Gate | Critério |
|---|---:|
| Catálogo de eventos e Nexus | verde |
| `npm run tipos:ts` | verde |
| `npm run tipos:v2` | verde |
| `npm test` | `1254/1254` |
| `npm run build` | verde |
| `npm run v2:integracao` | `48/48` |
| `npm run smoke` | `99/99` |
| `npm run caminho-critico` | `15/15` |
| Runner oficial | 21 gates verdes; Rust `blocked-known`, se permanecer igual |
| CI remoto | 8/8 workflows verdes no SHA correto |
| Desktop Release | workflow `32584486665` verde nos três sistemas e 8 assets verificáveis |

## Artefatos desktop esperados

O CI do commit `77dbfff135c788903c7f87a6618b38063f097a59` ficou verde e a tag `desktop-v1.2.8` acionou o workflow Desktop Release `32584486665`. Windows, macOS ARM64 e Ubuntu concluíram com sucesso; os três manifestos públicos declaram `version: 1.2.8`. A release pública está em [github.com/Lucas-Belucci-Bellini/Projeto-Baluarte/releases/tag/v1.2.8](https://github.com/Lucas-Belucci-Bellini/Projeto-Baluarte/releases/tag/v1.2.8).

Os assets reais publicados são:

| Asset | Tamanho | SHA-512 do manifesto | HTTP |
|---|---:|---|---:|
| `Baluarte-Launcher-Setup-1.2.8.exe` | 644.015.586 bytes | `sMCUeyAlDLFBZ3elmkUbmmZK6xLmZblcQixyxVNwltV9TxIeCHc87NmMyNtpiFYGX4wrtmFf9Aah9zs6UT11+A==` | 200 |
| `Baluarte-Launcher-1.2.8.AppImage` | 773.768.715 bytes | `Dd9LCgXhLZrABTdVoFzqxAIFVc8F2fBAu0K3/A32ux2YPKbuuzIbKLTtzE97IkvFXPbpHgIPF4adHhrrM57O5g==` | 200 |
| `Baluarte-Launcher-1.2.8-arm64.dmg` | 406.549.251 bytes | `rRocBJY131A4ZK/kCMY3Ux5yL5CRej25b5oap0hS2d/OwW5jHgPGk2+I//NEOnqCpzi67TCKqAR2Z8tFRSs07A==` | 200 |

Também foram publicados os dois blockmaps e os manifestos `latest.yml`, `latest-linux.yml` e `latest-mac.yml`. A API do GitHub confirmou release não draft e não prerelease, publicada em `2026-08-22T16:24:42Z`.

## Rollback

Antes das tags, o rollback é retornar ao commit anterior ao bump. Depois da publicação, a recuperação deve apontar para a última release validada, `v1.2.7`, sem apagar uma release correta. O Service Worker só deve ser invalidado por uma versão corrigida posterior.

## Critério de publicação final

A 1.2.8 recebeu `v1.2.8` e `desktop-v1.2.8` depois que o commit chegou à `main`, os gates locais e remotos passaram, o Desktop Release concluiu nos três sistemas, os instaladores responderam HTTP 200 e os manifestos declararam `version: 1.2.8`. Este documento agora registra a release publicada.

## Referências

[1]: ../v2/WIKI_ZOMBOID_SCHEMA_PILOT_2026-08-22.md "Piloto de schema Wiki Zomboid e Evidence"

[2]: ../v2/PHASE_02_EVIDENCE_SLICE.md "Phase 02 — Evidence Layer"

[3]: ../../.github/workflows/desktop-release.yml "Workflow de release desktop"

[4]: ../../src/data/version.js "Fonte canônica de versão"
