# Release 1.2.7 — Briefing→Evidence pelo Registry

**Status:** `PUBLICADA — artefatos verificados`

**Versão web:** `1.2.7`

**Versão do Launcher:** `1.2.7`

**Tag do desktop:** `desktop-v1.2.7`

**Tag pública:** `v1.2.7`

**Commit do código:** `0e200328612c64299f550363fe3440712e491806`

## Escopo

A release 1.2.7 promove o primeiro vínculo do vertical slice local Briefing→Evidence pelo caminho real de módulos V2. O manifesto do Briefing declara a referência fraca `references.modules: ['evidence']` e o lifecycle resolve `ctx.talvez('evidence', { versao: 1 })` pelo resolvedor governado do Core.

O harness V2 registra seis módulos ativos: Cripto, Editor, Militar, Evidence, Briefing e Visor 3D. Evidence permanece sem rota, de modo que as 19 rotas da V1 e os cinco itens de navegação continuam preservados. A superfície do Briefing mostra um indicador textual de Evidence local conectada, sem expor conteúdo de evidência, tokens ou autoridade operacional.

A release não adiciona rede, banco remoto, DDL, migration, RLS, Auth de produção, OpenClaw, WhatsApp, publicação automática, envio externo ou controle de reprodução. O fallback sem Evidence permanece suportado.

## Alterações de versão

A versão `1.2.7` é sincronizada em `package.json`, `package-lock.json`, `desktop/package.json`, `desktop/package-lock.json`, `src/data/version.js`, `public/sw.js` e no valor padrão informativo do workflow Desktop Release. O Service Worker usa `baluarte-v1.2.7` para invalidar caches anteriores.

## Validação prévia do marco

Antes do bump, o marco funcional Briefing→Evidence foi validado no `main` anterior com teste focal `10/10`, suíte `npm test` `1250/1250`, integração V2 `46/46`, smoke `99/99`, caminho crítico `15/15`, build e typechecks aprovados. O runner oficial terminou com 21 gates verdes; Rust local código 101 continua classificado como `blocked-known` por limitação conhecida de toolchain, sem mascaramento.

A validação da própria versão `1.2.7` repetiu os gates críticos depois do bump e antes da criação das tags. O commit de versão foi aprovado nos oito workflows remotos aplicáveis; somente depois disso foram criadas as tags `v1.2.7` e `desktop-v1.2.7`.

| Gate | Critério de publicação |
|---|---:|
| `npm run gen-catalogo-eventos.mjs --verificar` | verde |
| `npm run verificar-nexus` | verde |
| `npm run tipos:ts` | verde |
| `npm run tipos:v2` | verde |
| `npm test` | verde, sem regressão |
| `npm run build` | verde |
| `npm run v2:integracao` | `46/46` ou contagem maior somente se houver nova asserção validada |
| `npm run smoke` | `99/99` |
| `npm run caminho-critico` | `15/15` |
| Runner oficial | 21 gates verdes; Rust local `blocked-known` código 101, se permanecer igual |
| CI remoto do commit de versão | workflows aplicáveis verdes |
| Desktop Release `desktop-v1.2.7` | workflow `32581796791` verde nos três sistemas e assets publicados |

## Artefatos desktop esperados

Depois que o commit de versão e o CI remoto ficaram verdes, a tag `desktop-v1.2.7` acionou o workflow Desktop Release `32581796791`. A release `v1.2.7` foi confirmada como pública, não draft e não-prerelease em 2026-08-22T15:30:34Z.

Os oito assets foram produzidos pelo workflow e responderam HTTP 200. Os manifestos públicos declaram `version: 1.2.7` e os seguintes tamanhos e SHA-512:

| Plataforma/arquivo | Tamanho | SHA-512 do manifesto |
|---|---:|---|
| `Baluarte-Launcher-Setup-1.2.7.exe` | 644,015,362 bytes | `Jnhh8Ep6tI9h6IgBizlfW/artHocv+Mj6yla5ra94ZAWuIMdbJg+EbzPM+js+hXiO+gzq7lqqIPYOZYMERS/nQ==` |
| `Baluarte-Launcher-1.2.7.AppImage` | 773,768,701 bytes | `fIjmSiLOPXVANLPpV5DWoHi5AQTTXPDPYTJc34whKtsKZSsjrVK/4375qnNK/YP5w+uuaM7NBxEpMiIsAkMPIg==` |
| `Baluarte-Launcher-1.2.7-arm64.dmg` | 406,313,600 bytes | `qdw1g5w3I6N5dpwcG2yL0G+W73iNj8jX0XYzxEK0BjHWEaHi79wQQ0o9Uyx+xG1FOtw3Jxn3Pa6mni49ei1XwQ==` |
| `Baluarte-Launcher-Setup-1.2.7.exe.blockmap` | 669,590 bytes | gerado pelo electron-builder |
| `Baluarte-Launcher-1.2.7-arm64.dmg.blockmap` | 423,637 bytes | gerado pelo electron-builder |
| `latest.yml` | 363 bytes | manifesto Windows |
| `latest-linux.yml` | 386 bytes | manifesto Linux |
| `latest-mac.yml` | 363 bytes | manifesto macOS |

## Compatibilidade e limites

A release mantém o shell V1, o router, os wrappers JavaScript, a política deny-by-default, `runtimeAuthority: not-authorized` e `publicPromotionAllowed: false`. A Evidence Layer continua local, append-only e começando em `pending`; o vínculo não transforma candidatos de notícias em fatos verificados nem concede permissões a usuários.

O runtime Rust local pode continuar retornando código 101 quando o Cargo disponível não interpreta `edition2024`; esse resultado deve permanecer documentado como `blocked-known` e não pode ser escondido para aprovar a release. Warnings de chunks grandes permanecem não bloqueantes quando os gates de build concluem com sucesso.

## Rollback

Antes da criação das tags, o rollback é retornar ao commit anterior ao bump de versão. Depois da publicação, a recuperação deve apontar para `v1.2.6`, sem apagar uma release correta para esconder documentação incompleta. O Service Worker volta a ser invalidado somente por uma nova versão corrigida.

## Critério de publicação final

A 1.2.7 recebeu as tags `v1.2.7` e `desktop-v1.2.7` no commit `0e200328` depois de: (1) o commit de versão estar na `main`; (2) os gates locais passarem; (3) os oito workflows remotos aplicáveis terminarem verdes; (4) o Desktop Release `32581796791` concluir nos três sistemas; e (5) os assets e manifestos responderem HTTP 200 e declararem `version: 1.2.7`. A release pública está disponível em [v1.2.7](https://github.com/Lucas-Belucci-Bellini/Projeto-Baluarte/releases/tag/v1.2.7).

## Referências

[1]: ../v2/V2_BRIEFING_EVIDENCE_VERTICAL_SLICE_2026-08-22.md "Contrato do vertical slice Briefing→Evidence"

[2]: ../v2/V2_PROGRESS_REPORT_2026-08-22.md "Relatório de progresso V2"

[3]: ../../.github/workflows/desktop-release.yml "Workflow de release desktop"

[4]: ../../src/data/version.js "Fonte canônica de versão"
