# Release 1.3.1 — Preview local de retenção Evidence

**Status:** `CANDIDATA EM VALIDAÇÃO — ainda não publicada`  
**Versão web:** `1.3.1`  
**Versão do Launcher:** `1.3.1`  
**Tags planejadas:** `v1.3.1` e `desktop-v1.3.1`, somente após os gates e a CI do commit de versionamento passarem.

## Escopo

A candidata 1.3.1 adiciona ao módulo V2 Evidence uma projeção local `retentionPreview(options)`. O recurso mede a idade de registros observados com uma data `now` explícita e classifica cada item como `within-window`, `past-window` ou `future-observed`. Ele existe para preparar decisões futuras de retenção e auditoria sem apagar, alterar, verificar ou promover evidências.

A API usa `maxAgeDays` padrão 30, teto 3650, `limit` padrão 25 e teto 100. Argumentos inválidos lançam `TypeError`. A saída é congelada e contém apenas `now`, `maxAgeDays`, itens redigidos e resumo bounded. Cada item contém somente `id`, `moduleId`, `status`, `observedAt`, `ageDays` e `retention`; statement, source, URI, publisher, collector, confidence, claimKey, tokens, claims e permissões são omitidos.

O preview não apaga registros, não modifica status e não adiciona rota ou permissão. O fallback sem Evidence ou após `dispose` é um preview vazio depois da validação das opções. A implementação canônica permanece em TypeScript, o wrapper JavaScript é preservado e a API é registrada no módulo Evidence existente.

## Segurança e limites

Este marco é local, in-memory e read-only. Não há rede, scraping, worker, banco remoto, Supabase, SQL, RLS, Auth, OpenClaw, WhatsApp, Spotify, publicação automática ou uso de segredos. A política `runtimeAuthority: not-authorized` e `publicPromotionAllowed: false` permanece intacta.

A projeção não é uma política de descarte e não é uma autorização de revisão humana. Persistência, retenção operacional, auditoria, tenancy, concorrência, revisão server-side e rollback continuam fora desta release.

## Validação pré-release

| Gate | Resultado esperado/observado |
|---|---|
| Teste focal Evidence | `9/9` |
| `npm run tipos:ts` | Verde |
| `npm run tipos:v2` | Verde |
| `npm test` | A preencher após a rodada final |
| `npm run build` | A preencher após a rodada final; warnings conhecidos devem permanecer explícitos |
| `npm run v2:integracao` | `50/50` |
| `npm run smoke` | `99/99` |
| `npm run caminho-critico` | `15/15` |
| Runner oficial | A preencher; Rust pode permanecer `blocked-known` código 101 |
| CI remota do commit funcional | A preencher após observar o SHA correto |
| CI remota do commit de versionamento | A preencher antes das tags |
| Desktop Release | A preencher após `desktop-v1.3.1` |

## Critério de publicação

A release somente será considerada publicada depois de um commit de versionamento na `main` com manifests sincronizados, CI remota aplicável verde, tags anotadas `v1.3.1` e `desktop-v1.3.1`, Desktop Release verde em Windows, macOS ARM64 e Ubuntu, release pública não-draft/não-prerelease, oito assets verificáveis, manifests `latest.yml`, `latest-linux.yml` e `latest-mac.yml` declarando `version: 1.3.1` e instaladores/blockmaps respondendo HTTP 200.

Hashes, IDs de workflows, tamanhos e URL pública serão registrados somente após a verificação real. Até então, esta nota é uma especificação de candidata, não uma declaração de publicação.

## Rollback

A referência operacional anterior é a release pública `v1.3.0`. Antes das tags, um problema deve ser revertido por commit novo sem reescrever a história. Depois de publicada, o rollback operacional deve retornar à `v1.3.0` e usar uma nova chave de Service Worker em qualquer correção posterior; a chave `baluarte-v1.3.1` não deve ser reutilizada para conteúdo diferente.

## Próximo passo V2

Depois deste preview, o próximo passo válido é definir retenção operacional e auditoria de consumidor para Evidence local, com limites de idade, duplicidade, concorrência e descarte. Persistência Supabase/RLS só poderá ser considerada com aprovação explícita de staging, custo, migration, tenancy e rollback.
