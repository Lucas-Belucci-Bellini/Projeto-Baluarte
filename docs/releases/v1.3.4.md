# Release 1.3.4 — Referência visual dourada do JARVIS

**Status:** `PUBLICADA — 22 de agosto de 2026`

**Versão web:** `1.3.4`

**Versão do Launcher:** `1.3.4`

## Escopo

A candidata 1.3.4 corrige a composição visual da rota `/jarvis`. A captura limpa do núcleo dourado exibido no navegador foi adicionada como asset local em `public/jarvis/jarvis-nucleo-browser.webp` e passou a ser usada como fallback visual da integração V7. O iframe local `jarvis-nucleo-v7.html` continua sendo o caminho principal, preservando o visual interativo e o contrato same-origin já publicado.

Quando o artefato V7 não estiver disponível, o app não volta a exibir o console visual Mark XIII como composição principal. Ele exibe a imagem de referência dourada fornecida para esta correção; o console Mark XIII permanece montado de forma invisível para preservar seu estado de runtime, telemetria e descarte.

A mudança não altera chat, sessões, modos de IA, memória, observação do Runtime, integração de dados, permissões, Spotify ou Supabase. Não foram adicionados tokens, endpoints externos, captura de áudio, comunicação privilegiada ou dependências de produção.

## Contrato visual e segurança

O adaptador `createJarvisV7Visual()` continua validando somente o artefato local same-origin, rejeitando origem externa, query/hash e protocolos perigosos. O iframe mantém `sandbox="allow-scripts allow-same-origin"`; a imagem de referência é servida pelo próprio bundle e usa `alt` explícito em português.

A captura foi incorporada sem reprocessamento criativo ou alteração de conteúdo. O teste estrutural verifica a presença do asset, o caminho local e a ausência de URL externa no ponto de montagem.

## Validação local

| Gate | Resultado |
|---|---|
| Testes focados do visual e da referência | `6/6` |
| `npm run v2:integracao` | `56/56` |
| `npm audit --omit=dev --audit-level=high` | Verde |
| `npm test` | Verde |
| `npm run tipos:v2` | Verde |
| Invariantes Nexus, estabilidade, eventos e storage | Verdes |
| Invariantes Arma 3 | Verdes |
| `npm run build` | Verde; warning conhecido de chunks grandes permanece explícito |
| `git diff --check` | Verde |
| Smoke visual no preview local | Asset WebP servido e reconhecido na rota `/jarvis` |

Os workflows remotos relevantes do commit de preparação `e6ab583ce` e do ajuste de deployment `a36c29393` terminaram com sucesso, incluindo CI, Core CI, V2 Validation, V2 Runtime, V2 Core, Arma 3 Data CI, Vigia das rotas e CodeQL. A árvore de trabalho estava limpa no momento da promoção.

## Proteção do deployment Vercel

O primeiro deployment da preparação `e6ab583ce` foi recusado por `LAMBDA_SIZE_EXCEEDED`: o bundle agregado das funções Python atingiu 414,84 MB, acima do limite de 225 MB. A configuração `vercel.json` foi ajustada para aplicar às funções `claims.py` e `observability.py` a mesma exclusão de arquivos estáticos e de build já usada pelas demais funções. Essa mudança reduziu o pacote das Lambdas sem remover arquivos do Git e sem alterar o conteúdo servido pelo Vite. O deployment seguinte, do commit `a36c29393`, alcançou `READY`; o asset dourado respondeu HTTP 200 como `image/webp`, com 707.264 bytes, no domínio autenticado do deployment.

## Publicação e rollback

A publicação foi concluída sem force push com as tags anotadas e imutáveis `v1.3.4` e `desktop-v1.3.4`, ambas apontando para o commit de preparação `e6ab583ce`. A GitHub Release [v1.3.4](https://github.com/Lucas-Belucci-Bellini/Projeto-Baluarte/releases/tag/v1.3.4) foi publicada como a release mais recente, e o workflow Desktop Release anexou os instaladores Linux, macOS e Windows. O service worker foi avançado para `baluarte-v1.3.4`, impedindo reaproveitamento do cache `baluarte-v1.3.3`.

O rollback continuará sendo feito por novo commit para a release anterior publicada, sem reescrever histórico. A release 1.3.3 permanece imutável.

## Próximo passo

Depois da validação e publicação desta correção, o próximo slice da V2 continuará separado desta mudança visual, com gates browser e de Runtime independentes. Esta release não declara estabilidade geral da V2.
