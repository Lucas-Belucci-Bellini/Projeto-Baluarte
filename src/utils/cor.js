/**
 * Compatibilidade temporária para consumidores JavaScript.
 * A implementação canônica está em `cor.ts`.
 */
export {
  clamp,
  hexToRgb,
  rgbToHex,
  rgbToHsl,
  hslToRgb,
  srgbToLinear,
  rgbToOklch,
  relLuminance,
  contrastRatio,
} from './cor.ts';
