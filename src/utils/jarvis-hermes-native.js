/**
 * Compatibilidade temporária para consumidores JavaScript.
 * A implementação canônica está em `jarvis-hermes-native.ts`.
 */
export { nativeHermesStatus, makeNativeBrain } from './jarvis-hermes-native.ts';
