/**
 * Compatibilidade temporária para consumidores JavaScript.
 * A implementação canônica está em `baluarte-status.ts`.
 */
export {
  setStatus,
  clearStatus,
  setCurrentFunction,
  getStatusSnapshot,
  getStatusText
} from './baluarte-status.ts';
