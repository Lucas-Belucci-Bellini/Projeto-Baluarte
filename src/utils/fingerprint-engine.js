/**
 * Compatibilidade temporária para consumidores JavaScript.
 * A implementação canônica está em `fingerprint-engine.ts`.
 */
export { cosine, createFingerprintDB } from './fingerprint-engine.ts';
