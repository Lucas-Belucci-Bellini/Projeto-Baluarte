/**
 * Compatibilidade temporária para consumidores JavaScript.
 * A implementação canônica está em `theme.ts`.
 */
export {
  THEMES,
  applyTheme,
  getThemeId,
  setTheme,
  initTheme,
} from './theme.ts';
