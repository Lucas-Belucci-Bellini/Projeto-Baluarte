/**
 * Compatibilidade temporária para consumidores JavaScript.
 * A implementação canônica está em `triangulation.ts`.
 */
export {
  bearingTo,
  triangulate,
  gaussianNoise,
  dist,
} from './triangulation.ts';
