/**
 * Compatibilidade temporária para consumidores JavaScript.
 * A implementação canônica está em `jarvis-repo-memory.ts`.
 */
export { repoEnabled, saveEntry, listEntries } from './jarvis-repo-memory.ts';
