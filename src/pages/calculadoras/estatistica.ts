import { h } from '../../utils/helpers.js';

interface DescriptiveStats { readonly n: number; readonly sum: number; readonly mean: number; readonly min: number; readonly max: number; readonly range: number; readonly median: number; readonly q1: number; readonly q3: number; readonly iqr: number; readonly std: number; readonly stdp: number; readonly variance: number; readonly variancep: number; readonly cv: number; readonly modes: readonly number[]; }
const NUM = (value: string): number => Number.parseFloat(value.replace(',', '.'));
function fmt(value: number, decimals = 4): string { if (!Number.isFinite(value)) return '—'; if (Number.isInteger(value) && Math.abs(value) < 1e15) return new Intl.NumberFormat('pt-BR').format(value); return new Intl.NumberFormat('pt-BR', { maximumFractionDigits: decimals }).format(value); }
function parseList(value: string): number[] { return value.split(/[\s,;]+/).map(NUM).filter((number) => !Number.isNaN(number)); }
function describe(values: readonly number[]): DescriptiveStats | null { const n = values.length; if (!n) return null; const sorted = [...values].sort((left, right) => left - right); const sum = values.reduce((total, value) => total + value, 0); const mean = sum / n; const min = sorted[0] ?? Number.NaN; const max = sorted[n - 1] ?? Number.NaN; const variance = values.reduce((total, value) => total + (value - mean) ** 2, 0) / n; const variancep = values.reduce((total, value) => total + (value - mean) ** 2, 0) / Math.max(n - 1, 1); const std = Math.sqrt(variance); const stdp = Math.sqrt(variancep); const quantile = (percentile: number): number => { const index = (n - 1) * percentile; const lower = Math.floor(index); const upper = Math.ceil(index); const lowValue = sorted[lower] ?? Number.NaN; const highValue = sorted[upper] ?? Number.NaN; return lowValue + (index - lower) * (highValue - lowValue); }; const median = quantile(0.5); const q1 = quantile(0.25); const q3 = quantile(0.75); const counts = new Map<number, number>(); values.forEach((value) => counts.set(value, (counts.get(value) ?? 0) + 1)); let maxFrequency = 0; counts.forEach((count) => { if (count > maxFrequency) maxFrequency = count; }); const modes = maxFrequency > 1 ? [...counts.entries()].filter(([, count]) => count === maxFrequency).map(([value]) => value) : []; return { n, sum, mean, min, max, range: max - min, median, q1, q3, iqr: q3 - q1, std, stdp, variance, variancep, cv: mean !== 0 ? (std / Math.abs(mean)) * 100 : Number.NaN, modes }; }
function statsPanel(): HTMLDivElement { const card = h('div', { className: 'calc-tile' }); const input = h('textarea', { className: 'input', rows: 4, placeholder: '5, 8, 12, 15, 18, 22, 25, 30, 35, 42', value: '5, 8, 12, 15, 18, 22, 25, 30, 35, 42', oninput: render }); const out = h('div', { className: 'calc-tile__out' }); function render(): void { const values = parseList(input.value); if (!values.length) { out.innerHTML = '<div class="u-text-muted">Insira ao menos um número.</div>'; return; } const stats = describe(values); if (!stats) return; out.innerHTML = `<div class="stats-grid"><div class="stat-cell"><span>n</span><strong>${fmt(stats.n)}</strong></div><div class="stat-cell"><span>Σ soma</span><strong>${fmt(stats.sum)}</strong></div><div class="stat-cell"><span>μ média</span><strong>${fmt(stats.mean)}</strong></div><div class="stat-cell"><span>md mediana</span><strong>${fmt(stats.median)}</strong></div><div class="stat-cell"><span>min</span><strong>${fmt(stats.min)}</strong></div><div class="stat-cell"><span>max</span><strong>${fmt(stats.max)}</strong></div><div class="stat-cell"><span>amplitude</span><strong>${fmt(stats.range)}</strong></div><div class="stat-cell"><span>σ desvio (pop)</span><strong>${fmt(stats.std)}</strong></div><div class="stat-cell"><span>s desvio (amostral)</span><strong>${fmt(stats.stdp)}</strong></div><div class="stat-cell"><span>σ² variância</span><strong>${fmt(stats.variance)}</strong></div><div class="stat-cell"><span>Q1</span><strong>${fmt(stats.q1)}</strong></div><div class="stat-cell"><span>Q3</span><strong>${fmt(stats.q3)}</strong></div><div class="stat-cell"><span>IQR</span><strong>${fmt(stats.iqr)}</strong></div><div class="stat-cell"><span>CV %</span><strong>${fmt(stats.cv, 2)}</strong></div><div class="stat-cell stat-cell--wide"><span>moda</span><strong>${stats.modes.length ? stats.modes.map((mode) => fmt(mode)).join(', ') : '— sem moda'}</strong></div></div>`; } card.append(h('h3', { className: 'calc-tile__title' }, 'σ Estatística Descritiva'), h('div', { className: 'calc-tile__grid' }, h('label', null, 'Dados (vírgula, espaço ou nova linha)', input)), out); setTimeout(render, 0); return card; }
function regressionPanel(): HTMLDivElement { const card = h('div', { className: 'calc-tile' }); const xs = h('textarea', { className: 'input', rows: 3, placeholder: '1, 2, 3, 4, 5', value: '1, 2, 3, 4, 5', oninput: render }); const ys = h('textarea', { className: 'input', rows: 3, placeholder: '2.1, 4.3, 6.0, 8.2, 10.1', value: '2.1, 4.3, 6.0, 8.2, 10.1', oninput: render }); const xQuery = h('input', { className: 'input', type: 'number', value: '6', oninput: render }); const out = h('div', { className: 'calc-tile__out' }); function render(): void { const x = parseList(xs.value); const y = parseList(ys.value); const n = Math.min(x.length, y.length); if (n < 2) { out.innerHTML = '<div class="u-text-muted">Pelo menos 2 pares (x, y).</div>'; return; } const sx = x.slice(0, n).reduce((sum, value) => sum + value, 0); const sy = y.slice(0, n).reduce((sum, value) => sum + value, 0); const sxy = x.slice(0, n).reduce((sum, value, index) => sum + value * (y[index] ?? 0), 0); const sxx = x.slice(0, n).reduce((sum, value) => sum + value * value, 0); const syy = y.slice(0, n).reduce((sum, value) => sum + value * value, 0); const a = (n * sxy - sx * sy) / (n * sxx - sx * sx); const b = (sy - a * sx) / n; const numerator = n * sxy - sx * sy; const denominator = Math.sqrt((n * sxx - sx * sx) * (n * syy - sy * sy)); const r = denominator === 0 ? 0 : numerator / denominator; const r2 = r * r; const query = NUM(xQuery.value); const prediction = a * query + b; out.innerHTML = `<div class="calc-tile__result"><div><span>Equação:</span> <strong>y = ${fmt(a)} · x ${b >= 0 ? '+' : '-'} ${fmt(Math.abs(b))}</strong></div><div><span>Coef. Pearson (r):</span> <strong>${fmt(r)}</strong></div><div><span>R² (determinação):</span> <strong>${fmt(r2)}</strong></div><div><span>Ajuste:</span> <strong>${r2 > 0.9 ? '🟢 Forte' : r2 > 0.5 ? '🟡 Moderado' : '🔴 Fraco'}</strong></div><div><span>Predição f(${fmt(query)}):</span> <strong>${fmt(prediction)}</strong></div></div><div class="calc-tile__formula">a = (n·Σxy - Σx·Σy) / (n·Σx² - (Σx)²) · b = (Σy - a·Σx) / n</div>`; } card.append(h('h3', { className: 'calc-tile__title' }, '∝ Regressão Linear'), h('div', { className: 'calc-tile__grid' }, h('label', null, 'X (independente)', xs), h('label', null, 'Y (dependente)', ys), h('label', null, 'Predizer y para x =', xQuery)), out); setTimeout(render, 0); return card; }
export function estatisticaPanel(): HTMLDivElement { return h('div', { className: 'calc-grid' }, statsPanel(), regressionPanel()); }
