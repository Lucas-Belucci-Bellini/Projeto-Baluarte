/**
 * Compatibilidade temporária para consumidores JavaScript durante a migração.
 * A implementação canônica está em `sidebar.ts`.
 */
export {
  NAV_GROUPS,
  renderSidebar,
  updateActiveNav,
  wireSidebar,
} from './sidebar.ts';
