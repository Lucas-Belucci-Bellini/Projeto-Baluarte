/**
 * Compatibilidade temporária para consumidores JavaScript durante a migração.
 * A implementação canônica está em `shell.ts`.
 */
export {
  getShellRefs,
  mountShell,
  pinCurrentPage,
  renderPage,
} from './shell.ts';
