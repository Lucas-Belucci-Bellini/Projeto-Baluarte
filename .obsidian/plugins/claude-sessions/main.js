/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/utils/logger.ts
var LOG_LEVEL_PRIORITY, PREFIX, _Logger, Logger;
var init_logger = __esm({
  "src/utils/logger.ts"() {
    LOG_LEVEL_PRIORITY = {
      none: 0,
      error: 1,
      warn: 2,
      info: 3,
      debug: 4
    };
    PREFIX = "[claude-sessions]";
    _Logger = class _Logger {
      static init(settings) {
        _Logger.settings = settings;
      }
      static shouldLog(level) {
        if (!_Logger.settings) return false;
        return LOG_LEVEL_PRIORITY[level] <= LOG_LEVEL_PRIORITY[_Logger.settings.debugLevel];
      }
      static debug(message, data) {
        if (!_Logger.shouldLog("debug")) return;
        if (data !== void 0) console.debug(`${PREFIX} ${message}`, data);
        else console.debug(`${PREFIX} ${message}`);
      }
      static info(message, data) {
        if (!_Logger.shouldLog("info")) return;
        if (data !== void 0) console.debug(`${PREFIX} ${message}`, data);
        else console.debug(`${PREFIX} ${message}`);
      }
      static warn(message, data) {
        if (!_Logger.shouldLog("warn")) return;
        if (data !== void 0) console.warn(`${PREFIX} ${message}`, data);
        else console.warn(`${PREFIX} ${message}`);
      }
      static error(message, data) {
        if (!_Logger.shouldLog("error")) return;
        if (data !== void 0) console.error(`${PREFIX} ${message}`, data);
        else console.error(`${PREFIX} ${message}`);
      }
    };
    _Logger.settings = null;
    Logger = _Logger;
  }
});

// src/utils/rate-limits.ts
var rate_limits_exports = {};
__export(rate_limits_exports, {
  clearRateLimitCache: () => clearRateLimitCache,
  fetchRateLimits: () => fetchRateLimits
});
function getAccessToken() {
  var _a, _b;
  if (!import_obsidian2.Platform.isDesktop) {
    Logger.debug("[rate-limits] not desktop, skipping token retrieval");
    return null;
  }
  const home = process.env.HOME || process.env.USERPROFILE || "";
  if (process.platform === "darwin") {
    try {
      Logger.debug("[rate-limits] attempting keychain lookup...");
      const raw = (0, import_child_process.execSync)(
        'security find-generic-password -s "Claude Code-credentials" -w 2>/dev/null',
        { encoding: "utf-8", timeout: 3e3 }
      ).trim();
      if (raw) {
        const parsed = JSON.parse(raw);
        const creds = (_a = parsed.claudeAiOauth) != null ? _a : parsed;
        if (creds.accessToken) {
          if (creds.expiresAt && creds.expiresAt <= Date.now()) {
            Logger.debug("[rate-limits] keychain token past expiresAt, trying anyway", {
              expiresAt: new Date(creds.expiresAt).toISOString()
            });
          } else {
            Logger.debug("[rate-limits] keychain token found and valid");
          }
          return creds.accessToken;
        }
      }
      Logger.debug("[rate-limits] keychain entry found but no accessToken");
    } catch (err) {
      Logger.debug("[rate-limits] keychain lookup failed, trying file", err);
    }
  }
  const credPath = path.join(home, ".claude", ".credentials.json");
  Logger.debug("[rate-limits] checking credentials file:", credPath);
  try {
    if (!fs.existsSync(credPath)) {
      Logger.debug("[rate-limits] credentials file not found");
      return null;
    }
    const parsed = JSON.parse(fs.readFileSync(credPath, "utf-8"));
    const creds = (_b = parsed.claudeAiOauth) != null ? _b : parsed;
    if (creds.accessToken) {
      if (creds.expiresAt && creds.expiresAt <= Date.now()) {
        Logger.debug("[rate-limits] file token past expiresAt, trying anyway", {
          expiresAt: new Date(creds.expiresAt).toISOString()
        });
      } else {
        Logger.debug("[rate-limits] file token found and valid");
      }
      return creds.accessToken;
    }
    Logger.debug("[rate-limits] credentials file parsed but no accessToken");
  } catch (err) {
    Logger.error("[rate-limits] failed to read credentials file", err);
  }
  return null;
}
async function fetchRateLimits() {
  if (cached && Date.now() - cached.fetchedAt < CACHE_TTL_MS) {
    const age = Math.round((Date.now() - cached.fetchedAt) / 1e3);
    Logger.debug(`[rate-limits] returning cached data (${age}s old)`);
    return cached.data;
  }
  if (cached && Date.now() - cached.fetchedAt >= MAX_STALE_MS) {
    const age = Math.round((Date.now() - cached.fetchedAt) / 1e3 / 60);
    Logger.debug(`[rate-limits] clearing stale cache (${age}m old)`);
    cached = null;
  }
  if (inflight) {
    Logger.debug("[rate-limits] request already inflight, deduping");
    return inflight;
  }
  Logger.debug("[rate-limits] fetching fresh data...");
  inflight = (async () => {
    var _a, _b, _c, _d, _e, _f;
    try {
      const token = getAccessToken();
      if (!token) {
        Logger.warn("[rate-limits] no OAuth token found");
        return null;
      }
      Logger.debug("[rate-limits] token found, calling API...");
      const response = await (0, import_obsidian2.requestUrl)({
        url: "https://api.anthropic.com/api/oauth/usage",
        method: "GET",
        headers: {
          "Authorization": `Bearer ${token}`,
          "anthropic-beta": "oauth-2025-04-20",
          "Content-Type": "application/json"
        }
      });
      if (response.status !== 200) {
        if (response.status === 401) {
          Logger.warn("[rate-limits] API returned 401 - token truly expired or invalid");
        } else {
          Logger.warn(`[rate-limits] API returned status ${response.status}`, response.text);
        }
        return null;
      }
      const body = response.json;
      Logger.debug("[rate-limits] API response:", JSON.stringify(body, null, 2));
      const clamp = (v) => {
        if (v == null || typeof v !== "number" || !isFinite(v)) return null;
        return Math.max(0, Math.min(100, v));
      };
      const data = {
        fiveHourPercent: clamp((_a = body.five_hour) == null ? void 0 : _a.utilization),
        fiveHourResetsAt: (_c = (_b = body.five_hour) == null ? void 0 : _b.resets_at) != null ? _c : null,
        weeklyPercent: clamp((_d = body.seven_day) == null ? void 0 : _d.utilization),
        weeklyResetsAt: (_f = (_e = body.seven_day) == null ? void 0 : _e.resets_at) != null ? _f : null
      };
      cached = { data, fetchedAt: Date.now() };
      Logger.debug("[rate-limits] cached fresh data:", data);
      return data;
    } catch (err) {
      Logger.error("[rate-limits] fetch failed:", err);
      return null;
    } finally {
      inflight = null;
    }
  })();
  return inflight;
}
function clearRateLimitCache() {
  cached = null;
}
var import_obsidian2, fs, path, import_child_process, CACHE_TTL_MS, MAX_STALE_MS, cached, inflight;
var init_rate_limits = __esm({
  "src/utils/rate-limits.ts"() {
    import_obsidian2 = require("obsidian");
    fs = __toESM(require("fs"));
    path = __toESM(require("path"));
    import_child_process = require("child_process");
    init_logger();
    CACHE_TTL_MS = 5 * 60 * 1e3;
    MAX_STALE_MS = 15 * 60 * 1e3;
    cached = null;
    inflight = null;
  }
});

// src/constants.ts
var RT_USER, RT_ASSISTANT, RT_PROGRESS, RT_QUEUE_OPERATION, RT_FILE_HISTORY, RT_LAST_PROMPT, RT_SUMMARY, RT_SYSTEM, RT_CUSTOM_TITLE, RT_AGENT_NAME, SKIP_RECORD_TYPES, SKIP_TYPE_STRINGS, CUSTOM_TITLE_PATTERN, BT_TEXT, BT_THINKING, BT_TOOL_USE, BT_TOOL_RESULT, BT_IMAGE, PROGRESS_AGENT, SUBAGENT_TOOL_NAMES, MODEL_SYNTHETIC, SUBTYPE_LOCAL_COMMAND, TAG_TASK_NOTIFICATION, TAG_COMMAND_MESSAGE_OPEN, RE_COMMAND_NAME, RE_COMMAND_ARGS, RE_EXIT_COMMAND, RE_SLASH_COMMAND, RE_LOCAL_STDOUT, RE_LOCAL_CAVEAT, RE_LOCAL_STDERR, RE_SYSTEM_REMINDER, RE_COMMAND_MESSAGE_STRIP, RE_COMMAND_ARGS_STRIP, RE_IMAGE_REF, RE_TOOL_USE_ERROR, RE_LOCAL_STDOUT_TAGS, RE_BASH_INPUT, RE_BASH_STDOUT, RE_BASH_STDERR, RE_TN_TOOL_USE_ID, RE_TN_TASK_ID, RE_TN_RESULT, RE_TN_SUMMARY, RE_TN_DURATION, RE_AGENT_ID, TEXT_SESSION_ENDED, TEXT_INTERRUPTION, PREFIX_INTERRUPTION, ESC, ANSI_RE, ANSI_STRIP_RE, ANSI_PARSE_RE, ANSI_COMMANDS, TASK_TOOL_NAMES, DURATION_GAP_THRESHOLD_MS;
var init_constants = __esm({
  "src/constants.ts"() {
    RT_USER = "user";
    RT_ASSISTANT = "assistant";
    RT_PROGRESS = "progress";
    RT_QUEUE_OPERATION = "queue-operation";
    RT_FILE_HISTORY = "file-history-snapshot";
    RT_LAST_PROMPT = "last-prompt";
    RT_SUMMARY = "summary";
    RT_SYSTEM = "system";
    RT_CUSTOM_TITLE = "custom-title";
    RT_AGENT_NAME = "agent-name";
    SKIP_RECORD_TYPES = /* @__PURE__ */ new Set([RT_FILE_HISTORY, RT_LAST_PROMPT, RT_PROGRESS, RT_QUEUE_OPERATION, RT_AGENT_NAME]);
    SKIP_TYPE_STRINGS = [
      '"type":"file-history-snapshot"',
      '"type":"queue-operation"',
      '"type":"progress"',
      '"type":"last-prompt"',
      '"type":"custom-title"',
      '"type":"agent-name"'
    ];
    CUSTOM_TITLE_PATTERN = '"type":"custom-title"';
    BT_TEXT = "text";
    BT_THINKING = "thinking";
    BT_TOOL_USE = "tool_use";
    BT_TOOL_RESULT = "tool_result";
    BT_IMAGE = "image";
    PROGRESS_AGENT = "agent_progress";
    SUBAGENT_TOOL_NAMES = /* @__PURE__ */ new Set(["Agent", "Task"]);
    MODEL_SYNTHETIC = "<synthetic>";
    SUBTYPE_LOCAL_COMMAND = "local_command";
    TAG_TASK_NOTIFICATION = "<task-notification>";
    TAG_COMMAND_MESSAGE_OPEN = "<command-message>";
    RE_COMMAND_NAME = /<command-name>(\/[\w:./-]+)<\/command-name>/;
    RE_COMMAND_ARGS = /<command-args>([\s\S]*?)<\/command-args>/;
    RE_EXIT_COMMAND = /^(?:<command-message>[\s\S]*?<\/command-message>\s*)?<command-name>\/exit<\/command-name>/;
    RE_SLASH_COMMAND = /^(?:<command-message>[\s\S]*?<\/command-message>\s*)?<command-name>(\/[\w:./-]+)<\/command-name>/;
    RE_LOCAL_STDOUT = /^<local-command-stdout>/;
    RE_LOCAL_CAVEAT = /^<local-command-caveat>/;
    RE_LOCAL_STDERR = /^<local-command-stderr>/;
    RE_SYSTEM_REMINDER = /<system-reminder>[\s\S]*?<\/system-reminder>/g;
    RE_COMMAND_MESSAGE_STRIP = /<command-message>[\s\S]*?<\/command-message>/g;
    RE_COMMAND_ARGS_STRIP = /<command-args>[\s\S]*?<\/command-args>/g;
    RE_IMAGE_REF = /\[Image:\s*source:\s*.+?\]/gi;
    RE_TOOL_USE_ERROR = /^<tool_use_error>([\s\S]*?)<\/tool_use_error>\s*$/;
    RE_LOCAL_STDOUT_TAGS = /<\/?local-command-stdout>/g;
    RE_BASH_INPUT = /^<bash-input>([\s\S]*?)<\/bash-input>/;
    RE_BASH_STDOUT = /^<bash-stdout>([\s\S]*?)<\/bash-stdout>/;
    RE_BASH_STDERR = /<bash-stderr>([\s\S]*?)<\/bash-stderr>/;
    RE_TN_TOOL_USE_ID = /<tool-use-id>([\s\S]*?)<\/tool-use-id>/;
    RE_TN_TASK_ID = /<task-id>([\s\S]*?)<\/task-id>/;
    RE_TN_RESULT = /<result>([\s\S]*?)<\/result>/;
    RE_TN_SUMMARY = /<summary>([\s\S]*?)<\/summary>/;
    RE_TN_DURATION = /<duration_ms>([\s\S]*?)<\/duration_ms>/;
    RE_AGENT_ID = /agentId:\s*(\S+)/;
    TEXT_SESSION_ENDED = "*Session ended*";
    TEXT_INTERRUPTION = "*Request interrupted by user*";
    PREFIX_INTERRUPTION = "[Request interrupted by user";
    ESC = String.fromCharCode(27);
    ANSI_RE = new RegExp(ESC + "\\[[\\d;]*m");
    ANSI_STRIP_RE = new RegExp(ESC + "\\[[\\d;]*m", "g");
    ANSI_PARSE_RE = new RegExp(ESC + "\\[([\\d;]*)m", "g");
    ANSI_COMMANDS = /* @__PURE__ */ new Set(["/context"]);
    TASK_TOOL_NAMES = /* @__PURE__ */ new Set(["TaskCreate", "TaskUpdate", "TaskList", "TaskGet"]);
    DURATION_GAP_THRESHOLD_MS = 30 * 60 * 1e3;
  }
});

// src/utils/path-utils.ts
function expandHome(path5) {
  if (!path5.startsWith("~")) return path5;
  if (import_obsidian6.Platform.isDesktop) {
    try {
      return path5.replace(/^~/, os.homedir());
    } catch (e) {
      return path5;
    }
  }
  return path5;
}
function basename(path5) {
  const parts = path5.replace(/\\/g, "/").split("/");
  return parts[parts.length - 1] || "";
}
function dirname(path5) {
  const normalized = path5.replace(/\\/g, "/");
  const lastSlash = normalized.lastIndexOf("/");
  if (lastSlash === -1) return ".";
  return normalized.substring(0, lastSlash);
}
function shortenPath(fullPath) {
  if (import_obsidian6.Platform.isDesktop) {
    try {
      const home = os.homedir();
      if (fullPath.startsWith(home)) {
        return "~" + fullPath.slice(home.length);
      }
    } catch (e) {
    }
  }
  return fullPath;
}
function projectFromCwd(cwd) {
  return basename(cwd) || "unknown";
}
function extractProjectName(dirPath) {
  const parts = dirPath.replace(/\\/g, "/").split("/").filter(Boolean);
  const last = parts[parts.length - 1] || "unknown";
  if (last.startsWith("-")) {
    const decoded = last.split("-").filter(Boolean);
    return decoded[decoded.length - 1] || last;
  }
  return last;
}
var import_obsidian6, os;
var init_path_utils = __esm({
  "src/utils/path-utils.ts"() {
    import_obsidian6 = require("obsidian");
    os = __toESM(require("os"));
  }
});

// src/utils/streaming-reader.ts
async function readFileContent(filePath, onProgress) {
  if (import_obsidian10.Platform.isDesktop) {
    return readDesktop(filePath, onProgress);
  }
  new import_obsidian10.Notice("On mobile, use vault files instead of filesystem paths.");
  throw new Error("Filesystem access is not available on mobile.");
}
async function readDesktop(filePath, onProgress) {
  const resolved = path2.resolve(filePath);
  return new Promise((resolve2, reject) => {
    let stat;
    try {
      stat = fs2.statSync(resolved);
    } catch (e) {
      reject(new Error(`File not found: ${resolved}`));
      return;
    }
    const totalBytes = stat.size;
    let bytesRead = 0;
    const chunks = [];
    const stream = fs2.createReadStream(resolved, { encoding: "utf-8" });
    stream.on("data", (chunk) => {
      chunks.push(chunk);
      bytesRead += Buffer.byteLength(chunk, "utf-8");
      if (onProgress) {
        onProgress({ bytesRead, totalBytes });
      }
    });
    stream.on("end", () => {
      resolve2(chunks.join(""));
    });
    stream.on("error", (err) => {
      reject(err);
    });
  });
}
async function extractQuickMetadataAsync(filePath) {
  if (!import_obsidian10.Platform.isDesktop) return { hasContent: false };
  const result = { hasContent: false };
  const MAX_LINES_FOR_BASIC = 100;
  let lineCount = 0;
  let basicMetaDone = false;
  return new Promise((resolve2) => {
    const stream = fs2.createReadStream(filePath, { encoding: "utf-8" });
    const rl = readline.createInterface({ input: stream, crlfDelay: Infinity });
    const isBasicDone = () => result.sessionId !== void 0 && result.cwd !== void 0 && result.startTime !== void 0 && result.hasContent;
    rl.on("line", (line) => {
      lineCount++;
      const trimmed = line.trim();
      if (!trimmed) return;
      if (trimmed.includes(CUSTOM_TITLE_PATTERN)) {
        try {
          const record = JSON.parse(trimmed);
          if (record["type"] === "custom-title" && typeof record["customTitle"] === "string") {
            result.customTitle = record["customTitle"];
          }
        } catch (e) {
        }
        return;
      }
      if (basicMetaDone || lineCount > MAX_LINES_FOR_BASIC) {
        if (!basicMetaDone && lineCount > MAX_LINES_FOR_BASIC) {
          basicMetaDone = true;
        }
        return;
      }
      const head = trimmed.length > 200 ? trimmed.slice(0, 200) : trimmed;
      for (const sub of SKIP_TYPE_STRINGS) {
        if (head.includes(sub)) return;
      }
      try {
        const record = JSON.parse(trimmed);
        const recordType = record["type"];
        if (recordType === "user" || recordType === "assistant") {
          result.hasContent = true;
        }
        if (typeof record["sessionId"] === "string" && !result.sessionId) {
          result.sessionId = record["sessionId"];
        }
        if (typeof record["cwd"] === "string" && !result.cwd) {
          result.cwd = record["cwd"];
        }
        if (typeof record["timestamp"] === "string" && !result.startTime) {
          result.startTime = record["timestamp"];
        }
      } catch (e) {
      }
      if (isBasicDone()) {
        basicMetaDone = true;
      }
    });
    rl.on("close", () => {
      resolve2(result);
    });
    stream.on("error", () => {
      resolve2(result);
    });
  });
}
function listDirectoryFiles(dirPath) {
  if (!import_obsidian10.Platform.isDesktop) return Promise.resolve([]);
  const resolved = path2.resolve(dirPath);
  try {
    const entries = fs2.readdirSync(resolved, { withFileTypes: true });
    const names = entries.filter((e) => e.isFile()).map((e) => e.name);
    return Promise.resolve(names);
  } catch (e) {
    return Promise.resolve([]);
  }
}
function listDirectory(dirPath) {
  if (!import_obsidian10.Platform.isDesktop) return [];
  const resolved = path2.resolve(dirPath);
  try {
    const entries = fs2.readdirSync(resolved, { withFileTypes: true });
    return entries.filter(
      (e) => e.isFile() && e.name.endsWith(".jsonl") && !e.name.startsWith("agent-")
    ).map((e) => path2.join(resolved, e.name));
  } catch (e) {
    return [];
  }
}
function listSubdirectories(dirPath) {
  if (!import_obsidian10.Platform.isDesktop) return [];
  const resolved = path2.resolve(dirPath);
  try {
    const entries = fs2.readdirSync(resolved, { withFileTypes: true });
    return entries.filter((e) => e.isDirectory()).map((e) => path2.join(resolved, e.name));
  } catch (e) {
    return [];
  }
}
var import_obsidian10, fs2, path2, readline;
var init_streaming_reader = __esm({
  "src/utils/streaming-reader.ts"() {
    import_obsidian10 = require("obsidian");
    fs2 = __toESM(require("fs"));
    path2 = __toESM(require("path"));
    readline = __toESM(require("readline"));
    init_constants();
  }
});

// src/parsers/base-parser.ts
var MAX_LINE_BYTES, BaseParser;
var init_base_parser = __esm({
  "src/parsers/base-parser.ts"() {
    init_logger();
    MAX_LINE_BYTES = 10 * 1024 * 1024;
    BaseParser = class {
      constructor() {
        /** Parse error count for diagnostics. Reset per-file in parse(). */
        this.parseErrorCount = 0;
      }
      splitLines(content) {
        const normalized = content.replace(/\r\n/g, "\n").replace(/\r/g, "\n");
        const stripped = normalized.startsWith("\uFEFF") ? normalized.slice(1) : normalized;
        return stripped.split("\n").filter((line) => line.trim().length > 0);
      }
      tryParseJson(line) {
        if (line.length > MAX_LINE_BYTES) {
          Logger.warn(`Skipping oversized JSONL line (${(line.length / 1024 / 1024).toFixed(1)} MB)`);
          this.parseErrorCount++;
          return null;
        }
        try {
          return JSON.parse(line);
        } catch (e) {
          this.parseErrorCount++;
          Logger.debug("JSONL parse error on line: " + line.slice(0, 80), e);
          return null;
        }
      }
      formatTimestamp(ts) {
        if (!ts) return void 0;
        try {
          return new Date(ts).toISOString();
        } catch (e) {
          return ts;
        }
      }
    };
  }
});

// src/parsers/claude-subagent.ts
async function withTimeout(promise, ms, label) {
  let timeoutId;
  const timeoutPromise = new Promise((_, reject) => {
    timeoutId = window.setTimeout(() => reject(new Error(`Timeout reading ${label}`)), ms);
  });
  try {
    return await Promise.race([promise, timeoutPromise]);
  } finally {
    window.clearTimeout(timeoutId);
  }
}
function parseTaskNotification(content) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m;
  const toolUseId = (_b = (_a = content.match(RE_TN_TOOL_USE_ID)) == null ? void 0 : _a[1]) == null ? void 0 : _b.trim();
  if (!toolUseId) return null;
  const taskId = (_e = (_d = (_c = content.match(RE_TN_TASK_ID)) == null ? void 0 : _c[1]) == null ? void 0 : _d.trim()) != null ? _e : "";
  const summary = (_h = (_g = (_f = content.match(RE_TN_SUMMARY)) == null ? void 0 : _f[1]) == null ? void 0 : _g.trim()) != null ? _h : "";
  const result = (_k = (_j = (_i = content.match(RE_TN_RESULT)) == null ? void 0 : _i[1]) == null ? void 0 : _j.trim()) != null ? _k : "";
  const durationRaw = (_m = (_l = content.match(RE_TN_DURATION)) == null ? void 0 : _l[1]) == null ? void 0 : _m.trim();
  const durationMs = durationRaw ? parseInt(durationRaw, 10) : void 0;
  return { taskId, toolUseId, result, summary, durationMs: durationMs && !isNaN(durationMs) ? durationMs : void 0 };
}
function tryParseJson(text) {
  try {
    return JSON.parse(text);
  } catch (e) {
    return null;
  }
}
async function resolveSubAgentSessions(session, readFile, listFiles) {
  var _a;
  const sessionBase = session.rawPath.replace(/\.jsonl$/, "");
  const subagentsDir = `${sessionBase}/subagents`;
  const parser = new ClaudeParser({ allowSidechain: true });
  const withId = [];
  const withoutId = [];
  const resolvedIds = /* @__PURE__ */ new Set();
  for (const turn of session.turns) {
    for (const block of turn.contentBlocks) {
      if (block.type === BT_TOOL_USE && SUBAGENT_TOOL_NAMES.has(block.name) && block.subAgentSession) {
        if (block.subAgentSession.agentId) {
          withId.push({ agentId: block.subAgentSession.agentId, block });
        } else {
          withoutId.push({
            description: (_a = block.subAgentSession.description) != null ? _a : "",
            block
          });
        }
      }
    }
  }
  for (const { agentId, block } of withId) {
    const subagentPath = `${subagentsDir}/agent-${agentId}.jsonl`;
    try {
      const content = await withTimeout(readFile(subagentPath), READ_TIMEOUT_MS, subagentPath);
      const subSession = parser.parse(content, subagentPath);
      block.subAgentSession.turns = subSession.turns;
      resolvedIds.add(agentId);
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      if (msg.includes("ENOENT") || msg.includes("not found")) {
        Logger.debug(`Sub-agent file not found (agent may be running): ${subagentPath}`);
      } else {
        Logger.warn(`Failed to read sub-agent file: ${subagentPath}`, err);
      }
    }
  }
  if (withoutId.length > 0 && listFiles) {
    try {
      const files = await listFiles(subagentsDir);
      const metaFiles = files.filter((f) => f.endsWith(".meta.json"));
      const descriptionToId = /* @__PURE__ */ new Map();
      for (const metaFile of metaFiles) {
        const agentId = metaFile.replace(/^agent-/, "").replace(/\.meta\.json$/, "");
        if (resolvedIds.has(agentId)) continue;
        const metaPath = `${subagentsDir}/${metaFile}`;
        try {
          const metaContent = await withTimeout(readFile(metaPath), READ_TIMEOUT_MS, metaPath);
          const meta = tryParseJson(metaContent);
          if (meta && typeof meta.description === "string") {
            descriptionToId.set(meta.description, agentId);
          }
        } catch (err) {
          Logger.debug(`Failed to read sub-agent meta: ${metaPath}`, err);
        }
      }
      for (const { description, block } of withoutId) {
        const agentId = descriptionToId.get(description);
        if (!agentId) continue;
        const subagentPath = `${subagentsDir}/agent-${agentId}.jsonl`;
        try {
          const content = await withTimeout(readFile(subagentPath), READ_TIMEOUT_MS, subagentPath);
          const subSession = parser.parse(content, subagentPath);
          block.subAgentSession.agentId = agentId;
          block.subAgentSession.turns = subSession.turns;
          descriptionToId.delete(description);
          resolvedIds.add(agentId);
        } catch (err) {
          const msg = err instanceof Error ? err.message : String(err);
          if (!msg.includes("ENOENT") && !msg.includes("not found")) {
            Logger.warn(`Failed to read sub-agent file: ${subagentPath}`, err);
          }
        }
      }
    } catch (err) {
      Logger.debug("Sub-agents directory not found or inaccessible", err);
    }
  }
}
var READ_TIMEOUT_MS;
var init_claude_subagent = __esm({
  "src/parsers/claude-subagent.ts"() {
    init_claude_parser();
    init_constants();
    init_logger();
    READ_TIMEOUT_MS = 5e3;
  }
});

// src/parsers/claude-content.ts
function parseContentBlock(block, toolUseNames, timestamp, unknownBlockTypes) {
  switch (block.type) {
    case BT_TEXT:
      if (block.text && block.text.trim()) {
        return { type: "text", text: block.text, timestamp };
      }
      return null;
    case BT_THINKING:
      if (block.thinking && block.thinking.trim()) {
        return { type: "thinking", thinking: block.thinking, timestamp };
      }
      return null;
    case BT_TOOL_USE:
      if (block.id && block.name) {
        toolUseNames.set(block.id, block.name);
        return {
          type: "tool_use",
          id: block.id,
          name: block.name,
          input: block.input || {},
          timestamp
        };
      }
      return null;
    default:
      if (unknownBlockTypes && block.type) {
        const existing = unknownBlockTypes.get(block.type);
        if (existing) {
          existing.count++;
        } else {
          const sample = { type: block.type, keys: Object.keys(block).filter((k) => k !== "type") };
          unknownBlockTypes.set(block.type, { count: 1, sample });
        }
      }
      return null;
  }
}
function extractToolResultBlocks(msg, toolUseNames, timestamp) {
  var _a, _b, _c, _d;
  if (!msg) return [];
  const results = [];
  if (Array.isArray(msg.content)) {
    for (const block of msg.content) {
      if (block.type === BT_TOOL_RESULT && block.tool_use_id) {
        let resultContent = "";
        let images;
        if (typeof block.content === "string") {
          resultContent = block.content;
        } else if (Array.isArray(block.content)) {
          const texts = [];
          for (const c of block.content) {
            if (c.type === "image" && ((_a = c.source) == null ? void 0 : _a.data)) {
              if (!images) images = [];
              images.push({
                mediaType: (_b = c.source.media_type) != null ? _b : "image/png",
                data: c.source.data
              });
            } else if (c.type === "tool_reference" && c.tool_name) {
              texts.push(c.tool_name);
            } else {
              const t = (_d = (_c = c.text) != null ? _c : c.content) != null ? _d : "";
              if (t) texts.push(t);
            }
          }
          resultContent = texts.join("\n");
        }
        if (block.is_error) {
          const m = RE_TOOL_USE_ERROR.exec(resultContent);
          if (m) resultContent = m[1].trim();
        }
        const result = {
          type: "tool_result",
          toolUseId: block.tool_use_id,
          toolName: toolUseNames.get(block.tool_use_id),
          content: resultContent,
          isError: block.is_error || false,
          timestamp
        };
        if (images) result.images = images;
        results.push(result);
      }
    }
  }
  return results;
}
function isInterruptionMessage(msg) {
  var _a;
  if (!msg) return false;
  const content = msg.content;
  if (typeof content === "string") {
    return content.startsWith(PREFIX_INTERRUPTION);
  }
  if (Array.isArray(content)) {
    for (const block of content) {
      if (block.type === BT_TEXT && ((_a = block.text) == null ? void 0 : _a.startsWith(PREFIX_INTERRUPTION))) {
        return true;
      }
    }
  }
  return false;
}
var init_claude_content = __esm({
  "src/parsers/claude-content.ts"() {
    init_constants();
  }
});

// src/parsers/claude-parser.ts
var claude_parser_exports = {};
__export(claude_parser_exports, {
  ClaudeParser: () => ClaudeParser
});
function hasAnsiCodes2(text) {
  return ANSI_RE.test(text);
}
function getPricing(model) {
  const m = model.toLowerCase();
  if (m.includes("opus")) return MODEL_PRICING.opus;
  if (m.includes("haiku")) return MODEL_PRICING.haiku;
  return MODEL_PRICING.sonnet;
}
function estimateCost(model, input, output, cacheRead, cacheWrite) {
  const p = getPricing(model);
  return input / 1e6 * p.input + output / 1e6 * p.output + cacheRead / 1e6 * p.cacheRead + cacheWrite / 1e6 * p.cacheWrite;
}
var ClaudeParser, MODEL_PRICING;
var init_claude_parser = __esm({
  "src/parsers/claude-parser.ts"() {
    init_base_parser();
    init_path_utils();
    init_constants();
    init_claude_subagent();
    init_logger();
    init_claude_content();
    ClaudeParser = class extends BaseParser {
      constructor(opts) {
        var _a;
        super();
        this.format = "claude";
        /** Track commands whose stdout should be captured. */
        this.pendingCommand = null;
        /** When true, next user blocks should merge into the preceding slash command turn. */
        this._pendingCommandResult = false;
        /** Track slash command name so the following isMeta skill expansion can be captured. */
        this._pendingSlashCommand = null;
        /** Track pending bash-input awaiting bash-stdout/stderr. */
        this._pendingBashCommand = null;
        this.allowSidechain = (_a = opts == null ? void 0 : opts.allowSidechain) != null ? _a : false;
      }
      canParse(firstLines) {
        for (const line of firstLines) {
          const record = this.tryParseJson(line);
          if (!record) continue;
          const type = record["type"];
          if (type === RT_USER || type === RT_ASSISTANT) {
            const msg = record["message"];
            if (msg && "role" in msg) return true;
          }
          if (type === RT_FILE_HISTORY || record["sessionId"]) return true;
        }
        return false;
      }
      parse(content, filePath) {
        var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v;
        this.parseErrorCount = 0;
        const lines = this.splitLines(content);
        const records = [];
        let sessionId = "";
        let cwd = "";
        let version = "";
        let branch = "";
        let model = "";
        let startTime = "";
        let customTitle = "";
        const agentProgressMap = /* @__PURE__ */ new Map();
        const usageByMsg = /* @__PURE__ */ new Map();
        let anonymousUsageCounter = 0;
        let lastCallInput = 0;
        let lastCallCacheRead = 0;
        let lastCallCacheWrite = 0;
        let compactionCount = 0;
        let peakContextTokens = 0;
        const enrichedResults = /* @__PURE__ */ new Map();
        const taskNotifications = /* @__PURE__ */ new Map();
        for (const line of lines) {
          const record = this.tryParseJson(line);
          if (!record) continue;
          if (record.type === RT_PROGRESS && ((_a = record.data) == null ? void 0 : _a.type) === PROGRESS_AGENT && record.parentToolUseID) {
            const group = (_b = agentProgressMap.get(record.parentToolUseID)) != null ? _b : [];
            group.push(record);
            agentProgressMap.set(record.parentToolUseID, group);
          }
          if (record.type === RT_ASSISTANT && ((_c = record.message) == null ? void 0 : _c.usage)) {
            const msgId = record.message["id"];
            let key;
            if (msgId) {
              key = msgId;
            } else if (record.uuid) {
              key = record.uuid;
            } else {
              const contentSig = typeof record.message.content === "string" ? record.message.content.slice(0, 200) : Array.isArray(record.message.content) ? record.message.content.map((b) => {
                var _a2, _b2;
                return `${b.type}:${((_b2 = (_a2 = b.text) != null ? _a2 : b.thinking) != null ? _b2 : "").slice(0, 50)}`;
              }).join("|") : "";
              key = `__anon_${contentSig}_${anonymousUsageCounter++}`;
            }
            const u = record.message.usage;
            const prev = usageByMsg.get(key);
            usageByMsg.set(key, {
              inp: Math.max((_d = prev == null ? void 0 : prev.inp) != null ? _d : 0, (_e = u.input_tokens) != null ? _e : 0),
              out: Math.max((_f = prev == null ? void 0 : prev.out) != null ? _f : 0, (_g = u.output_tokens) != null ? _g : 0),
              cr: Math.max((_h = prev == null ? void 0 : prev.cr) != null ? _h : 0, (_i = u.cache_read_input_tokens) != null ? _i : 0),
              cc: Math.max((_j = prev == null ? void 0 : prev.cc) != null ? _j : 0, (_k = u.cache_creation_input_tokens) != null ? _k : 0)
            });
            lastCallInput = (_l = u.input_tokens) != null ? _l : 0;
            lastCallCacheRead = (_m = u.cache_read_input_tokens) != null ? _m : 0;
            lastCallCacheWrite = (_n = u.cache_creation_input_tokens) != null ? _n : 0;
          }
          if (record.type === RT_USER && record.toolUseResult) {
            if (record.sourceToolUseID) {
              enrichedResults.set(record.sourceToolUseID, record.toolUseResult);
            } else if (Array.isArray((_o = record.message) == null ? void 0 : _o.content)) {
              for (const item of record.message.content) {
                if (item.type === BT_TOOL_RESULT && item.tool_use_id) {
                  enrichedResults.set(item.tool_use_id, record.toolUseResult);
                }
              }
            }
          }
          if (record.type === RT_QUEUE_OPERATION && ((_p = record.content) == null ? void 0 : _p.startsWith(TAG_TASK_NOTIFICATION))) {
            const tn = parseTaskNotification(record.content);
            if (tn) taskNotifications.set(tn.toolUseId, tn);
          }
          if (record.type === RT_USER && typeof ((_q = record.message) == null ? void 0 : _q.content) === "string" && record.message.content.startsWith(TAG_TASK_NOTIFICATION)) {
            const tn = parseTaskNotification(record.message.content);
            if (tn) taskNotifications.set(tn.toolUseId, tn);
          }
          if (record.type === RT_SYSTEM && record.subtype === "compact_boundary" && record.compactMetadata) {
            compactionCount++;
            if (typeof record.compactMetadata.preTokens === "number") {
              peakContextTokens = Math.max(peakContextTokens, record.compactMetadata.preTokens);
            }
          }
          if (record.type === RT_CUSTOM_TITLE) {
            const title = record.customTitle;
            if (title) customTitle = title;
            continue;
          }
          if (SKIP_RECORD_TYPES.has(record.type)) continue;
          if (record.isSidechain && !this.allowSidechain) continue;
          if (record.isMeta && record.type !== RT_USER) continue;
          if (record.type === RT_ASSISTANT && ((_r = record.message) == null ? void 0 : _r.model) === MODEL_SYNTHETIC) continue;
          if (record.type === RT_SUMMARY) {
            records.push(record);
            continue;
          }
          if (record.sessionId && !sessionId) sessionId = record.sessionId;
          if (record.cwd && !cwd) cwd = record.cwd;
          if (record.version && !version) version = record.version;
          if (record.gitBranch && !branch) branch = record.gitBranch;
          if (record.timestamp && !startTime) startTime = record.timestamp;
          if (((_s = record.message) == null ? void 0 : _s.model) && !model) model = record.message.model;
          records.push(record);
        }
        const uuidToIndex = /* @__PURE__ */ new Map();
        const ordered = [];
        for (const record of records) {
          if (record.uuid) {
            const existingIdx = uuidToIndex.get(record.uuid);
            if (existingIdx !== void 0) {
              ordered[existingIdx] = record;
            } else {
              uuidToIndex.set(record.uuid, ordered.length);
              ordered.push(record);
            }
          } else {
            ordered.push(record);
          }
        }
        const unknownRecordTypes = /* @__PURE__ */ new Map();
        const unknownBlockTypes = /* @__PURE__ */ new Map();
        const { turns, systemEvents } = this.buildTurns(ordered, unknownRecordTypes, unknownBlockTypes);
        if (agentProgressMap.size > 0) {
          for (const turn of turns) {
            for (const block of turn.contentBlocks) {
              if (block.type === BT_TOOL_USE && SUBAGENT_TOOL_NAMES.has(block.name) && agentProgressMap.has(block.id)) {
                const agentRecords = agentProgressMap.get(block.id);
                block.subAgentSession = this.buildSubAgentSession(agentRecords, block);
              }
            }
          }
        }
        if (taskNotifications.size > 0) {
          for (const turn of turns) {
            for (const block of turn.contentBlocks) {
              if (block.type === BT_TOOL_USE && SUBAGENT_TOOL_NAMES.has(block.name) && !block.subAgentSession && taskNotifications.has(block.id)) {
                const tn = taskNotifications.get(block.id);
                block.subAgentSession = {
                  agentId: tn.taskId,
                  description: typeof block.input["description"] === "string" ? block.input["description"] : "",
                  subagentType: typeof block.input["subagent_type"] === "string" ? block.input["subagent_type"] : "",
                  prompt: typeof block.input["prompt"] === "string" ? block.input["prompt"] : "",
                  turns: [],
                  isBackground: true,
                  durationMs: tn.durationMs
                };
              }
              if (block.type === BT_TOOL_RESULT && taskNotifications.has(block.toolUseId)) {
                block.content = taskNotifications.get(block.toolUseId).result;
              }
            }
          }
        }
        for (const turn of turns) {
          for (const block of turn.contentBlocks) {
            if (block.type === BT_TOOL_USE && SUBAGENT_TOOL_NAMES.has(block.name) && !block.subAgentSession) {
              const isBg = block.input["run_in_background"] === true;
              block.subAgentSession = {
                agentId: "",
                description: typeof block.input["description"] === "string" ? block.input["description"] : "",
                subagentType: typeof block.input["subagent_type"] === "string" ? block.input["subagent_type"] : "",
                prompt: typeof block.input["prompt"] === "string" ? block.input["prompt"] : "",
                turns: [],
                isBackground: isBg || void 0
              };
            }
          }
        }
        {
          const agentBlocksById = /* @__PURE__ */ new Map();
          for (const turn of turns) {
            for (const block of turn.contentBlocks) {
              if (block.type === BT_TOOL_USE && SUBAGENT_TOOL_NAMES.has(block.name) && block.subAgentSession && !block.subAgentSession.agentId) {
                agentBlocksById.set(block.id, block);
              }
            }
          }
          if (agentBlocksById.size > 0) {
            for (const turn of turns) {
              for (const block of turn.contentBlocks) {
                if (block.type === BT_TOOL_RESULT && agentBlocksById.has(block.toolUseId)) {
                  const match = (_t = block.content) == null ? void 0 : _t.match(RE_AGENT_ID);
                  if (match) {
                    agentBlocksById.get(block.toolUseId).subAgentSession.agentId = match[1];
                    agentBlocksById.delete(block.toolUseId);
                  }
                }
              }
            }
          }
        }
        const resultIds = /* @__PURE__ */ new Set();
        for (const turn of turns) {
          for (const block of turn.contentBlocks) {
            if (block.type === BT_TOOL_RESULT) {
              resultIds.add(block.toolUseId);
              if (enrichedResults.has(block.toolUseId)) {
                block.enrichedResult = enrichedResults.get(block.toolUseId);
              }
            }
          }
        }
        let lastAssistantTurn;
        for (let i = turns.length - 1; i >= 0; i--) {
          if (turns[i].role === "assistant") {
            lastAssistantTurn = turns[i];
            break;
          }
        }
        for (const turn of turns) {
          for (const block of turn.contentBlocks) {
            if (block.type === BT_TOOL_USE && !resultIds.has(block.id)) {
              if (turn === lastAssistantTurn) {
                block.isPending = true;
              } else {
                block.isOrphaned = true;
              }
            }
          }
        }
        const project = cwd ? projectFromCwd(cwd) : extractProjectName(dirname(filePath));
        const toolUseCounts = {};
        let userTurns = 0;
        let assistantTurns = 0;
        for (const turn of turns) {
          if (turn.role === "user") userTurns++;
          else assistantTurns++;
          for (const block of turn.contentBlocks) {
            if (block.type === BT_TOOL_USE) {
              toolUseCounts[block.name] = ((_u = toolUseCounts[block.name]) != null ? _u : 0) + 1;
            }
          }
        }
        let durationMs = 0;
        if (turns.length > 0) {
          for (let i = 0; i < turns.length; i++) {
            const turn = turns[i];
            if (!turn.timestamp) continue;
            const turnStart = new Date(turn.timestamp).getTime();
            const turnEnd = new Date((_v = turn.endTimestamp) != null ? _v : turn.timestamp).getTime();
            if (!isNaN(turnStart) && !isNaN(turnEnd) && turnEnd >= turnStart) {
              durationMs += turnEnd - turnStart;
            }
            if (i < turns.length - 1) {
              const nextTs = turns[i + 1].timestamp;
              if (nextTs) {
                const nextStart = new Date(nextTs).getTime();
                if (!isNaN(turnEnd) && !isNaN(nextStart)) {
                  const gap = nextStart - turnEnd;
                  if (gap > 0 && gap <= DURATION_GAP_THRESHOLD_MS) {
                    durationMs += gap;
                  }
                }
              }
            }
          }
        }
        let totalInputTokens = 0;
        let totalOutputTokens = 0;
        let totalCacheReadTokens = 0;
        let totalCacheCreationTokens = 0;
        for (const u of usageByMsg.values()) {
          totalInputTokens += u.inp;
          totalOutputTokens += u.out;
          totalCacheReadTokens += u.cr;
          totalCacheCreationTokens += u.cc;
        }
        const contextWindowTokens = lastCallInput + lastCallCacheRead + lastCallCacheWrite;
        const costUSD = estimateCost(
          model,
          totalInputTokens,
          totalOutputTokens,
          totalCacheReadTokens,
          totalCacheCreationTokens
        );
        const stats = {
          userTurns,
          assistantTurns,
          inputTokens: totalInputTokens,
          outputTokens: totalOutputTokens,
          cacheReadTokens: totalCacheReadTokens,
          cacheCreationTokens: totalCacheCreationTokens,
          totalTokens: totalInputTokens + totalOutputTokens + totalCacheReadTokens + totalCacheCreationTokens,
          contextWindowTokens,
          peakContextTokens,
          compactionCount,
          costUSD,
          toolUseCounts,
          durationMs
        };
        const warnings = [];
        if (unknownRecordTypes.size > 0) {
          for (const [type, { count }] of unknownRecordTypes) {
            Logger.warn(`Skipped unknown record type "${type}" (${count}x)`);
            warnings.push({
              type: "unknown_record_type",
              message: `Unknown record type "${type}" \u2014 plugin may need update`,
              count
            });
          }
        }
        if (unknownBlockTypes.size > 0) {
          for (const [type, { count }] of unknownBlockTypes) {
            Logger.warn(`Skipped unknown block type "${type}" (${count}x)`);
            warnings.push({
              type: "unknown_block_type",
              message: `Unknown block type "${type}" \u2014 plugin may need update`,
              count
            });
          }
        }
        if (this.parseErrorCount > 0) {
          warnings.push({
            type: "parse_errors",
            message: `${this.parseErrorCount} JSONL line(s) failed to parse`,
            count: this.parseErrorCount
          });
        }
        return {
          metadata: {
            id: sessionId || basename(filePath).replace(/\.jsonl$/, ""),
            format: "claude",
            project,
            cwd,
            branch: branch || void 0,
            model: model || void 0,
            version: version || void 0,
            startTime: this.formatTimestamp(startTime),
            totalTurns: turns.length,
            customTitle: customTitle || void 0
          },
          stats,
          turns,
          systemEvents,
          rawPath: filePath,
          warnings: warnings.length > 0 ? warnings : void 0
        };
      }
      /**
       * Build turns by merging consecutive same-role records.
       * Claude streams assistant responses as multiple records (thinking, text, tool_use),
       * each with its own uuid. These should form a single assistant turn.
       * User records containing only tool_result blocks should have those results
       * attached to the preceding assistant turn (not become separate user turns).
       */
      /**
       * Build Turn[] from ordered, deduplicated records.
       *
       * STRUCTURAL ASSUMPTIONS (update if Claude Code changes its JSONL format):
       * 1. Consecutive assistant records merge into one Turn. If a non-assistant
       *    record (e.g. progress) interleaves, the turn is split.
       * 2. User records with only tool_result blocks attach to the preceding
       *    assistant turn. If results arrive before their tool_use, attachment fails.
       * 3. XML tags (<task-notification>, <command-name>, etc.) are parsed via regex.
       *    Schema changes to the XML structure will break extraction silently.
       */
      buildTurns(ordered, unknownRecordTypes, unknownBlockTypes) {
        var _a, _b, _c, _d, _e, _f, _g, _h;
        const turns = [];
        const systemEvents = [];
        const toolUseNames = /* @__PURE__ */ new Map();
        const uuidToToolResults = /* @__PURE__ */ new Map();
        const uuidToParent = /* @__PURE__ */ new Map();
        let currentAssistantTurn = null;
        let pendingCompactMeta = null;
        const flushAssistant = () => {
          if (currentAssistantTurn && currentAssistantTurn.contentBlocks.length > 0) {
            currentAssistantTurn.index = turns.length;
            turns.push(currentAssistantTurn);
          }
          currentAssistantTurn = null;
        };
        for (const record of ordered) {
          if (record.uuid && record.parentUuid) {
            uuidToParent.set(record.uuid, record.parentUuid);
          }
          if (record.type === RT_SUMMARY) {
            flushAssistant();
            const ts = this.formatTimestamp(record.timestamp);
            turns.push({
              index: turns.length,
              role: "assistant",
              timestamp: ts,
              endTimestamp: ts,
              contentBlocks: [{
                type: "compaction",
                summary: record.summary,
                timestamp: record.timestamp
              }]
            });
            continue;
          }
          if (record.type === RT_SYSTEM) {
            if (record.subtype === "compact_boundary" && record.compactMetadata) {
              pendingCompactMeta = record.compactMetadata;
            }
            if (record.subtype === SUBTYPE_LOCAL_COMMAND && record.content) {
              const blocks = this.extractSystemContent(record);
              if (blocks.length > 0) {
                if (this._pendingCommandResult) {
                  this._pendingCommandResult = false;
                  const lastTurn = turns[turns.length - 1];
                  if ((lastTurn == null ? void 0 : lastTurn.role) === "user") {
                    for (const b of blocks) lastTurn.contentBlocks.push(b);
                    if (record.timestamp) lastTurn.endTimestamp = this.formatTimestamp(record.timestamp);
                  }
                } else {
                  flushAssistant();
                  const ts = this.formatTimestamp(record.timestamp);
                  turns.push({
                    index: turns.length,
                    role: "user",
                    timestamp: ts,
                    endTimestamp: ts,
                    contentBlocks: blocks
                  });
                }
              }
            }
            continue;
          }
          if (record.type === RT_ASSISTANT) {
            const blocks = this.parseAssistantBlocks(record, toolUseNames, unknownBlockTypes);
            if (blocks.length === 0) continue;
            if (!currentAssistantTurn) {
              currentAssistantTurn = {
                index: 0,
                role: "assistant",
                timestamp: this.formatTimestamp(record.timestamp),
                endTimestamp: this.formatTimestamp(record.timestamp),
                contentBlocks: []
              };
            }
            if (record.timestamp) {
              currentAssistantTurn.endTimestamp = this.formatTimestamp(record.timestamp);
            }
            if (((_a = record.message) == null ? void 0 : _a.model) && record.message.model !== MODEL_SYNTHETIC && !currentAssistantTurn.model) {
              currentAssistantTurn.model = record.message.model;
            }
            if ((_b = record.message) == null ? void 0 : _b.stop_reason) {
              currentAssistantTurn.stopReason = record.message.stop_reason;
            }
            if (record.isApiErrorMessage && record.error) {
              currentAssistantTurn.isApiError = true;
              currentAssistantTurn.errorType = record.error;
            }
            for (const b of blocks) {
              currentAssistantTurn.contentBlocks.push(b);
            }
          } else if (record.type === RT_USER) {
            if (record.isMeta && this._pendingSlashCommand) {
              const expText = this.extractSkillExpansionText(record);
              if (expText) {
                const lastTurn = turns[turns.length - 1];
                if ((lastTurn == null ? void 0 : lastTurn.role) === "user") {
                  lastTurn.contentBlocks.push({
                    type: "slash_command",
                    commandName: this._pendingSlashCommand,
                    text: expText,
                    timestamp: record.timestamp
                  });
                }
                this._pendingSlashCommand = null;
                continue;
              }
            }
            if (record.isMeta) continue;
            this._pendingSlashCommand = null;
            if (record.isCompactSummary) {
              flushAssistant();
              const ts = this.formatTimestamp(record.timestamp);
              let summary;
              const mc = (_c = record.message) == null ? void 0 : _c.content;
              if (typeof mc === "string" && mc.trim()) {
                summary = mc;
              } else if (Array.isArray(mc)) {
                const texts = [];
                for (const b of mc) {
                  if (b.type === BT_TEXT && ((_d = b.text) == null ? void 0 : _d.trim())) texts.push(b.text);
                }
                if (texts.length > 0) summary = texts.join("\n\n");
              }
              const block = {
                type: "compaction",
                summary,
                timestamp: record.timestamp
              };
              if (pendingCompactMeta == null ? void 0 : pendingCompactMeta.preTokens) {
                block.preTokens = pendingCompactMeta.preTokens;
              }
              pendingCompactMeta = null;
              turns.push({
                index: turns.length,
                role: "assistant",
                timestamp: ts,
                endTimestamp: ts,
                contentBlocks: [block]
              });
              continue;
            }
            if (typeof ((_e = record.message) == null ? void 0 : _e.content) === "string" && record.message.content.startsWith(TAG_TASK_NOTIFICATION)) {
              continue;
            }
            if (isInterruptionMessage(record.message)) {
              if (currentAssistantTurn) {
                currentAssistantTurn.contentBlocks.push({
                  type: "text",
                  text: TEXT_INTERRUPTION,
                  timestamp: record.timestamp
                });
                if (record.timestamp) {
                  currentAssistantTurn.endTimestamp = this.formatTimestamp(record.timestamp);
                }
              }
              continue;
            }
            const toolResults = this.extractToolResultBlocks(record, toolUseNames);
            if (toolResults.length > 0 && record.uuid) {
              const entries = toolResults.map((r) => {
                var _a2;
                return {
                  toolUseId: r.toolUseId,
                  toolName: (_a2 = r.toolName) != null ? _a2 : ""
                };
              });
              uuidToToolResults.set(record.uuid, entries);
            }
            if (toolResults.length > 0 && currentAssistantTurn) {
              for (const result of toolResults) {
                currentAssistantTurn.contentBlocks.push(result);
              }
              if (record.timestamp) {
                currentAssistantTurn.endTimestamp = this.formatTimestamp(record.timestamp);
              }
            } else if (toolResults.length > 0) {
              const lastAssistant = this.findLastAssistantTurn(turns);
              if (lastAssistant) {
                for (const result of toolResults) {
                  lastAssistant.contentBlocks.push(result);
                }
                if (record.timestamp) {
                  lastAssistant.endTimestamp = this.formatTimestamp(record.timestamp);
                }
              }
            }
            if (this._pendingBashCommand) {
              const rc = typeof ((_f = record.message) == null ? void 0 : _f.content) === "string" ? record.message.content : "";
              if (!RE_BASH_STDOUT.test(rc)) {
                flushAssistant();
                const ts = this.formatTimestamp(this._pendingBashCommand.timestamp);
                turns.push({
                  index: turns.length,
                  role: "user",
                  timestamp: ts,
                  endTimestamp: ts,
                  contentBlocks: [{
                    type: "bash_command",
                    command: this._pendingBashCommand.command,
                    stdout: "",
                    stderr: "",
                    timestamp: this._pendingBashCommand.timestamp
                  }]
                });
                this._pendingBashCommand = null;
              }
            }
            const userBlocks = this.extractUserContent(record);
            if (userBlocks.length > 0) {
              if (this._pendingCommandResult) {
                this._pendingCommandResult = false;
                const lastTurn = turns[turns.length - 1];
                if ((lastTurn == null ? void 0 : lastTurn.role) === "user") {
                  for (const b of userBlocks) lastTurn.contentBlocks.push(b);
                  if (record.timestamp) lastTurn.endTimestamp = this.formatTimestamp(record.timestamp);
                }
              } else {
                flushAssistant();
                const ts = this.formatTimestamp(record.timestamp);
                turns.push({
                  index: turns.length,
                  role: "user",
                  timestamp: ts,
                  endTimestamp: ts,
                  contentBlocks: userBlocks
                });
              }
            }
          } else if (record.type === "permission-mode") {
            systemEvents.push({
              type: "permission-mode",
              uuid: record.uuid || "",
              timestamp: this.formatTimestamp(record.timestamp) || "",
              permissionMode: record.permissionMode || "unknown"
            });
          } else if (record.type === "attachment" && record.attachment) {
            const att = record.attachment;
            const str = (v) => typeof v === "string" ? v : "";
            const baseEvent = {
              uuid: record.uuid || "",
              timestamp: this.formatTimestamp(record.timestamp) || "",
              parentUuid: record.parentUuid
            };
            if (att.type === "skill_listing") {
              systemEvents.push({
                ...baseEvent,
                type: "skill_listing",
                content: str(att.content),
                skillCount: typeof att.skillCount === "number" ? att.skillCount : 0,
                isInitial: Boolean(att.isInitial)
              });
            } else if (att.type === "hook_success") {
              systemEvents.push({
                ...baseEvent,
                type: "hook_success",
                hookName: str(att.hookName),
                hookEvent: str(att.hookEvent),
                command: str(att.command),
                durationMs: typeof att.durationMs === "number" ? att.durationMs : 0,
                stdout: str(att.stdout),
                stderr: str(att.stderr),
                exitCode: typeof att.exitCode === "number" ? att.exitCode : 0,
                toolUseId: typeof att.toolUseID === "string" ? att.toolUseID : void 0
              });
            } else if (att.type === "async_hook_response") {
              let resolvedToolUseId;
              let currentUuid = record.parentUuid;
              const maxDepth = 20;
              for (let depth = 0; depth < maxDepth && currentUuid; depth++) {
                const parentEntries = uuidToToolResults.get(currentUuid);
                if (parentEntries && parentEntries.length > 0) {
                  const hookToolName = (_g = str(att.hookName).split(":")[1]) != null ? _g : "";
                  const match = parentEntries.find((e) => e.toolName === hookToolName);
                  resolvedToolUseId = (_h = match == null ? void 0 : match.toolUseId) != null ? _h : parentEntries[0].toolUseId;
                  break;
                }
                currentUuid = uuidToParent.get(currentUuid);
              }
              systemEvents.push({
                ...baseEvent,
                type: "async_hook_response",
                hookName: str(att.hookName),
                hookEvent: str(att.hookEvent),
                processId: str(att.processId),
                stdout: str(att.stdout),
                stderr: str(att.stderr),
                exitCode: typeof att.exitCode === "number" ? att.exitCode : 0,
                toolUseId: resolvedToolUseId
              });
            } else if (att.type === "task_reminder") {
              systemEvents.push({
                ...baseEvent,
                type: "task_reminder",
                content: Array.isArray(att.content) ? att.content : [],
                itemCount: typeof att.itemCount === "number" ? att.itemCount : 0
              });
            }
          } else if (unknownRecordTypes) {
            const existing = unknownRecordTypes.get(record.type);
            if (existing) {
              existing.count++;
            } else {
              const sample = { type: record.type };
              if (record.message) {
                const msg = record.message;
                sample["message.role"] = msg.role;
                if (typeof msg.content === "string") sample["message.content"] = msg.content.slice(0, 200);
                else if (Array.isArray(msg.content)) sample["message.content"] = msg.content.map((b) => b.type);
              }
              sample["keys"] = Object.keys(record).filter((k) => k !== "type");
              unknownRecordTypes.set(record.type, { count: 1, sample });
            }
          }
        }
        flushAssistant();
        return { turns, systemEvents };
      }
      /**
       * Parse sub-agent progress records into a SubAgentSession.
       * Normalizes the nested agent_progress format into standard ClaudeRecords,
       * deduplicates by uuid, and builds turns using the same merging logic.
       */
      buildSubAgentSession(agentRecords, parentBlock) {
        var _a;
        const normalized = [];
        let agentId = "";
        let prompt = "";
        for (const record of agentRecords) {
          const data = record.data;
          if (!(data == null ? void 0 : data.message)) continue;
          if (data.agentId && !agentId) agentId = data.agentId;
          if (data.prompt && !prompt) prompt = data.prompt;
          const innerMsg = data.message;
          const innerType = innerMsg.type;
          if (!innerType) continue;
          normalized.push({
            type: innerType,
            uuid: innerMsg.uuid,
            timestamp: (_a = innerMsg.timestamp) != null ? _a : record.timestamp,
            message: innerMsg.message ? {
              role: innerMsg.message.role,
              content: innerMsg.message.content,
              model: innerMsg.message.model,
              usage: innerMsg.message.usage
            } : void 0
          });
        }
        const uuidToIndex = /* @__PURE__ */ new Map();
        const ordered = [];
        for (const record of normalized) {
          if (record.uuid) {
            const existingIdx = uuidToIndex.get(record.uuid);
            if (existingIdx !== void 0) {
              ordered[existingIdx] = record;
            } else {
              uuidToIndex.set(record.uuid, ordered.length);
              ordered.push(record);
            }
          } else {
            ordered.push(record);
          }
        }
        const savedPending = this.pendingCommand;
        const savedSlash = this._pendingSlashCommand;
        const savedBash = this._pendingBashCommand;
        const savedResult = this._pendingCommandResult;
        this.pendingCommand = null;
        this._pendingSlashCommand = null;
        this._pendingBashCommand = null;
        this._pendingCommandResult = false;
        const { turns } = this.buildTurns(ordered);
        this.pendingCommand = savedPending;
        this._pendingSlashCommand = savedSlash;
        this._pendingBashCommand = savedBash;
        this._pendingCommandResult = savedResult;
        return {
          agentId,
          description: typeof parentBlock.input["description"] === "string" ? parentBlock.input["description"] : "",
          subagentType: typeof parentBlock.input["subagent_type"] === "string" ? parentBlock.input["subagent_type"] : "",
          prompt,
          turns
        };
      }
      parseAssistantBlocks(record, toolUseNames, unknownBlockTypes) {
        const msg = record.message;
        if (!msg) return [];
        const timestamp = record.timestamp;
        const blocks = [];
        if (typeof msg.content === "string") {
          if (msg.content.trim()) {
            blocks.push({ type: "text", text: msg.content, timestamp });
          }
        } else if (Array.isArray(msg.content)) {
          for (const block of msg.content) {
            const parsed = parseContentBlock(block, toolUseNames, timestamp, unknownBlockTypes);
            if (parsed) blocks.push(parsed);
          }
        }
        return blocks;
      }
      extractToolResultBlocks(record, toolUseNames) {
        return extractToolResultBlocks(record.message, toolUseNames, record.timestamp);
      }
      /** Extract content from system records (slash commands like /rename, /compact). */
      extractSystemContent(record) {
        var _a;
        const content = (_a = record.content) != null ? _a : "";
        const timestamp = record.timestamp;
        if (RE_LOCAL_CAVEAT.test(content) || RE_LOCAL_STDERR.test(content)) {
          return [];
        }
        if (RE_LOCAL_STDOUT.test(content)) {
          if (this.pendingCommand) {
            const label = this.pendingCommand;
            this.pendingCommand = null;
            const stdout = content.replace(RE_LOCAL_STDOUT_TAGS, "");
            if (ANSI_COMMANDS.has(label) || hasAnsiCodes2(stdout)) {
              this._pendingCommandResult = true;
              return [{ type: "ansi", label, text: stdout, timestamp }];
            }
            if (stdout.trim()) {
              this._pendingCommandResult = true;
              return [{ type: "text", text: stdout.trim(), timestamp }];
            }
          }
          return [];
        }
        const cmdMatch = content.match(RE_COMMAND_NAME);
        if (!cmdMatch) return [];
        const cmd = cmdMatch[1];
        if (cmd === "/exit") return [];
        this.pendingCommand = cmd;
        const argsMatch = content.match(RE_COMMAND_ARGS);
        const text = argsMatch ? `${cmd} ${argsMatch[1]}` : cmd;
        return [{ type: "text", text, timestamp }];
      }
      /** Extract user content blocks from a record, handling string, text, and image blocks. */
      extractUserContent(record) {
        var _a, _b, _c, _d, _e, _f, _g;
        const content = (_a = record.message) == null ? void 0 : _a.content;
        const timestamp = record.timestamp;
        if (typeof content === "string") {
          if (RE_EXIT_COMMAND.test(content)) {
            this.pendingCommand = null;
            return [{ type: "text", text: TEXT_SESSION_ENDED, timestamp }];
          }
          const cmdMatch = content.match(RE_SLASH_COMMAND);
          if (cmdMatch) {
            const cmd = cmdMatch[1];
            this.pendingCommand = cmd;
            if (content.includes(TAG_COMMAND_MESSAGE_OPEN)) {
              this._pendingSlashCommand = cmd;
            }
            const displayCmd = cmd.includes(":") ? cmd.split(":")[0] : cmd;
            if (ANSI_COMMANDS.has(cmd) || ANSI_COMMANDS.has(displayCmd)) {
              return [{ type: "text", text: displayCmd, timestamp }];
            }
            const argsMatch = content.match(RE_COMMAND_ARGS);
            const parts = [displayCmd];
            if ((_b = argsMatch == null ? void 0 : argsMatch[1]) == null ? void 0 : _b.trim()) parts.push(argsMatch[1].trim());
            return [{ type: "text", text: parts.join(" "), timestamp }];
          }
          if (RE_LOCAL_STDOUT.test(content) && this.pendingCommand) {
            const label = this.pendingCommand;
            this.pendingCommand = null;
            const stdout = content.replace(RE_LOCAL_STDOUT_TAGS, "");
            if (ANSI_COMMANDS.has(label) || hasAnsiCodes2(stdout)) {
              this._pendingCommandResult = true;
              return [{ type: "ansi", label, text: stdout, timestamp }];
            }
            if (stdout.trim()) {
              this._pendingCommandResult = true;
              return [{ type: "text", text: stdout.trim(), timestamp }];
            }
            return [];
          }
          if (RE_LOCAL_STDOUT.test(content)) {
            return [];
          }
          if (RE_LOCAL_CAVEAT.test(content) || RE_LOCAL_STDERR.test(content)) {
            return [];
          }
          const bashInputMatch = content.match(RE_BASH_INPUT);
          if (bashInputMatch) {
            this._pendingBashCommand = { command: bashInputMatch[1], timestamp };
            return [];
          }
          const bashStdoutMatch = content.match(RE_BASH_STDOUT);
          if (bashStdoutMatch != null) {
            const stdout = (_c = bashStdoutMatch[1]) != null ? _c : "";
            const stderrMatch = content.match(RE_BASH_STDERR);
            const stderr = (_d = stderrMatch == null ? void 0 : stderrMatch[1]) != null ? _d : "";
            if (this._pendingBashCommand) {
              const block = {
                type: "bash_command",
                command: this._pendingBashCommand.command,
                stdout,
                stderr,
                timestamp: this._pendingBashCommand.timestamp
              };
              this._pendingBashCommand = null;
              return [block];
            }
            return [];
          }
          if (content.startsWith(TAG_TASK_NOTIFICATION)) {
            return [];
          }
          let cleaned = content;
          cleaned = cleaned.replace(RE_SYSTEM_REMINDER, "");
          cleaned = cleaned.replace(RE_COMMAND_MESSAGE_STRIP, "");
          cleaned = cleaned.replace(RE_COMMAND_ARGS_STRIP, "");
          cleaned = cleaned.replace(RE_IMAGE_REF, "");
          cleaned = cleaned.trim();
          if (!cleaned) {
            return [];
          }
          return [{ type: "text", text: cleaned, timestamp }];
        }
        if (Array.isArray(content)) {
          const blocks = [];
          for (const block of content) {
            if (block.type === BT_TEXT && ((_e = block.text) == null ? void 0 : _e.trim())) {
              blocks.push({ type: "text", text: block.text, timestamp });
            } else if (block.type === BT_IMAGE && ((_f = block.source) == null ? void 0 : _f.data)) {
              blocks.push({
                type: "image",
                mediaType: (_g = block.source.media_type) != null ? _g : "image/png",
                data: block.source.data,
                timestamp
              });
            }
          }
          return blocks;
        }
        return [];
      }
      /** Extract text content from an isMeta user record (skill expansion prompt). */
      extractSkillExpansionText(record) {
        var _a, _b;
        const content = (_a = record.message) == null ? void 0 : _a.content;
        if (typeof content === "string" && RE_LOCAL_CAVEAT.test(content)) return null;
        if (Array.isArray(content)) {
          const texts = [];
          for (const block of content) {
            if (block.type === BT_TEXT && ((_b = block.text) == null ? void 0 : _b.trim())) {
              if (RE_LOCAL_CAVEAT.test(block.text)) continue;
              const cleaned = block.text.replace(RE_SYSTEM_REMINDER, "").trim();
              if (cleaned) texts.push(cleaned);
            }
          }
          return texts.length > 0 ? texts.join("\n\n") : null;
        }
        if (typeof content === "string") {
          const cleaned = content.replace(RE_SYSTEM_REMINDER, "").trim();
          return cleaned || null;
        }
        return null;
      }
      findLastAssistantTurn(turns) {
        for (let i = turns.length - 1; i >= 0; i--) {
          if (turns[i].role === "assistant") return turns[i];
        }
        return null;
      }
    };
    MODEL_PRICING = {
      opus: { input: 15, output: 75, cacheRead: 1.5, cacheWrite: 18.75 },
      sonnet: { input: 3, output: 15, cacheRead: 0.3, cacheWrite: 3.75 },
      haiku: { input: 0.8, output: 4, cacheRead: 0.08, cacheWrite: 1 }
    };
  }
});

// src/parsers/detect.ts
function detectParser(content) {
  const lines = content.split("\n").filter((l) => l.trim().length > 0).slice(0, 20);
  if (lines.length === 0) return null;
  for (const parser of parsers) {
    if (parser.canParse(lines)) return parser;
  }
  return null;
}
var parsers;
var init_detect = __esm({
  "src/parsers/detect.ts"() {
    init_claude_parser();
    parsers = [
      new ClaudeParser()
    ];
  }
});

// src/views/session-browser-modal.ts
var session_browser_modal_exports = {};
__export(session_browser_modal_exports, {
  SessionBrowserModal: () => SessionBrowserModal,
  scanSessionDirs: () => scanSessionDirs
});
async function scanSessionDirs(plugin) {
  if (import_obsidian13.Platform.isMobile) {
    new import_obsidian13.Notice("On mobile, session browsing is limited to vault files.");
    return { entries: [], total: 0, updated: 0 };
  }
  const index = plugin.sessionIndex;
  index.load();
  const entries = [];
  const discoveredPaths = /* @__PURE__ */ new Set();
  let updated = 0;
  for (const dir of plugin.settings.sessionDirs) {
    const expanded = expandHome(dir);
    try {
      const subdirs = listSubdirectories(expanded);
      for (const subdir of subdirs) {
        const files = listDirectory(subdir);
        for (const file of files) {
          discoveredPaths.add(file);
          const entry = await buildEntry(file, extractProjectName(subdir), fs5, index);
          if (entry) {
            if (entry._updated) updated++;
            entries.push(entry.entry);
          }
        }
      }
      const rootFiles = listDirectory(expanded);
      for (const file of rootFiles) {
        discoveredPaths.add(file);
        const entry = await buildEntry(file, extractProjectName(expanded), fs5, index);
        if (entry) {
          if (entry._updated) updated++;
          entries.push(entry.entry);
        }
      }
    } catch (e) {
    }
  }
  index.prune(discoveredPaths);
  index.save();
  entries.sort((a, b) => b.mtime - a.mtime);
  return { entries, total: entries.length, updated };
}
async function buildEntry(filePath, fallbackProject, fs9, index) {
  let mtime;
  try {
    const stat = fs9.statSync(filePath);
    mtime = stat.mtimeMs;
  } catch (e) {
    return null;
  }
  let wasUpdated = false;
  let cached2 = index.get(filePath, mtime);
  if (!cached2) {
    const meta = await extractQuickMetadataAsync(filePath);
    cached2 = {
      sessionId: meta.sessionId,
      cwd: meta.cwd,
      startTime: meta.startTime,
      hasContent: meta.hasContent,
      mtime,
      customTitle: meta.customTitle
    };
    index.set(filePath, cached2);
    wasUpdated = true;
  }
  if (!cached2.hasContent) return null;
  const project = cached2.cwd ? projectFromCwd(cached2.cwd) : fallbackProject;
  const dateSource = cached2.startTime ? new Date(cached2.startTime) : new Date(mtime);
  const date = formatSessionDate(dateSource);
  return {
    entry: {
      id: cached2.sessionId || basename(filePath).replace(/\.\w+$/, ""),
      project,
      format: "claude",
      date,
      path: filePath,
      cwd: cached2.cwd,
      startTime: cached2.startTime,
      mtime,
      customTitle: cached2.customTitle
    },
    _updated: wasUpdated
  };
}
function formatSessionDate(d) {
  const now = /* @__PURE__ */ new Date();
  const time = d.toLocaleTimeString(void 0, { hour: "2-digit", minute: "2-digit", hour12: false });
  if (d.toDateString() === now.toDateString()) {
    return time;
  }
  const weekAgo = new Date(now);
  weekAgo.setDate(weekAgo.getDate() - 7);
  if (d > weekAgo) {
    const day = d.toLocaleDateString(void 0, { weekday: "short" });
    return `${day} ${time}`;
  }
  const month = d.toLocaleDateString(void 0, { month: "short", day: "numeric" });
  if (d.getFullYear() === now.getFullYear()) {
    return `${month}, ${time}`;
  }
  return `${month}, ${d.getFullYear()}`;
}
var import_obsidian13, fs5, SessionBrowserModal;
var init_session_browser_modal = __esm({
  "src/views/session-browser-modal.ts"() {
    import_obsidian13 = require("obsidian");
    fs5 = __toESM(require("fs"));
    init_path_utils();
    init_streaming_reader();
    init_detect();
    init_claude_subagent();
    SessionBrowserModal = class extends import_obsidian13.SuggestModal {
      constructor(app, plugin, entries) {
        super(app);
        this.plugin = plugin;
        this.entries = entries;
        this.setPlaceholder("Search sessions...");
      }
      getSuggestions(query) {
        let results = this.entries;
        if (query) {
          const q = query.toLowerCase();
          results = results.filter((e) => {
            var _a, _b;
            const displayName = e.customTitle || e.project;
            return displayName.toLowerCase().includes(q) || ((_b = (_a = e.cwd) == null ? void 0 : _a.toLowerCase().includes(q)) != null ? _b : false) || e.id.toLowerCase().includes(q);
          });
        }
        const pinned = this.plugin.settings.pinnedSessions;
        const pinnedSet = new Set(pinned);
        const pinnedEntries = results.filter((e) => pinnedSet.has(e.path));
        const unpinnedEntries = results.filter((e) => !pinnedSet.has(e.path));
        return [...pinnedEntries, ...unpinnedEntries];
      }
      renderSuggestion(item, el) {
        el.addClass("claude-sessions-suggestion-item");
        const isPinned = this.plugin.settings.pinnedSessions.includes(item.path);
        if (isPinned) el.addClass("is-pinned");
        const line1 = el.createDiv({ cls: "claude-sessions-suggestion-line1" });
        const displayName = item.customTitle || item.project;
        line1.createSpan({ cls: "claude-sessions-suggestion-project", text: displayName });
        const pinBtn = line1.createSpan({ cls: `claude-sessions-suggestion-pin clickable-icon${isPinned ? " is-active" : ""}` });
        pinBtn.setAttribute("aria-label", isPinned ? "Unpin session" : "Pin session");
        (0, import_obsidian13.setIcon)(pinBtn, "pin");
        pinBtn.addEventListener("click", (e) => {
          e.stopPropagation();
          e.preventDefault();
          this.togglePin(item.path);
        });
        if (item.date) {
          line1.createSpan({ cls: "claude-sessions-suggestion-date", text: item.date });
        }
        const line2 = el.createDiv({ cls: "claude-sessions-suggestion-line2" });
        const pathText = item.cwd ? shortenPath(item.cwd) : "";
        const line2Left = pathText ? `${pathText} \xB7 ${item.id}` : item.id;
        line2.createSpan({ cls: "claude-sessions-suggestion-path", text: line2Left });
        line2.createSpan({ cls: "claude-sessions-suggestion-format", text: item.format });
      }
      togglePin(path5) {
        const pinned = this.plugin.settings.pinnedSessions;
        const idx = pinned.indexOf(path5);
        if (idx >= 0) {
          pinned.splice(idx, 1);
        } else {
          pinned.push(path5);
        }
        void this.plugin.saveSettings();
        const inputEl = this.inputEl;
        if (inputEl) {
          inputEl.dispatchEvent(new Event("input"));
        }
      }
      onChooseSuggestion(item) {
        void (async () => {
          try {
            const content = await readFileContent(item.path);
            const parser = detectParser(content);
            if (!parser) {
              new import_obsidian13.Notice("Could not detect session format.");
              return;
            }
            const session = parser.parse(content, item.path);
            await resolveSubAgentSessions(session, readFileContent, listDirectoryFiles);
            await this.plugin.openSession(session);
          } catch (e) {
            const msg = e instanceof Error ? e.message : String(e);
            new import_obsidian13.Notice(`Failed to load session: ${msg}`);
          }
        })();
      }
    };
  }
});

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => ClaudeSessionsPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian21 = require("obsidian");

// src/settings.ts
var import_obsidian3 = require("obsidian");

// src/utils/folder-suggest.ts
var import_obsidian = require("obsidian");
var FolderSuggest = class extends import_obsidian.AbstractInputSuggest {
  constructor(app, inputEl) {
    super(app, inputEl);
    this.el = inputEl;
    this.folders = this.app.vault.getAllFolders().map((folder) => folder.path).sort();
    if (!this.folders.includes("/")) {
      this.folders.unshift("/");
    }
  }
  getSuggestions(inputStr) {
    if (!inputStr) return this.folders;
    const q = inputStr.toLowerCase();
    return this.folders.filter((f) => f.toLowerCase().includes(q));
  }
  renderSuggestion(folder, el) {
    el.createDiv({ text: folder });
  }
  selectSuggestion(folder) {
    this.el.value = folder;
    this.el.dispatchEvent(new Event("input", { bubbles: true }));
    this.close();
  }
};

// src/settings.ts
var WIDTH_PRESETS = [
  { label: "Narrow (680px)", value: 680 },
  { label: "Medium (800px)", value: 800 },
  { label: "Default (960px)", value: 960 },
  { label: "Wide (1200px)", value: 1200 },
  { label: "Full width", value: 0 }
];
var SettingsTab = class extends import_obsidian3.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    new import_obsidian3.Setting(containerEl).setHeading().setName("Session directories");
    const dirsContainer = containerEl.createDiv({ cls: "claude-sessions-dirs-list" });
    this.renderDirsList(dirsContainer);
    let addDirInput;
    const addDir = async () => {
      const val = addDirInput.getValue().trim();
      if (val) {
        this.plugin.settings.sessionDirs.push(val);
        await this.plugin.saveSettings();
        addDirInput.setValue("");
        this.renderDirsList(dirsContainer);
      }
    };
    new import_obsidian3.Setting(containerEl).setName("Add session directory").setDesc("Path to a directory containing session files (supports ~ for home).").addText((text) => {
      addDirInput = text;
      text.setPlaceholder("~/.claude/projects");
      text.inputEl.addEventListener("keydown", (e) => {
        if (e.key === "Enter") {
          void addDir();
        }
      });
    }).addButton((btn) => btn.setButtonText("Add").setCta().onClick(() => addDir()));
    new import_obsidian3.Setting(containerEl).setHeading().setName("Export");
    new import_obsidian3.Setting(containerEl).setName("Export folder").setDesc("Vault folder for exported session files.").addSearch((search) => {
      search.setPlaceholder("Claude sessions").setValue(this.plugin.settings.exportFolder).onChange(async (value) => {
        this.plugin.settings.exportFolder = value;
        await this.plugin.saveSettings();
      });
      new FolderSuggest(this.app, search.inputEl);
    });
    new import_obsidian3.Setting(containerEl).setHeading().setName("Distill");
    new import_obsidian3.Setting(containerEl).setName("Distill folder").setDesc("Vault folder for distilled session notes with queryable frontmatter.").addSearch((search) => {
      search.setPlaceholder("Claude sessions/distilled").setValue(this.plugin.settings.distillFolder).onChange(async (value) => {
        this.plugin.settings.distillFolder = value;
        await this.plugin.saveSettings();
      });
      new FolderSuggest(this.app, search.inputEl);
    });
    new import_obsidian3.Setting(containerEl).setName("Bases folder").setDesc("Vault folder for dashboard templates.").addSearch((search) => {
      search.setPlaceholder("Claude sessions/bases").setValue(this.plugin.settings.basesFolder).onChange(async (value) => {
        this.plugin.settings.basesFolder = value;
        await this.plugin.saveSettings();
      });
      new FolderSuggest(this.app, search.inputEl);
    });
    new import_obsidian3.Setting(containerEl).setHeading().setName("Display");
    new import_obsidian3.Setting(containerEl).setName("Show thinking blocks").setDesc("Display assistant thinking/reasoning blocks in session view.").addToggle((toggle) => toggle.setValue(this.plugin.settings.showThinkingBlocks).onChange(async (value) => {
      this.plugin.settings.showThinkingBlocks = value;
      await this.plugin.saveSettings();
      this.plugin.updateTimelineViews();
    }));
    new import_obsidian3.Setting(containerEl).setName("Show tool calls").setDesc("Display tool use blocks (read, bash, etc.) in session view.").addToggle((toggle) => toggle.setValue(this.plugin.settings.showToolCalls).onChange(async (value) => {
      this.plugin.settings.showToolCalls = value;
      await this.plugin.saveSettings();
      this.plugin.updateTimelineViews();
    }));
    new import_obsidian3.Setting(containerEl).setName("Show tool results").setDesc("Display tool result output in session view.").addToggle((toggle) => toggle.setValue(this.plugin.settings.showToolResults).onChange(async (value) => {
      this.plugin.settings.showToolResults = value;
      await this.plugin.saveSettings();
      this.plugin.updateTimelineViews();
    }));
    new import_obsidian3.Setting(containerEl).setName("Content width").setDesc("Maximum width of session content. Narrower widths improve readability.").addDropdown((dropdown) => {
      for (const preset of WIDTH_PRESETS) {
        dropdown.addOption(String(preset.value), preset.label);
      }
      dropdown.setValue(String(this.plugin.settings.maxContentWidth));
      dropdown.onChange(async (value) => {
        this.plugin.settings.maxContentWidth = parseInt(value, 10);
        await this.plugin.saveSettings();
        this.plugin.updateTimelineWidth();
      });
    });
    new import_obsidian3.Setting(containerEl).setName("Tool group threshold").setDesc("Consecutive tool calls above this number are collapsed into a group.").addText((text) => text.setValue(String(this.plugin.settings.toolGroupThreshold)).onChange(async (value) => {
      const n = parseInt(value, 10);
      if (!isNaN(n) && n >= 1) {
        this.plugin.settings.toolGroupThreshold = n;
        await this.plugin.saveSettings();
        this.plugin.updateTimelineViews();
      }
    }));
    new import_obsidian3.Setting(containerEl).setHeading().setName("Live watch");
    new import_obsidian3.Setting(containerEl).setName("Auto-scroll on update").setDesc("Scroll to bottom when new content arrives during live watch.").addToggle((toggle) => toggle.setValue(this.plugin.settings.autoScrollOnUpdate).onChange(async (value) => {
      this.plugin.settings.autoScrollOnUpdate = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian3.Setting(containerEl).setName("Notify on pending tool").setDesc("Show a system notification when a live-watched session has a tool call waiting for permission.").addToggle((toggle) => toggle.setValue(this.plugin.settings.notifyOnPendingTool).onChange(async (value) => {
      this.plugin.settings.notifyOnPendingTool = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian3.Setting(containerEl).setHeading().setName("Beta");
    new import_obsidian3.Setting(containerEl).setName("Show rate limits").setDesc(
      "Display Claude account rate limit utilization (5-hour and weekly) in the summary hero cards. Requires network access \u2014 reads your Claude OAuth credentials and queries the Anthropic API. Data is cached for 5 minutes."
    ).addToggle((toggle) => toggle.setValue(this.plugin.settings.showRateLimits).onChange(async (value) => {
      this.plugin.settings.showRateLimits = value;
      await this.plugin.saveSettings();
      if (!value) {
        const { clearRateLimitCache: clearRateLimitCache2 } = await Promise.resolve().then(() => (init_rate_limits(), rate_limits_exports));
        clearRateLimitCache2();
      }
      this.plugin.updateTimelineViews();
    }));
    new import_obsidian3.Setting(containerEl).setHeading().setName("Debug");
    new import_obsidian3.Setting(containerEl).setName("Debug level").setDesc("Control console logging verbosity for debugging.").addDropdown((dropdown) => dropdown.addOption("none", "None").addOption("error", "Errors only").addOption("warn", "Warnings and errors").addOption("info", "Info, warnings, and errors").addOption("debug", "Debug (all logs)").setValue(this.plugin.settings.debugLevel).onChange(async (value) => {
      this.plugin.settings.debugLevel = value;
      await this.plugin.saveSettings();
    }));
  }
  renderDirsList(container) {
    container.empty();
    for (let i = 0; i < this.plugin.settings.sessionDirs.length; i++) {
      const dir = this.plugin.settings.sessionDirs[i];
      const row = container.createDiv({ cls: "claude-sessions-dir-row" });
      row.createSpan({ text: dir, cls: "claude-sessions-dir-path" });
      const removeBtn = row.createEl("button", {
        cls: "claude-sessions-dir-remove clickable-icon",
        attr: {
          "aria-label": `Remove directory ${dir}`,
          "data-tooltip-position": "top"
        }
      });
      (0, import_obsidian3.setIcon)(removeBtn, "trash-2");
      removeBtn.addEventListener("click", () => {
        this.plugin.settings.sessionDirs.splice(i, 1);
        void this.plugin.saveSettings();
        this.renderDirsList(container);
      });
    }
  }
};

// src/types.ts
var DEFAULT_SETTINGS = {
  sessionDirs: ["~/.claude/projects"],
  exportFolder: "Claude sessions",
  showThinkingBlocks: true,
  showToolCalls: true,
  showToolResults: true,
  autoScrollOnUpdate: true,
  notifyOnPendingTool: false,
  toolGroupThreshold: 4,
  maxContentWidth: 960,
  debugLevel: "warn",
  pinnedSessions: [],
  showRateLimits: false,
  // Distill settings
  distillFolder: "Claude sessions/distilled",
  basesFolder: "Claude sessions/bases"
};

// src/views/timeline-view.ts
var import_obsidian11 = require("obsidian");
var fs3 = __toESM(require("fs"));

// src/views/render-helpers.ts
var import_obsidian4 = require("obsidian");
var COLLAPSE_THRESHOLD = 10;
var EXT_TO_LANG = {
  ts: "typescript",
  tsx: "tsx",
  js: "javascript",
  mjs: "javascript",
  cjs: "javascript",
  jsx: "jsx",
  py: "python",
  rb: "ruby",
  rs: "rust",
  go: "go",
  java: "java",
  kt: "kotlin",
  cs: "csharp",
  cpp: "cpp",
  c: "c",
  h: "c",
  swift: "swift",
  m: "objectivec",
  sh: "bash",
  zsh: "bash",
  bash: "bash",
  fish: "fish",
  json: "json",
  jsonl: "json",
  yaml: "yaml",
  yml: "yaml",
  toml: "toml",
  xml: "xml",
  html: "html",
  css: "css",
  scss: "scss",
  less: "less",
  sql: "sql",
  graphql: "graphql",
  gql: "graphql",
  md: "markdown",
  mdx: "mdx",
  tex: "latex",
  dockerfile: "dockerfile",
  makefile: "makefile",
  lua: "lua",
  r: "r",
  pl: "perl",
  php: "php",
  ex: "elixir",
  erl: "erlang",
  hs: "haskell",
  ml: "ocaml",
  scala: "scala",
  clj: "clojure",
  vue: "vue",
  svelte: "svelte",
  astro: "astro",
  tf: "hcl",
  hcl: "hcl",
  nix: "nix",
  zig: "zig",
  v: "v"
};
function stripLineNumbers(text) {
  return text.replace(/^\d+[\u2192\t]/gm, "");
}
function langFromPath(filePath) {
  var _a, _b, _c, _d;
  const basename3 = (_a = filePath.split("/").pop()) != null ? _a : "";
  const lowerBase = basename3.toLowerCase();
  if (lowerBase === "makefile") return "makefile";
  if (lowerBase === "dockerfile") return "dockerfile";
  const ext = (_c = (_b = basename3.split(".").pop()) == null ? void 0 : _b.toLowerCase()) != null ? _c : "";
  return (_d = EXT_TO_LANG[ext]) != null ? _d : "";
}
function shortModelName(model) {
  let m = model.match(/claude-(opus|sonnet|haiku)-(\d+)-(\d+)/);
  if (m) return `${m[1]} ${m[2]}.${m[3]}`;
  m = model.match(/claude-(\d+)-(\d+)-(opus|sonnet|haiku)/);
  if (m) return `${m[3]} ${m[1]}.${m[2]}`;
  return model;
}
function fence(content, lang = "") {
  let max = 2;
  const re = /`{3,}/g;
  let m;
  while ((m = re.exec(content)) !== null) {
    if (m[0].length > max) max = m[0].length;
  }
  const ticks = "`".repeat(max + 1);
  return ticks + lang + "\n" + content + "\n" + ticks;
}
function normalizeMarkdown(text) {
  return text.replace(/^([^|\n][^\n]*)\n(\|[^\n]+\|\s*\n\|[-:| ]+\|)/gm, "$1\n\n$2");
}
function makeClickable(el, opts) {
  var _a;
  el.setAttribute("tabindex", "0");
  el.setAttribute("role", (_a = opts.role) != null ? _a : "button");
  if (opts.label) el.setAttribute("aria-label", opts.label);
  if (opts.expanded !== void 0) el.setAttribute("aria-expanded", String(opts.expanded));
  el.addEventListener("keydown", (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      el.click();
    }
  });
}
function formatElapsed(ms) {
  if (ms <= 0) return "0:00";
  const totalSec = Math.round(ms / 1e3);
  const h = Math.floor(totalSec / 3600);
  const m = Math.floor(totalSec % 3600 / 60);
  const s = totalSec % 60;
  if (h > 0) return `${h}:${String(m).padStart(2, "0")}:${String(s).padStart(2, "0")}`;
  return `${m}:${String(s).padStart(2, "0")}`;
}
function addCopyButton(container, text, label, cls = "claude-sessions-summary-copy") {
  const btn = container.createEl("button", {
    cls: `${cls} clickable-icon`,
    attr: { "aria-label": label, "data-tooltip-position": "top", "data-copy-text": text }
  });
  (0, import_obsidian4.setIcon)(btn, "copy");
  btn.addEventListener("click", (e) => {
    e.stopPropagation();
    void navigator.clipboard.writeText(text);
    (0, import_obsidian4.setIcon)(btn, "check");
    window.setTimeout(() => (0, import_obsidian4.setIcon)(btn, "copy"), 1500);
  });
}

// src/views/timeline-renderer.ts
var import_obsidian9 = require("obsidian");
init_constants();

// src/views/summary-renderer.ts
var import_obsidian5 = require("obsidian");
init_rate_limits();
function renderSummary(session, container, ctx) {
  const { metadata, stats } = session;
  const pinnedHeroes = container.createDiv({ cls: "claude-sessions-pinned-heroes" });
  buildHeroCards(pinnedHeroes, stats, metadata, null);
  const unpinBtn = pinnedHeroes.createEl("button", {
    cls: "claude-sessions-heroes-pin clickable-icon is-active",
    attr: {
      "aria-label": "Click to unpin stats",
      "data-tooltip-position": "bottom"
    }
  });
  (0, import_obsidian5.setIcon)(unpinBtn, "pin");
  const el = container.createDiv({ cls: "claude-sessions-summary" });
  const header = el.createDiv({ cls: "claude-sessions-summary-header" });
  header.createSpan({ cls: "claude-sessions-summary-chevron", text: "\u25B6" });
  const icon = header.createSpan({ cls: "claude-sessions-summary-icon" });
  (0, import_obsidian5.setIcon)(icon, "bar-chart-2");
  header.createSpan({ cls: "claude-sessions-summary-title", text: "Session summary" });
  if (stats.contextWindowTokens > 0) {
    header.createSpan({
      cls: "claude-sessions-summary-inline",
      text: `${formatTokens(stats.contextWindowTokens)} context`
    });
  }
  if (stats.costUSD > 0) {
    header.createSpan({
      cls: "claude-sessions-summary-inline",
      text: formatCost(stats.costUSD)
    });
  }
  if (metadata.totalTurns > 0) {
    header.createSpan({
      cls: "claude-sessions-summary-inline",
      text: `${metadata.totalTurns} turns`
    });
  }
  const body = el.createDiv({ cls: "claude-sessions-summary-body" });
  makeClickable(header, { label: "Toggle session summary", expanded: false });
  header.addEventListener("click", () => {
    const willOpen = !el.hasClass("open");
    el.toggleClass("open", willOpen);
    header.setAttribute("aria-expanded", String(willOpen));
  });
  const heroes = body.createDiv({ cls: "claude-sessions-dash-heroes" });
  buildHeroCards(heroes, stats, metadata, null);
  const pinBtn = heroes.createEl("button", {
    cls: "claude-sessions-heroes-pin clickable-icon",
    attr: {
      "aria-label": "Click to pin stats to top",
      "data-tooltip-position": "top"
    }
  });
  (0, import_obsidian5.setIcon)(pinBtn, "pin");
  const togglePin = (willPin) => {
    pinnedHeroes.toggleClass("is-pinned", willPin);
    pinBtn.toggleClass("is-active", willPin);
    pinBtn.setAttribute("aria-label", willPin ? "Click to unpin stats" : "Click to pin stats to top");
  };
  pinBtn.addEventListener("click", (e) => {
    e.stopPropagation();
    togglePin(!pinnedHeroes.hasClass("is-pinned"));
  });
  unpinBtn.addEventListener("click", (e) => {
    e.stopPropagation();
    togglePin(false);
  });
  const charts = body.createDiv({ cls: "claude-sessions-dash-charts" });
  const totalInput = stats.inputTokens + stats.cacheReadTokens + stats.cacheCreationTokens;
  if (totalInput > 0 || stats.outputTokens > 0) {
    const tokenCard = charts.createDiv({ cls: "claude-sessions-dash-card" });
    tokenCard.createDiv({ cls: "claude-sessions-dash-card-title", text: "Token usage" });
    if (totalInput > 0) {
      const inputRow = tokenCard.createDiv({ cls: "claude-sessions-dash-bar-row" });
      inputRow.createSpan({ cls: "claude-sessions-dash-bar-label", text: "Input" });
      inputRow.createSpan({ cls: "claude-sessions-dash-bar-value", text: formatTokens(totalInput) });
      const barTrack = tokenCard.createDiv({ cls: "claude-sessions-dash-stacked-track" });
      const segments = [];
      if (stats.cacheReadTokens > 0) {
        segments.push({ value: stats.cacheReadTokens, cls: "cache-read", label: `Cache read ${formatTokens(stats.cacheReadTokens)}` });
      }
      if (stats.cacheCreationTokens > 0) {
        segments.push({ value: stats.cacheCreationTokens, cls: "cache-write", label: `Cache write ${formatTokens(stats.cacheCreationTokens)}` });
      }
      if (stats.inputTokens > 0) {
        segments.push({ value: stats.inputTokens, cls: "uncached", label: `Uncached ${formatTokens(stats.inputTokens)}` });
      }
      for (const seg of segments) {
        const pct = seg.value / totalInput * 100;
        const segEl = barTrack.createDiv({ cls: `claude-sessions-dash-stacked-seg ${seg.cls}` });
        segEl.style.width = `${Math.max(pct, 1)}%`;
        segEl.setAttribute("aria-label", seg.label);
        segEl.setAttribute("data-tooltip-position", "top");
      }
      const legend = tokenCard.createDiv({ cls: "claude-sessions-dash-legend" });
      if (stats.cacheReadTokens > 0) addLegendItem(legend, "cache-read", "Cache read", formatTokens(stats.cacheReadTokens));
      if (stats.cacheCreationTokens > 0) addLegendItem(legend, "cache-write", "Cache write", formatTokens(stats.cacheCreationTokens));
      if (stats.inputTokens > 0) addLegendItem(legend, "uncached", "Uncached", formatTokens(stats.inputTokens));
    }
    if (stats.outputTokens > 0) {
      const outputRow = tokenCard.createDiv({ cls: "claude-sessions-dash-bar-row claude-sessions-dash-output-row" });
      outputRow.createSpan({ cls: "claude-sessions-dash-bar-label", text: "Output" });
      outputRow.createSpan({ cls: "claude-sessions-dash-bar-value", text: formatTokens(stats.outputTokens) });
      if (totalInput > 0) {
        const outTrack = tokenCard.createDiv({ cls: "claude-sessions-dash-stacked-track" });
        const outPct = Math.min(stats.outputTokens / totalInput * 100, 100);
        const outSeg = outTrack.createDiv({ cls: "claude-sessions-dash-stacked-seg output" });
        outSeg.style.width = `${Math.max(outPct, 1)}%`;
      }
    }
  }
  renderToolChart(stats, charts);
  if (session.warnings && session.warnings.length > 0) {
    const warningCard = body.createDiv({ cls: "claude-sessions-dash-card claude-sessions-dash-warnings" });
    const warningHeader = warningCard.createDiv({ cls: "claude-sessions-dash-warnings-header" });
    const warningIcon = warningHeader.createSpan({ cls: "claude-sessions-dash-warnings-icon" });
    (0, import_obsidian5.setIcon)(warningIcon, "alert-triangle");
    warningHeader.createSpan({ text: "Parse warnings" });
    const warningList = warningCard.createDiv({ cls: "claude-sessions-dash-warnings-list" });
    for (const warning of session.warnings) {
      const item = warningList.createDiv({ cls: "claude-sessions-dash-warnings-item" });
      item.createSpan({ text: `${warning.message} (${warning.count}x)` });
    }
    if (session.warnings.some((w) => w.type === "unknown_record_type" || w.type === "unknown_block_type")) {
      warningCard.createDiv({
        cls: "claude-sessions-dash-warnings-hint",
        text: "Some data may be missing. Check for plugin updates."
      });
    }
  }
  const metaCard = body.createDiv({ cls: "claude-sessions-dash-card claude-sessions-dash-meta" });
  metaCard.createDiv({ cls: "claude-sessions-dash-card-title", text: "Session details" });
  const metaGrid = metaCard.createDiv({ cls: "claude-sessions-dash-meta-grid" });
  if (metadata.customTitle) addMetaItem(metaGrid, "Title", metadata.customTitle);
  if (metadata.project) addMetaItem(metaGrid, "Project", metadata.project);
  if (metadata.model) addMetaItem(metaGrid, "Model", metadata.model);
  if (metadata.version) addMetaItem(metaGrid, "Version", metadata.version);
  if (metadata.branch) addMetaItem(metaGrid, "Branch", metadata.branch);
  if (metadata.startTime) {
    const d = new Date(metadata.startTime);
    addMetaItem(metaGrid, "Started", d.toLocaleString());
  }
  if (stats.durationMs > 0) addMetaItem(metaGrid, "Duration", formatDuration(stats.durationMs));
  if (metadata.cwd) addMetaItem(metaGrid, "Working dir", metadata.cwd, true);
  if (stats.userTurns > 0 || stats.assistantTurns > 0) {
    const turnRow = metaCard.createDiv({ cls: "claude-sessions-dash-turn-breakdown" });
    turnRow.createSpan({ cls: "claude-sessions-dash-turn-item", text: `${stats.userTurns} user` });
    turnRow.createSpan({ cls: "claude-sessions-dash-turn-sep", text: "/" });
    turnRow.createSpan({ cls: "claude-sessions-dash-turn-item", text: `${stats.assistantTurns} assistant` });
    turnRow.createSpan({ cls: "claude-sessions-dash-turn-sep", text: "=" });
    turnRow.createSpan({ cls: "claude-sessions-dash-turn-item claude-sessions-dash-turn-total", text: `${metadata.totalTurns} turns` });
  }
  const idSection = body.createDiv({ cls: "claude-sessions-dash-ids" });
  const idRow = idSection.createDiv({ cls: "claude-sessions-dash-id-row" });
  idRow.createSpan({ cls: "claude-sessions-dash-id-label", text: "ID" });
  idRow.createSpan({ cls: "claude-sessions-dash-id-value", text: metadata.id });
  addCopyButton(idRow, metadata.id, "Copy session ID");
  addCopyButton(idRow, session.rawPath, "Copy file path");
  const resumeCmd = `claude --resume ${metadata.id}`;
  const resumeRow = idSection.createDiv({ cls: "claude-sessions-dash-id-row" });
  resumeRow.createSpan({ cls: "claude-sessions-dash-id-label", text: "Resume" });
  resumeRow.createSpan({ cls: "claude-sessions-dash-id-value", text: resumeCmd });
  addCopyButton(resumeRow, resumeCmd, "Copy resume command");
  const obsidianUri = `obsidian://claude-sessions?session=${encodeURIComponent(session.rawPath)}`;
  const displayName = metadata.customTitle || metadata.project;
  const mdLink = `[${displayName} session](${obsidianUri})`;
  const uriRow = idSection.createDiv({ cls: "claude-sessions-dash-id-row" });
  uriRow.createSpan({ cls: "claude-sessions-dash-id-label", text: "URI" });
  const uriPreview = obsidianUri.length > 50 ? obsidianUri.substring(0, 50) + "\u2026" : obsidianUri;
  uriRow.createSpan({ cls: "claude-sessions-dash-id-value", text: uriPreview });
  addCopyButton(uriRow, obsidianUri, "Copy URI");
  addCopyButton(uriRow, mdLink, "Copy markdown link");
  if (ctx.settings.showRateLimits) {
    void fetchRateLimits().then((rl) => {
      if (!rl) return;
      appendRateLimitCards(pinnedHeroes, rl);
      appendRateLimitCards(heroes, rl);
    });
  }
}
function buildHeroCards(container, stats, metadata, _rl) {
  if (stats.costUSD > 0) addHeroCard(container, formatCost(stats.costUSD), "Cost", "receipt");
  if (stats.contextWindowTokens > 0) {
    const subtitle = stats.compactionCount > 0 && stats.peakContextTokens > 0 ? `Peak: ${formatTokens(stats.peakContextTokens)}` : void 0;
    addHeroCard(container, formatTokens(stats.contextWindowTokens), "Context", "layers", subtitle);
  }
  if (metadata.totalTurns > 0) addHeroCard(container, String(metadata.totalTurns), "Turns", "message-circle");
  if (stats.durationMs > 0) addHeroCard(container, formatDuration(stats.durationMs), "Duration", "clock");
}
function appendRateLimitCards(container, rl) {
  if (rl.fiveHourPercent != null) {
    addRateLimitCard(container, rl.fiveHourPercent, "5h limit", "gauge", rl.fiveHourResetsAt);
  }
  if (rl.weeklyPercent != null) {
    addRateLimitCard(container, rl.weeklyPercent, "7d limit", "calendar-clock", rl.weeklyResetsAt);
  }
}
function addRateLimitCard(container, percent, label, iconName, resetsAt) {
  const card = container.createDiv({ cls: "claude-sessions-dash-hero claude-sessions-dash-hero-rate" });
  const iconEl = card.createDiv({ cls: "claude-sessions-dash-hero-icon" });
  (0, import_obsidian5.setIcon)(iconEl, iconName);
  card.createDiv({ cls: "claude-sessions-dash-hero-value", text: `${Math.round(percent)}%` });
  card.createDiv({ cls: "claude-sessions-dash-hero-label", text: label });
  const track = card.createDiv({ cls: "claude-sessions-dash-hero-bar-track" });
  const fill = track.createDiv({ cls: "claude-sessions-dash-hero-bar-fill" });
  fill.style.width = `${Math.min(percent, 100)}%`;
  if (percent >= 90) fill.addClass("critical");
  else if (percent >= 70) fill.addClass("warning");
  if (resetsAt) {
    const resetDate = new Date(resetsAt);
    const exactLabel = resetDate.toLocaleDateString(void 0, {
      weekday: "short",
      month: "short",
      day: "numeric",
      hour: "numeric",
      minute: "2-digit"
    });
    card.createDiv({
      cls: "claude-sessions-dash-hero-reset",
      text: `Resets ${formatRelativeTime(resetDate)}`,
      attr: { "aria-label": exactLabel, "data-tooltip-position": "bottom" }
    });
  }
}
function addHeroCard(container, value, label, iconName, subtitle) {
  const card = container.createDiv({ cls: "claude-sessions-dash-hero" });
  const iconEl = card.createDiv({ cls: "claude-sessions-dash-hero-icon" });
  (0, import_obsidian5.setIcon)(iconEl, iconName);
  card.createDiv({ cls: "claude-sessions-dash-hero-value", text: value });
  card.createDiv({ cls: "claude-sessions-dash-hero-label", text: label });
  if (subtitle) {
    card.createDiv({ cls: "claude-sessions-dash-hero-subtitle", text: subtitle });
  }
}
function addLegendItem(container, cls, label, value) {
  const item = container.createDiv({ cls: "claude-sessions-dash-legend-item" });
  item.createSpan({ cls: `claude-sessions-dash-legend-dot ${cls}` });
  item.createSpan({ text: label });
  item.createSpan({ cls: "claude-sessions-dash-legend-value", text: value });
}
function addMetaItem(grid, label, value, fullWidth) {
  const item = grid.createDiv({ cls: "claude-sessions-dash-meta-item" + (fullWidth ? " full-width" : "") });
  item.createSpan({ cls: "claude-sessions-dash-meta-label", text: label });
  item.createSpan({ cls: "claude-sessions-dash-meta-value", text: value });
}
function renderToolChart(stats, container) {
  const toolNames = Object.keys(stats.toolUseCounts);
  if (toolNames.length === 0) return;
  const toolCard = container.createDiv({ cls: "claude-sessions-dash-card" });
  toolCard.createDiv({ cls: "claude-sessions-dash-card-title", text: "Tool usage" });
  const sorted = toolNames.sort((a, b) => stats.toolUseCounts[b] - stats.toolUseCounts[a]);
  const maxCount = stats.toolUseCounts[sorted[0]];
  const totalCalls = sorted.reduce((sum, n) => sum + stats.toolUseCounts[n], 0);
  toolCard.createDiv({ cls: "claude-sessions-dash-tool-total", text: `${totalCalls} total calls` });
  const bars = toolCard.createDiv({ cls: "claude-sessions-dash-tool-bars" });
  for (const name of sorted) {
    const count = stats.toolUseCounts[name];
    const pct = count / maxCount * 100;
    const row = bars.createDiv({ cls: "claude-sessions-dash-tool-row" });
    row.createSpan({ cls: "claude-sessions-dash-tool-name", text: name });
    const barWrap = row.createDiv({ cls: "claude-sessions-dash-tool-bar-wrap" });
    const bar = barWrap.createDiv({ cls: "claude-sessions-dash-tool-bar" });
    bar.style.width = `${Math.max(pct, 2)}%`;
    row.createSpan({ cls: "claude-sessions-dash-tool-count", text: String(count) });
  }
}
function formatTokens(n) {
  if (n >= 1e6) return (n / 1e6).toFixed(1) + "M";
  if (n >= 1e3) return (n / 1e3).toFixed(1) + "k";
  return String(n);
}
function formatCost(usd) {
  if (usd >= 1) return `$${usd.toFixed(2)}`;
  return `$${usd.toFixed(3)}`;
}
function formatDuration(ms) {
  const totalSec = Math.round(ms / 1e3);
  const h = Math.floor(totalSec / 3600);
  const m = Math.floor(totalSec % 3600 / 60);
  const s = totalSec % 60;
  if (h > 0) return `${h}h ${m}m`;
  if (m > 0) return `${m}m ${s}s`;
  return `${s}s`;
}
function formatRelativeTime(date) {
  const now = Date.now();
  const diffMs = date.getTime() - now;
  const absDiffMs = Math.abs(diffMs);
  const inFuture = diffMs > 0;
  const minutes = Math.round(absDiffMs / 6e4);
  let relative;
  if (minutes < 1) relative = "now";
  else if (minutes < 60) relative = `${minutes}m`;
  else if (absDiffMs < 864e5) {
    const h = Math.floor(absDiffMs / 36e5);
    const m = Math.floor(absDiffMs % 36e5 / 6e4);
    relative = m > 0 ? `${h}h ${m}m` : `${h}h`;
  } else {
    const d = Math.floor(absDiffMs / 864e5);
    const h = Math.floor(absDiffMs % 864e5 / 36e5);
    relative = h > 0 ? `${d}d ${h}h` : `${d}d`;
  }
  if (relative === "now") return relative;
  return inFuture ? `in ${relative}` : `${relative} ago`;
}

// src/views/system-events-renderer.ts
var import_obsidian7 = require("obsidian");
init_path_utils();
function renderSystemEvents(session, container) {
  const events = session.systemEvents;
  if (!events || events.length === 0) return;
  const hooks = events.filter((e) => e.type === "hook_success" && !e.toolUseId || e.type === "async_hook_response" && !e.toolUseId);
  const skills = events.filter((e) => e.type === "skill_listing");
  const tasks = events.filter((e) => e.type === "task_reminder" && e.itemCount > 0);
  if (hooks.length === 0 && skills.length === 0 && tasks.length === 0) return;
  const el = container.createDiv({ cls: "claude-sessions-system-events" });
  const header = el.createDiv({ cls: "claude-sessions-system-events-header" });
  header.createSpan({ cls: "claude-sessions-system-events-chevron", text: "\u25B6" });
  const icon = header.createSpan({ cls: "claude-sessions-system-events-icon" });
  (0, import_obsidian7.setIcon)(icon, "settings-2");
  header.createSpan({ cls: "claude-sessions-system-events-title", text: "System events" });
  const counts = [];
  if (hooks.length > 0) counts.push(`${hooks.length} hooks`);
  if (skills.length > 0) counts.push(`${skills.length} skills`);
  if (tasks.length > 0) counts.push(`${tasks.length} tasks`);
  header.createSpan({ cls: "claude-sessions-system-events-count", text: counts.join(", ") });
  const body = el.createDiv({ cls: "claude-sessions-system-events-body" });
  makeClickable(header, { label: "Toggle system events", expanded: false });
  header.addEventListener("click", () => {
    const willOpen = !el.hasClass("open");
    el.toggleClass("open", willOpen);
    header.setAttribute("aria-expanded", String(willOpen));
  });
  if (hooks.length > 0) {
    renderHooksSection(body, hooks);
  }
  if (skills.length > 0) {
    renderSkillsSection(body, skills);
  }
  if (tasks.length > 0) {
    renderTasksSection(body, tasks);
  }
}
function renderHooksSection(container, hooks) {
  const section = container.createDiv({ cls: "claude-sessions-system-events-section" });
  const sectionHeader = section.createDiv({ cls: "claude-sessions-system-events-section-header" });
  const headerIcon = sectionHeader.createSpan({ cls: "claude-sessions-system-events-section-icon" });
  (0, import_obsidian7.setIcon)(headerIcon, "zap");
  sectionHeader.createSpan({ text: `Hooks (${hooks.length})` });
  const list = section.createDiv({ cls: "claude-sessions-system-events-list" });
  for (const hook of hooks) {
    const row = list.createDiv({ cls: "claude-sessions-system-events-row" });
    const nameParts = hook.hookName.split(":");
    const eventType = nameParts[0] || hook.hookEvent;
    const toolName = nameParts[1] || "";
    const badge = row.createSpan({ cls: "claude-sessions-system-events-badge" });
    badge.createSpan({ cls: "claude-sessions-system-events-badge-event", text: eventType });
    if (toolName) {
      badge.createSpan({ cls: "claude-sessions-system-events-badge-tool", text: toolName });
    }
    if (hook.type === "hook_success" && hook.durationMs > 0) {
      row.createSpan({
        cls: "claude-sessions-system-events-duration",
        text: `${hook.durationMs}ms`
      });
    }
    if (hook.type === "hook_success" && hook.command) {
      row.createSpan({
        cls: "claude-sessions-system-events-command",
        text: basename(hook.command),
        attr: { title: hook.command }
      });
    }
    if (hook.exitCode !== 0) {
      row.createSpan({
        cls: "claude-sessions-system-events-error",
        text: `exit ${hook.exitCode}`
      });
    }
    if (hook.stdout && hook.stdout.trim()) {
      const stdout = hook.stdout.trim();
      if (stdout.length > 100) {
        const preview = row.createDiv({ cls: "claude-sessions-system-events-stdout collapsed" });
        preview.createSpan({ text: stdout.slice(0, 100) + "..." });
        const expandBtn = preview.createSpan({ cls: "claude-sessions-system-events-expand", text: "show more" });
        makeClickable(expandBtn, { label: "Expand output" });
        expandBtn.addEventListener("click", (e) => {
          e.stopPropagation();
          preview.empty();
          preview.removeClass("collapsed");
          preview.createEl("pre", { text: stdout });
        });
      } else {
        row.createDiv({ cls: "claude-sessions-system-events-stdout", text: stdout });
      }
    }
  }
}
function renderSkillsSection(container, skills) {
  const section = container.createDiv({ cls: "claude-sessions-system-events-section" });
  const sectionHeader = section.createDiv({ cls: "claude-sessions-system-events-section-header" });
  const headerIcon = sectionHeader.createSpan({ cls: "claude-sessions-system-events-section-icon" });
  (0, import_obsidian7.setIcon)(headerIcon, "list");
  sectionHeader.createSpan({ text: "Available skills" });
  for (const skill of skills) {
    const content = section.createDiv({ cls: "claude-sessions-system-events-skills" });
    const lines = skill.content.split("\n").filter((l) => l.trim().startsWith("- "));
    for (const line of lines) {
      const match = line.match(/^-\s+(\S+):\s*(.*)$/);
      if (match) {
        const [, name, desc] = match;
        const skillRow = content.createDiv({ cls: "claude-sessions-system-events-skill-row" });
        skillRow.createSpan({ cls: "claude-sessions-system-events-skill-name", text: name });
        const shortDesc = desc.length > 80 ? desc.slice(0, 80) + "..." : desc;
        skillRow.createSpan({ cls: "claude-sessions-system-events-skill-desc", text: shortDesc });
      }
    }
  }
}
function renderTasksSection(container, tasks) {
  const section = container.createDiv({ cls: "claude-sessions-system-events-section" });
  const sectionHeader = section.createDiv({ cls: "claude-sessions-system-events-section-header" });
  const headerIcon = sectionHeader.createSpan({ cls: "claude-sessions-system-events-section-icon" });
  (0, import_obsidian7.setIcon)(headerIcon, "check-square");
  const totalItems = tasks.reduce((sum, t) => sum + t.itemCount, 0);
  sectionHeader.createSpan({ text: `Task reminders (${totalItems} items)` });
}

// src/views/tool-renderer.ts
var import_obsidian8 = require("obsidian");

// node_modules/diff/libesm/diff/base.js
var Diff = class {
  diff(oldStr, newStr, options = {}) {
    let callback;
    if (typeof options === "function") {
      callback = options;
      options = {};
    } else if ("callback" in options) {
      callback = options.callback;
    }
    const oldString = this.castInput(oldStr, options);
    const newString = this.castInput(newStr, options);
    const oldTokens = this.removeEmpty(this.tokenize(oldString, options));
    const newTokens = this.removeEmpty(this.tokenize(newString, options));
    return this.diffWithOptionsObj(oldTokens, newTokens, options, callback);
  }
  diffWithOptionsObj(oldTokens, newTokens, options, callback) {
    var _a;
    const done = (value) => {
      value = this.postProcess(value, options);
      if (callback) {
        setTimeout(function() {
          callback(value);
        }, 0);
        return void 0;
      } else {
        return value;
      }
    };
    const newLen = newTokens.length, oldLen = oldTokens.length;
    let editLength = 1;
    let maxEditLength = newLen + oldLen;
    if (options.maxEditLength != null) {
      maxEditLength = Math.min(maxEditLength, options.maxEditLength);
    }
    const maxExecutionTime = (_a = options.timeout) !== null && _a !== void 0 ? _a : Infinity;
    const abortAfterTimestamp = Date.now() + maxExecutionTime;
    const bestPath = [{ oldPos: -1, lastComponent: void 0 }];
    let newPos = this.extractCommon(bestPath[0], newTokens, oldTokens, 0, options);
    if (bestPath[0].oldPos + 1 >= oldLen && newPos + 1 >= newLen) {
      return done(this.buildValues(bestPath[0].lastComponent, newTokens, oldTokens));
    }
    let minDiagonalToConsider = -Infinity, maxDiagonalToConsider = Infinity;
    const execEditLength = () => {
      for (let diagonalPath = Math.max(minDiagonalToConsider, -editLength); diagonalPath <= Math.min(maxDiagonalToConsider, editLength); diagonalPath += 2) {
        let basePath;
        const removePath = bestPath[diagonalPath - 1], addPath = bestPath[diagonalPath + 1];
        if (removePath) {
          bestPath[diagonalPath - 1] = void 0;
        }
        let canAdd = false;
        if (addPath) {
          const addPathNewPos = addPath.oldPos - diagonalPath;
          canAdd = addPath && 0 <= addPathNewPos && addPathNewPos < newLen;
        }
        const canRemove = removePath && removePath.oldPos + 1 < oldLen;
        if (!canAdd && !canRemove) {
          bestPath[diagonalPath] = void 0;
          continue;
        }
        if (!canRemove || canAdd && removePath.oldPos < addPath.oldPos) {
          basePath = this.addToPath(addPath, true, false, 0, options);
        } else {
          basePath = this.addToPath(removePath, false, true, 1, options);
        }
        newPos = this.extractCommon(basePath, newTokens, oldTokens, diagonalPath, options);
        if (basePath.oldPos + 1 >= oldLen && newPos + 1 >= newLen) {
          return done(this.buildValues(basePath.lastComponent, newTokens, oldTokens)) || true;
        } else {
          bestPath[diagonalPath] = basePath;
          if (basePath.oldPos + 1 >= oldLen) {
            maxDiagonalToConsider = Math.min(maxDiagonalToConsider, diagonalPath - 1);
          }
          if (newPos + 1 >= newLen) {
            minDiagonalToConsider = Math.max(minDiagonalToConsider, diagonalPath + 1);
          }
        }
      }
      editLength++;
    };
    if (callback) {
      (function exec() {
        setTimeout(function() {
          if (editLength > maxEditLength || Date.now() > abortAfterTimestamp) {
            return callback(void 0);
          }
          if (!execEditLength()) {
            exec();
          }
        }, 0);
      })();
    } else {
      while (editLength <= maxEditLength && Date.now() <= abortAfterTimestamp) {
        const ret = execEditLength();
        if (ret) {
          return ret;
        }
      }
    }
  }
  addToPath(path5, added, removed, oldPosInc, options) {
    const last = path5.lastComponent;
    if (last && !options.oneChangePerToken && last.added === added && last.removed === removed) {
      return {
        oldPos: path5.oldPos + oldPosInc,
        lastComponent: { count: last.count + 1, added, removed, previousComponent: last.previousComponent }
      };
    } else {
      return {
        oldPos: path5.oldPos + oldPosInc,
        lastComponent: { count: 1, added, removed, previousComponent: last }
      };
    }
  }
  extractCommon(basePath, newTokens, oldTokens, diagonalPath, options) {
    const newLen = newTokens.length, oldLen = oldTokens.length;
    let oldPos = basePath.oldPos, newPos = oldPos - diagonalPath, commonCount = 0;
    while (newPos + 1 < newLen && oldPos + 1 < oldLen && this.equals(oldTokens[oldPos + 1], newTokens[newPos + 1], options)) {
      newPos++;
      oldPos++;
      commonCount++;
      if (options.oneChangePerToken) {
        basePath.lastComponent = { count: 1, previousComponent: basePath.lastComponent, added: false, removed: false };
      }
    }
    if (commonCount && !options.oneChangePerToken) {
      basePath.lastComponent = { count: commonCount, previousComponent: basePath.lastComponent, added: false, removed: false };
    }
    basePath.oldPos = oldPos;
    return newPos;
  }
  equals(left, right, options) {
    if (options.comparator) {
      return options.comparator(left, right);
    } else {
      return left === right || !!options.ignoreCase && left.toLowerCase() === right.toLowerCase();
    }
  }
  removeEmpty(array) {
    const ret = [];
    for (let i = 0; i < array.length; i++) {
      if (array[i]) {
        ret.push(array[i]);
      }
    }
    return ret;
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  castInput(value, options) {
    return value;
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  tokenize(value, options) {
    return Array.from(value);
  }
  join(chars) {
    return chars.join("");
  }
  postProcess(changeObjects, options) {
    return changeObjects;
  }
  get useLongestToken() {
    return false;
  }
  buildValues(lastComponent, newTokens, oldTokens) {
    const components = [];
    let nextComponent;
    while (lastComponent) {
      components.push(lastComponent);
      nextComponent = lastComponent.previousComponent;
      delete lastComponent.previousComponent;
      lastComponent = nextComponent;
    }
    components.reverse();
    const componentLen = components.length;
    let componentPos = 0, newPos = 0, oldPos = 0;
    for (; componentPos < componentLen; componentPos++) {
      const component = components[componentPos];
      if (!component.removed) {
        if (!component.added && this.useLongestToken) {
          let value = newTokens.slice(newPos, newPos + component.count);
          value = value.map(function(value2, i) {
            const oldValue = oldTokens[oldPos + i];
            return oldValue.length > value2.length ? oldValue : value2;
          });
          component.value = this.join(value);
        } else {
          component.value = this.join(newTokens.slice(newPos, newPos + component.count));
        }
        newPos += component.count;
        if (!component.added) {
          oldPos += component.count;
        }
      } else {
        component.value = this.join(oldTokens.slice(oldPos, oldPos + component.count));
        oldPos += component.count;
      }
    }
    return components;
  }
};

// node_modules/diff/libesm/diff/line.js
var LineDiff = class extends Diff {
  constructor() {
    super(...arguments);
    this.tokenize = tokenize;
  }
  equals(left, right, options) {
    if (options.ignoreWhitespace) {
      if (!options.newlineIsToken || !left.includes("\n")) {
        left = left.trim();
      }
      if (!options.newlineIsToken || !right.includes("\n")) {
        right = right.trim();
      }
    } else if (options.ignoreNewlineAtEof && !options.newlineIsToken) {
      if (left.endsWith("\n")) {
        left = left.slice(0, -1);
      }
      if (right.endsWith("\n")) {
        right = right.slice(0, -1);
      }
    }
    return super.equals(left, right, options);
  }
};
var lineDiff = new LineDiff();
function diffLines(oldStr, newStr, options) {
  return lineDiff.diff(oldStr, newStr, options);
}
function tokenize(value, options) {
  if (options.stripTrailingCr) {
    value = value.replace(/\r\n/g, "\n");
  }
  const retLines = [], linesAndNewlines = value.split(/(\n|\r\n)/);
  if (!linesAndNewlines[linesAndNewlines.length - 1]) {
    linesAndNewlines.pop();
  }
  for (let i = 0; i < linesAndNewlines.length; i++) {
    const line = linesAndNewlines[i];
    if (i % 2 && !options.newlineIsToken) {
      retLines[retLines.length - 1] += line;
    } else {
      retLines.push(line);
    }
  }
  return retLines;
}

// src/views/tool-renderer.ts
init_constants();
function renderToolGroup(blocks, container, ctx, delegate, groupThreshold, contentBlockIndices) {
  const toolUses = blocks.filter((b) => b.type === "tool_use");
  const toolResults = blocks.filter((b) => b.type === "tool_result");
  const resultMap = /* @__PURE__ */ new Map();
  for (const r of toolResults) {
    resultMap.set(r.toolUseId, r);
  }
  const toolUseIdxMap = /* @__PURE__ */ new Map();
  const toolResultIdxMap = /* @__PURE__ */ new Map();
  if (contentBlockIndices && contentBlockIndices.length === blocks.length) {
    for (let i = 0; i < blocks.length; i++) {
      const b = blocks[i];
      if (b.type === "tool_use") toolUseIdxMap.set(b.id, contentBlockIndices[i]);
      else if (b.type === "tool_result") toolResultIdxMap.set(b.toolUseId, contentBlockIndices[i]);
    }
  }
  if (!ctx.settings.showToolCalls) return;
  const threshold = groupThreshold != null ? groupThreshold : ctx.settings.toolGroupThreshold;
  if (toolUses.length <= threshold) {
    for (const tu of toolUses) {
      renderToolCall(tu, resultMap.get(tu.id), container, ctx, delegate, toolUseIdxMap.get(tu.id), toolResultIdxMap.get(tu.id));
    }
  } else {
    const groupEl = container.createDiv({ cls: "claude-sessions-tool-group" });
    const groupHeader = groupEl.createDiv({ cls: "claude-sessions-tool-group-header" });
    groupHeader.createSpan({ cls: "claude-sessions-tool-group-chevron", text: "\u25B6" });
    const uniqueNames = [...new Set(toolUses.map((t) => t.name))].join(", ");
    const hasError = toolResults.some((r) => r.isError);
    if (hasError) {
      groupHeader.createSpan({ cls: "claude-sessions-tool-indicator claude-sessions-tool-error" });
    }
    groupHeader.createSpan({ text: `${toolUses.length} tool calls ` });
    groupHeader.createSpan({ cls: "claude-sessions-tool-group-names", text: uniqueNames });
    const groupBody = groupEl.createDiv({ cls: "claude-sessions-tool-group-body" });
    makeClickable(groupHeader, { label: `Toggle ${toolUses.length} tool calls`, expanded: false });
    groupHeader.addEventListener("click", () => {
      const willOpen = !groupEl.hasClass("open");
      groupEl.toggleClass("open", willOpen);
      groupHeader.setAttribute("aria-expanded", String(willOpen));
    });
    for (const tu of toolUses) {
      renderToolCall(tu, resultMap.get(tu.id), groupBody, ctx, delegate, toolUseIdxMap.get(tu.id), toolResultIdxMap.get(tu.id));
    }
  }
}
function renderToolCall(block, result, container, ctx, delegate, toolUseContentBlockIdx, toolResultContentBlockIdx) {
  var _a, _b, _c, _d, _e, _f;
  const toolEl = container.createDiv({ cls: "claude-sessions-tool-block" });
  const header = toolEl.createDiv({ cls: "claude-sessions-tool-header" });
  const isError = (_a = result == null ? void 0 : result.isError) != null ? _a : false;
  const indicatorCls = block.isOrphaned || block.isPending ? "claude-sessions-tool-indicator claude-sessions-tool-orphaned" : `claude-sessions-tool-indicator ${isError ? "claude-sessions-tool-error" : ""}`;
  header.createSpan({ cls: indicatorCls });
  const mcpParts = parseMcpToolName(block.name);
  if (mcpParts) {
    header.createSpan({ cls: "claude-sessions-tool-mcp-server", text: mcpParts.server });
    header.createSpan({ cls: "claude-sessions-tool-name", text: mcpParts.tool });
  } else {
    header.createSpan({ cls: "claude-sessions-tool-name", text: block.name });
  }
  header.createSpan({ cls: "claude-sessions-tool-preview", text: toolPreview(block) });
  if (block.isPending) {
    header.createSpan({ cls: "claude-sessions-tool-duration claude-sessions-tool-orphaned-label", text: "in progress" });
  } else if (block.isOrphaned) {
    header.createSpan({ cls: "claude-sessions-tool-duration claude-sessions-tool-orphaned-label", text: "interrupted" });
  } else if (((_b = block.subAgentSession) == null ? void 0 : _b.isBackground) && block.subAgentSession.durationMs) {
    header.createSpan({
      cls: "claude-sessions-tool-duration",
      text: formatToolDuration(block.subAgentSession.durationMs)
    });
  } else if (block.timestamp && (result == null ? void 0 : result.timestamp)) {
    const elapsed = new Date(result.timestamp).getTime() - new Date(block.timestamp).getTime();
    if (elapsed > 0 && !isNaN(elapsed)) {
      header.createSpan({ cls: "claude-sessions-tool-duration", text: formatToolDuration(elapsed) });
    }
  }
  if (((_c = block.subAgentSession) == null ? void 0 : _c.isBackground) && !block.subAgentSession.durationMs) {
    header.createSpan({
      cls: "claude-sessions-tool-duration claude-sessions-tool-background-label",
      text: "background"
    });
  }
  const hookEvents = (_d = ctx.hookEventsByToolId) == null ? void 0 : _d.get(block.id);
  if (hookEvents && hookEvents.length > 0) {
    const hasPreToolUse = hookEvents.some((h) => h.hookEvent === "PreToolUse");
    const hasPermission = hookEvents.some((h) => h.hookEvent === "PermissionRequest");
    if (hasPreToolUse) {
      const preToolIndicator = header.createSpan({ cls: "claude-sessions-tool-hook-indicator" });
      (0, import_obsidian8.setIcon)(preToolIndicator, "zap");
      const preToolEvents = hookEvents.filter((h) => h.hookEvent === "PreToolUse");
      const tooltip = preToolEvents.map((h) => {
        const parts = ["PreToolUse"];
        if (h.type === "hook_success" && h.durationMs > 0) parts.push(`${h.durationMs}ms`);
        return parts.join(" \xB7 ");
      }).join("\n");
      preToolIndicator.setAttribute("aria-label", tooltip);
      preToolIndicator.setAttribute("data-tooltip-position", "top");
    }
    if (hasPermission) {
      const permIndicator = header.createSpan({ cls: "claude-sessions-tool-hook-indicator claude-sessions-tool-hook-permission" });
      (0, import_obsidian8.setIcon)(permIndicator, "shield-check");
      permIndicator.setAttribute("aria-label", "Permission request");
      permIndicator.setAttribute("data-tooltip-position", "top");
    }
  }
  header.createSpan({ cls: "claude-sessions-tool-chevron", text: "\u25B6" });
  const body = toolEl.createDiv({ cls: "claude-sessions-tool-body" });
  if (block.name === "AskUserQuestion") {
    renderAskUserQuestion(block, result, body, ctx);
  } else if ((block.name === "Agent" || block.name === "Task") && block.subAgentSession) {
    renderSubAgentSession(block.subAgentSession, body, result, ctx, delegate);
  } else if (block.name === "Edit" && block.input["old_string"] != null) {
    renderDiffView(block, result, body, ctx, toolUseContentBlockIdx, toolResultContentBlockIdx);
  } else if (block.name === "Write" && block.input["content"] != null) {
    renderWriteView(block, result, body, ctx, toolUseContentBlockIdx, toolResultContentBlockIdx);
  } else if (block.name === "Bash") {
    renderBashInput(block, body, ctx, toolUseContentBlockIdx);
  } else if (Object.keys(block.input).length > 0) {
    const inputEl = body.createDiv({ cls: "claude-sessions-tool-input" });
    if (toolUseContentBlockIdx !== void 0) {
      inputEl.setAttribute("data-content-block-idx", String(toolUseContentBlockIdx));
    }
    inputEl.createDiv({ cls: "claude-sessions-tool-section-label", text: "INPUT" });
    const inputText = formatInput(block.input);
    const inputMd = fence(inputText, "json");
    const inputMdContainer = inputEl.createDiv({ cls: "claude-sessions-tool-input-code" });
    void import_obsidian8.MarkdownRenderer.render(ctx.app, inputMd, inputMdContainer, "", ctx.component);
  }
  if (result && ctx.settings.showToolResults && block.name !== "AskUserQuestion" && !(block.name === "Edit" && block.input["old_string"] != null) && !(block.name === "Write" && block.input["content"] != null) && !((block.name === "Agent" || block.name === "Task") && block.subAgentSession)) {
    const resultContainer = toolResultContentBlockIdx !== void 0 ? body.createDiv({
      cls: "claude-sessions-tool-result-wrapper",
      attr: { "data-content-block-idx": String(toolResultContentBlockIdx) }
    }) : body;
    renderToolResult(block, result, isError, resultContainer, ctx, delegate);
  }
  const preToolUseHooks = (_e = hookEvents == null ? void 0 : hookEvents.filter((h) => h.type === "hook_success")) != null ? _e : [];
  if (preToolUseHooks.length > 0) {
    const hookSection = body.createDiv({ cls: "claude-sessions-tool-hook-section" });
    hookSection.createDiv({ cls: "claude-sessions-tool-section-label", text: "HOOKS" });
    for (const hook of preToolUseHooks) {
      const hookEl = hookSection.createDiv({ cls: "claude-sessions-tool-hook-item" });
      const hookHeader = hookEl.createDiv({ cls: "claude-sessions-tool-hook-header" });
      hookHeader.createSpan({ cls: "claude-sessions-tool-hook-event", text: hook.hookEvent });
      if (hook.durationMs > 0) {
        hookHeader.createSpan({ cls: "claude-sessions-tool-hook-duration", text: `${hook.durationMs}ms` });
      }
      if (hook.command) {
        const cmdName = (_f = hook.command.split("/").pop()) != null ? _f : hook.command;
        hookHeader.createSpan({ cls: "claude-sessions-tool-hook-command", text: cmdName });
      }
      if (hook.stdout && hook.stdout.trim()) {
        const stdoutEl = hookEl.createDiv({ cls: "claude-sessions-tool-hook-stdout" });
        try {
          const parsed = JSON.parse(hook.stdout);
          const formatted = JSON.stringify(parsed, null, 2);
          const codeEl = stdoutEl.createEl("pre");
          codeEl.createEl("code", { text: formatted });
        } catch (e) {
          stdoutEl.createEl("pre").createEl("code", { text: hook.stdout.trim() });
        }
      }
    }
  }
  makeClickable(header, { label: `Toggle ${block.name} details`, expanded: false });
  header.addEventListener("click", () => {
    const willOpen = !toolEl.hasClass("open");
    toolEl.toggleClass("open", willOpen);
    header.setAttribute("aria-expanded", String(willOpen));
  });
}
function hasAnsiCodes(text) {
  return ANSI_RE.test(text);
}
function renderToolResult(block, result, isError, body, ctx, delegate) {
  const resultEl = body.createDiv({
    cls: `claude-sessions-tool-result ${isError ? "claude-sessions-tool-result-error" : ""}`
  });
  resultEl.createDiv({ cls: "claude-sessions-tool-section-label", text: "RESULT" });
  const resultText = result.content.length > 5e3 ? result.content.substring(0, 5e3) + "\n... (truncated)" : result.content;
  if (TASK_TOOL_NAMES.has(block.name) && !isError && result.enrichedResult) {
    renderTaskResult(block, result.enrichedResult, resultEl, delegate);
  } else if (block.name === "ToolSearch" && !isError && result.enrichedResult) {
    renderToolSearchResult(result.enrichedResult, resultEl);
  } else if (result.images && result.images.length > 0) {
    renderToolResultImages(result.images, resultEl, delegate);
    if (resultText) {
      const md = fence(resultText);
      const mdContainer = resultEl.createDiv({ cls: "claude-sessions-tool-result-code" });
      void import_obsidian8.MarkdownRenderer.render(ctx.app, md, mdContainer, "", ctx.component);
    }
  } else if (block.name === "Read" && !isError) {
    const filePath = typeof block.input["file_path"] === "string" ? block.input["file_path"] : "";
    const lang = langFromPath(filePath);
    const cleaned = stripLineNumbers(resultText).replace(RE_SYSTEM_REMINDER, "").trim();
    const isMarkdownFile = /\.mdx?$/i.test(filePath);
    if (isMarkdownFile) {
      renderMarkdownToggle(cleaned, lang, resultEl, ctx);
    } else {
      const md = fence(cleaned, lang);
      const mdContainer = resultEl.createDiv({ cls: "claude-sessions-read-result" });
      void import_obsidian8.MarkdownRenderer.render(ctx.app, md, mdContainer, "", ctx.component);
    }
  } else if (block.name === "Bash" && !isError && isBashDiffResult(block, resultText)) {
    const resultMd = fence(resultText, "diff");
    const resultMdContainer = resultEl.createDiv({ cls: "claude-sessions-tool-result-code" });
    void import_obsidian8.MarkdownRenderer.render(ctx.app, resultMd, resultMdContainer, "", ctx.component);
  } else if (block.name === "Bash" && !isError && hasAnsiCodes(resultText)) {
    const pre = resultEl.createEl("pre", { cls: "claude-sessions-ansi-block" });
    delegate.buildAnsiDom(resultText, pre);
  } else {
    const resultMd = fence(resultText);
    const resultMdContainer = resultEl.createDiv({ cls: "claude-sessions-tool-result-code" });
    void import_obsidian8.MarkdownRenderer.render(ctx.app, resultMd, resultMdContainer, "", ctx.component);
  }
  if (block.name === "Bash" && result.enrichedResult) {
    const exitCode = typeof result.enrichedResult["exitCode"] === "number" ? result.enrichedResult["exitCode"] : void 0;
    const stderr = typeof result.enrichedResult["stderr"] === "string" ? result.enrichedResult["stderr"] : void 0;
    if (exitCode != null && exitCode !== 0) {
      resultEl.createDiv({ cls: "claude-sessions-tool-exit-code", text: `Exit code: ${exitCode}` });
    }
    if (stderr == null ? void 0 : stderr.trim()) {
      const stderrLabel = resultEl.createDiv({ cls: "claude-sessions-tool-section-label" });
      stderrLabel.createSpan({ text: "STDERR" });
      const stderrText = stderr.length > 2e3 ? stderr.substring(0, 2e3) + "\n... (truncated)" : stderr;
      const stderrMd = fence(stderrText);
      const stderrContainer = resultEl.createDiv({ cls: "claude-sessions-tool-result-code claude-sessions-tool-result-error" });
      void import_obsidian8.MarkdownRenderer.render(ctx.app, stderrMd, stderrContainer, "", ctx.component);
    }
  }
}
function renderToolResultImages(images, container, delegate) {
  for (const image of images) {
    const dataUri = `data:${image.mediaType};base64,${image.data}`;
    const img = container.createEl("img", {
      cls: "claude-sessions-image-thumbnail",
      attr: { src: dataUri, alt: "Tool result image" }
    });
    makeClickable(img, { label: "View tool result image" });
    img.addEventListener("click", () => {
      delegate.openImageModal(dataUri, image.mediaType);
    });
  }
}
var TASK_STATUS_ICON = {
  pending: "circle",
  in_progress: "circle-dot",
  completed: "circle-check"
};
var TASK_STATUS_CLS = {
  pending: "pending",
  in_progress: "in-progress",
  completed: "completed"
};
function renderTaskResult(block, enriched, container, delegate) {
  const ts = delegate.taskState;
  if (block.name === "TaskCreate" && enriched["task"]) {
    const t = enriched["task"];
    const id = typeof t["id"] === "string" ? t["id"] : "";
    ts.set(id, { id, subject: typeof t["subject"] === "string" ? t["subject"] : "", status: "pending" });
  } else if (block.name === "TaskUpdate" && enriched["taskId"]) {
    const id = typeof enriched["taskId"] === "string" ? enriched["taskId"] : "";
    const existing = ts.get(id);
    const sc = enriched["statusChange"];
    const status = (sc == null ? void 0 : sc["to"]) || (existing == null ? void 0 : existing.status) || "pending";
    ts.set(id, { id, subject: (existing == null ? void 0 : existing.subject) || "", status });
  } else if (block.name === "TaskList" && Array.isArray(enriched["tasks"])) {
    ts.clear();
    for (const t of enriched["tasks"]) {
      const id = typeof t["id"] === "string" ? t["id"] : "";
      ts.set(id, {
        id,
        subject: typeof t["subject"] === "string" ? t["subject"] : "",
        status: typeof t["status"] === "string" ? t["status"] : "pending"
      });
    }
  } else if (block.name === "TaskGet" && enriched["id"]) {
    const id = typeof enriched["id"] === "string" ? enriched["id"] : "";
    const existing = ts.get(id);
    ts.set(id, {
      id,
      subject: typeof enriched["subject"] === "string" ? enriched["subject"] : (existing == null ? void 0 : existing.subject) || "",
      status: typeof enriched["status"] === "string" ? enriched["status"] : (existing == null ? void 0 : existing.status) || "pending"
    });
  }
  if (ts.size === 0) return;
  const list = container.createDiv({ cls: "claude-sessions-task-list" });
  for (const task of ts.values()) {
    const statusCls = TASK_STATUS_CLS[task.status] || "pending";
    const item = list.createDiv({ cls: `claude-sessions-task-item claude-sessions-task-${statusCls}` });
    const icon = item.createSpan({ cls: "claude-sessions-task-icon" });
    (0, import_obsidian8.setIcon)(icon, TASK_STATUS_ICON[task.status] || "circle");
    const label = item.createSpan({ cls: "claude-sessions-task-label" });
    label.createSpan({ cls: "claude-sessions-task-id", text: `#${task.id}` });
    label.createSpan({ text: task.subject });
  }
}
function renderToolSearchResult(enriched, container) {
  const matches = Array.isArray(enriched["matches"]) ? enriched["matches"] : [];
  const total = typeof enriched["total_deferred_tools"] === "number" ? enriched["total_deferred_tools"] : void 0;
  if (matches.length === 0) {
    container.createDiv({ cls: "claude-sessions-toolsearch-empty", text: "No matching tools found" });
    return;
  }
  const list = container.createDiv({ cls: "claude-sessions-toolsearch-matches" });
  for (const name of matches) {
    const item = list.createDiv({ cls: "claude-sessions-toolsearch-match" });
    const icon = item.createSpan({ cls: "claude-sessions-toolsearch-icon" });
    (0, import_obsidian8.setIcon)(icon, "wrench");
    item.createSpan({ text: name });
  }
  if (total != null) {
    container.createDiv({
      cls: "claude-sessions-toolsearch-total",
      text: `${matches.length} of ${total} deferred tools matched`
    });
  }
}
function renderAskUserQuestion(block, result, container, _ctx) {
  var _a, _b;
  const questions = block.input["questions"];
  if (!(questions == null ? void 0 : questions.length)) return;
  const rawJson = formatInput(block.input);
  const answers = /* @__PURE__ */ new Map();
  const isRejected = (_a = result == null ? void 0 : result.isError) != null ? _a : false;
  if (result && !isRejected) {
    const answerMatches = result.content.matchAll(/"([^"]+)"="([^"]+)"/g);
    for (const m of answerMatches) {
      answers.set(m[1], m[2]);
    }
  }
  const wrapper = container.createDiv({ cls: "claude-sessions-ask-user" });
  for (let i = 0; i < questions.length; i++) {
    const q = questions[i];
    const questionEl = wrapper.createDiv({ cls: "claude-sessions-ask-question" });
    const headerRow = questionEl.createDiv({ cls: "claude-sessions-ask-header-row" });
    if (q.header) {
      headerRow.createSpan({ cls: "claude-sessions-ask-header", text: q.header });
    }
    if (i === 0) {
      addCopyButton(headerRow, rawJson, "Copy raw JSON");
    }
    questionEl.createDiv({ cls: "claude-sessions-ask-text", text: q.question });
    const optionsEl = questionEl.createDiv({ cls: "claude-sessions-ask-options" });
    const answer = (_b = answers.get(q.question)) != null ? _b : "";
    const selectedLabels = new Set(answer.split(", ").map((s) => s.trim()).filter(Boolean));
    for (const opt of q.options) {
      const isSelected = selectedLabels.has(opt.label);
      const optEl = optionsEl.createDiv({
        cls: `claude-sessions-ask-option ${isSelected ? "claude-sessions-ask-option-selected" : ""}`
      });
      const labelRow = optEl.createDiv({ cls: "claude-sessions-ask-option-label" });
      labelRow.createSpan({ text: opt.label });
      if (isSelected) {
        const check = labelRow.createSpan({ cls: "claude-sessions-ask-option-check" });
        (0, import_obsidian8.setIcon)(check, "check");
      }
      if (opt.description) {
        optEl.createDiv({ cls: "claude-sessions-ask-option-desc", text: opt.description });
      }
    }
  }
  if (isRejected && result) {
    const rejectedEl = wrapper.createDiv({ cls: "claude-sessions-ask-rejected" });
    rejectedEl.createDiv({ cls: "claude-sessions-tool-section-label", text: "CLARIFICATION" });
    rejectedEl.createDiv({ cls: "claude-sessions-ask-rejected-text", text: result.content });
  }
  if (answers.size > 0) {
    const answerEl = wrapper.createDiv({ cls: "claude-sessions-ask-answers" });
    answerEl.createDiv({ cls: "claude-sessions-tool-section-label", text: "USER ANSWERS" });
    for (const [question, answer] of answers) {
      const row = answerEl.createDiv({ cls: "claude-sessions-ask-answer-row" });
      row.createSpan({ cls: "claude-sessions-ask-answer-q", text: question + " " });
      row.createSpan({ cls: "claude-sessions-ask-answer-a", text: answer });
    }
  }
}
function renderBashInput(block, container, ctx, toolUseContentBlockIdx) {
  const inputEl = container.createDiv({ cls: "claude-sessions-tool-input" });
  if (toolUseContentBlockIdx !== void 0) {
    inputEl.setAttribute("data-content-block-idx", String(toolUseContentBlockIdx));
  }
  inputEl.createDiv({ cls: "claude-sessions-tool-section-label", text: "INPUT" });
  const command = typeof block.input["command"] === "string" ? block.input["command"] : "";
  const md = fence(command, "bash");
  const mdContainer = inputEl.createDiv({ cls: "claude-sessions-tool-input-code" });
  void import_obsidian8.MarkdownRenderer.render(ctx.app, md, mdContainer, "", ctx.component);
}
function renderMarkdownToggle(content, lang, container, ctx) {
  const wrapper = container.createDiv({ cls: "claude-sessions-read-result claude-sessions-read-md-toggle" });
  const toggleRow = wrapper.createDiv({ cls: "claude-sessions-read-md-toggle-row" });
  const codeBtn = toggleRow.createEl("button", {
    cls: "claude-sessions-read-md-btn clickable-icon is-active",
    attr: { "aria-pressed": "true" }
  });
  (0, import_obsidian8.setIcon)(codeBtn, "code");
  makeClickable(codeBtn, { label: "Show raw code" });
  const previewBtn = toggleRow.createEl("button", {
    cls: "claude-sessions-read-md-btn clickable-icon",
    attr: { "aria-pressed": "false" }
  });
  (0, import_obsidian8.setIcon)(previewBtn, "eye");
  makeClickable(previewBtn, { label: "Show rendered Markdown" });
  const codeView = wrapper.createDiv({ cls: "claude-sessions-read-md-code" });
  const codeMd = fence(content, lang);
  void import_obsidian8.MarkdownRenderer.render(ctx.app, codeMd, codeView, "", ctx.component);
  const previewView = wrapper.createDiv({ cls: "claude-sessions-read-md-preview claude-sessions-read-md-hidden" });
  void import_obsidian8.MarkdownRenderer.render(ctx.app, content, previewView, "", ctx.component);
  const setActive = (mode) => {
    const isCode = mode === "code";
    codeBtn.toggleClass("is-active", isCode);
    previewBtn.toggleClass("is-active", !isCode);
    codeBtn.setAttribute("aria-pressed", String(isCode));
    previewBtn.setAttribute("aria-pressed", String(!isCode));
    codeView.toggleClass("claude-sessions-read-md-hidden", !isCode);
    previewView.toggleClass("claude-sessions-read-md-hidden", isCode);
  };
  codeBtn.addEventListener("click", () => setActive("code"));
  previewBtn.addEventListener("click", () => setActive("preview"));
}
function renderSubAgentSession(session, container, result, ctx, delegate) {
  const timeline = container.createDiv({ cls: "claude-sessions-subagent-timeline" });
  const promptSection = timeline.createDiv({ cls: "claude-sessions-subagent-prompt" });
  const promptHeader = promptSection.createDiv({ cls: "claude-sessions-subagent-prompt-header" });
  promptHeader.createSpan({ cls: "claude-sessions-subagent-prompt-chevron", text: "\u25B6" });
  promptHeader.createSpan({ cls: "claude-sessions-tool-section-label", text: "PROMPT" });
  const promptBody = promptSection.createDiv({ cls: "claude-sessions-subagent-prompt-body" });
  delegate.renderTextContent(session.prompt, promptBody, "claude-sessions-user-text");
  makeClickable(promptHeader, { label: "Toggle sub-agent prompt", expanded: false });
  promptHeader.addEventListener("click", () => {
    const willOpen = !promptSection.hasClass("open");
    promptSection.toggleClass("open", willOpen);
    promptHeader.setAttribute("aria-expanded", String(willOpen));
  });
  const allBlocks = [];
  for (const turn of session.turns) {
    if (turn.role === "user") continue;
    for (const block of turn.contentBlocks) {
      allBlocks.push(block);
    }
  }
  if (allBlocks.length > 0) {
    const turnEl = timeline.createDiv({ cls: "claude-sessions-subagent-turn" });
    delegate.renderAssistantBlocks(allBlocks, turnEl, 0, 0);
  }
  if (result && ctx.settings.showToolResults) {
    const outputEl = timeline.createDiv({ cls: "claude-sessions-subagent-output" });
    const outputLabel = outputEl.createDiv({ cls: "claude-sessions-tool-section-label" });
    outputLabel.createSpan({ text: "OUTPUT" });
    addCopyButton(outputLabel, result.content, "Copy output");
    const lines = result.content.split("\n").length;
    if (lines > COLLAPSE_THRESHOLD) {
      const wrapEl = outputEl.createDiv({ cls: "claude-sessions-collapsible-wrap is-collapsed" });
      const contentEl = wrapEl.createDiv({ cls: "claude-sessions-collapsible-content" });
      const bodyEl = contentEl.createDiv({ cls: "claude-sessions-subagent-output-body" });
      void import_obsidian8.MarkdownRenderer.render(ctx.app, result.content, bodyEl, "", ctx.component);
      wrapEl.createDiv({ cls: "claude-sessions-collapsible-fade" });
      const toggleBtn = wrapEl.createEl("button", {
        cls: "claude-sessions-collapsible-toggle",
        text: `Show more (${lines} lines)`,
        attr: { "aria-expanded": "false", "data-line-count": String(lines) }
      });
      toggleBtn.addEventListener("click", () => {
        const collapsed = wrapEl.hasClass("is-collapsed");
        wrapEl.toggleClass("is-collapsed", !collapsed);
        toggleBtn.setText(collapsed ? "Show less" : `Show more (${lines} lines)`);
        toggleBtn.setAttribute("aria-expanded", String(collapsed));
      });
    } else {
      const bodyEl = outputEl.createDiv({ cls: "claude-sessions-subagent-output-body" });
      void import_obsidian8.MarkdownRenderer.render(ctx.app, result.content, bodyEl, "", ctx.component);
    }
  }
}
function isBashDiffResult(block, resultText) {
  const command = (typeof block.input["command"] === "string" ? block.input["command"] : "").toLowerCase();
  if (!/\bdiff\b/.test(command)) return false;
  return /^(diff\s|---\s|@@\s)/m.test(resultText);
}
function renderDiffView(block, result, container, ctx, toolUseContentBlockIdx, toolResultContentBlockIdx) {
  const diffEl = container.createDiv({ cls: "claude-sessions-diff-view" });
  if (toolUseContentBlockIdx !== void 0) {
    diffEl.setAttribute("data-content-block-idx", String(toolUseContentBlockIdx));
  }
  if (block.input["file_path"]) {
    diffEl.createDiv({
      cls: "claude-sessions-diff-file",
      text: (typeof block.input["file_path"] === "string" ? block.input["file_path"] : "") + (block.input["replace_all"] ? " (replace all)" : "")
    });
  }
  const oldStr = typeof block.input["old_string"] === "string" ? block.input["old_string"] : "";
  const newStr = typeof block.input["new_string"] === "string" ? block.input["new_string"] : "";
  const changes = diffLines(oldStr, newStr);
  const outputLines = [];
  for (const change of changes) {
    const lines = change.value.replace(/\n$/, "").split("\n");
    const prefix = change.added ? "+ " : change.removed ? "- " : "  ";
    for (const line of lines) {
      outputLines.push(prefix + line);
    }
  }
  const md = fence(outputLines.join("\n"), "diff");
  const mdContainer = diffEl.createDiv({ cls: "claude-sessions-diff-code" });
  void import_obsidian8.MarkdownRenderer.render(ctx.app, md, mdContainer, "", ctx.component);
  if (result == null ? void 0 : result.isError) {
    renderErrorOutput(result, container, ctx, toolResultContentBlockIdx);
  }
}
function renderWriteView(block, result, container, ctx, toolUseContentBlockIdx, toolResultContentBlockIdx) {
  const writeEl = container.createDiv({ cls: "claude-sessions-tool-input" });
  if (toolUseContentBlockIdx !== void 0) {
    writeEl.setAttribute("data-content-block-idx", String(toolUseContentBlockIdx));
  }
  const filePath = typeof block.input["file_path"] === "string" ? block.input["file_path"] : "";
  if (filePath) {
    writeEl.createDiv({ cls: "claude-sessions-diff-file", text: filePath });
  }
  const content = typeof block.input["content"] === "string" ? block.input["content"] : "";
  const lang = langFromPath(filePath);
  const isMarkdownFile = /\.mdx?$/i.test(filePath);
  if (isMarkdownFile) {
    renderMarkdownToggle(content, lang, writeEl, ctx);
  } else {
    const md = fence(content, lang);
    const mdContainer = writeEl.createDiv({ cls: "claude-sessions-tool-input-code" });
    void import_obsidian8.MarkdownRenderer.render(ctx.app, md, mdContainer, "", ctx.component);
  }
  if (result == null ? void 0 : result.isError) {
    renderErrorOutput(result, container, ctx, toolResultContentBlockIdx);
  }
}
function renderErrorOutput(result, container, ctx, toolResultContentBlockIdx) {
  const el = container.createDiv({
    cls: "claude-sessions-tool-result claude-sessions-tool-result-error"
  });
  if (toolResultContentBlockIdx !== void 0) {
    el.setAttribute("data-content-block-idx", String(toolResultContentBlockIdx));
  }
  el.createDiv({ cls: "claude-sessions-tool-section-label", text: "OUTPUT" });
  const md = fence(result.content);
  const mdContainer = el.createDiv({ cls: "claude-sessions-tool-result-code" });
  void import_obsidian8.MarkdownRenderer.render(ctx.app, md, mdContainer, "", ctx.component);
}
function toolPreview(block) {
  var _a, _b, _c, _d;
  const input = block.input;
  const truncate = (s, max) => s.length > max ? s.substring(0, max) + "..." : s;
  const baseName = (p) => {
    var _a2;
    return (_a2 = p.split("/").pop()) != null ? _a2 : p;
  };
  switch (block.name) {
    case "Edit": {
      const fp = typeof input["file_path"] === "string" ? input["file_path"] : "";
      if (!fp) return "";
      const name = baseName(fp);
      const old = input["old_string"];
      const nw = input["new_string"];
      if (old && nw) {
        const ol = old.split("\n").length;
        const nl = nw.split("\n").length;
        return ol === nl ? `${name} \u2014 ${ol} line${ol > 1 ? "s" : ""}` : `${name} \u2014 ${ol} \u2192 ${nl} lines`;
      }
      return name;
    }
    case "Write": {
      const fp = typeof input["file_path"] === "string" ? input["file_path"] : "";
      if (!fp) return "";
      const name = baseName(fp);
      const content = input["content"];
      if (content) {
        return `${name} \u2014 ${content.split("\n").length} lines`;
      }
      return name;
    }
    case "Read": {
      const fp = typeof input["file_path"] === "string" ? input["file_path"] : "";
      if (!fp) return "";
      const name = baseName(fp);
      const limit = input["limit"];
      const offset = input["offset"];
      if (limit) {
        const start = offset != null ? offset : 1;
        return `${name} \u2014 lines ${start}\u2013${start + limit - 1}`;
      }
      return name;
    }
    case "Grep": {
      const pat = input["pattern"];
      if (!pat) return "";
      const patStr = `"${truncate(pat, 30)}"`;
      const glob = input["glob"];
      const path5 = input["path"];
      if (glob) return `${patStr} in ${glob}`;
      if (path5) return `${patStr} in ${baseName(path5)}`;
      return patStr;
    }
    case "Glob": {
      const pat = input["pattern"];
      if (!pat) return "";
      const patStr = `"${truncate(pat, 30)}"`;
      const path5 = input["path"];
      if (path5) return `${patStr} in ${baseName(path5)}`;
      return patStr;
    }
    case "Bash": {
      const desc = input["description"];
      if (desc) return truncate(desc, 50);
      return truncate(typeof input["command"] === "string" ? input["command"] : "", 50);
    }
    case "AskUserQuestion": {
      const qs = input["questions"];
      if (!(qs == null ? void 0 : qs.length)) return "";
      const first = qs[0];
      const q = typeof first["question"] === "string" ? first["question"] : "";
      const prefix = qs.length > 1 ? `${qs.length} questions \u2014 ` : "";
      return `${prefix}${truncate(q, 40)}`;
    }
    case "Agent":
    case "Task": {
      const desc = typeof input["description"] === "string" ? input["description"] : "";
      const subType = input["subagent_type"];
      const prefix = subType ? `${subType} \u2014 ` : "";
      if (desc) return `${prefix}${truncate(desc, 40)}`;
      return subType != null ? subType : "";
    }
    case "WebFetch": {
      const url = input["url"];
      if (url) {
        try {
          const u = new URL(url);
          return truncate(u.hostname + u.pathname, 50);
        } catch (e) {
          return truncate(url, 50);
        }
      }
      return "";
    }
    case "WebSearch": {
      const query = input["query"];
      return query ? `"${truncate(query, 40)}"` : "";
    }
    default: {
      if (Object.keys(input).length === 0) return "";
      const nameField = (_d = (_c = (_b = (_a = input["name"]) != null ? _a : input["path"]) != null ? _b : input["file"]) != null ? _c : input["query"]) != null ? _d : input["command"];
      if (typeof nameField === "string") return truncate(nameField, 50);
      return compactInputPreview(input, 55);
    }
  }
}
function formatToolDuration(ms) {
  if (ms < 1e3) return `${ms}ms`;
  const sec = ms / 1e3;
  if (sec < 60) return `${sec.toFixed(1)}s`;
  const min = Math.floor(sec / 60);
  const remSec = Math.round(sec % 60);
  return `${min}m ${remSec}s`;
}
function formatInput(input) {
  try {
    return JSON.stringify(input, null, 2);
  } catch (e) {
    return JSON.stringify(input);
  }
}
function parseMcpToolName(name) {
  if (!name.startsWith("mcp__")) return null;
  const rest = name.slice(5);
  const sep = rest.indexOf("__");
  if (sep < 0) return null;
  return { server: rest.slice(0, sep), tool: rest.slice(sep + 2) };
}
function compactInputPreview(input, max) {
  const parts = [];
  let len = 0;
  for (const [k, v] of Object.entries(input)) {
    const val = typeof v === "string" ? v : JSON.stringify(v);
    const part = `${k}=${val}`;
    if (len + part.length > max && parts.length > 0) {
      parts.push("...");
      break;
    }
    parts.push(part);
    len += part.length + 1;
  }
  return parts.join(" ");
}

// src/views/timeline-renderer.ts
var SAFE_IMAGE_TYPES = /* @__PURE__ */ new Set([
  "image/png",
  "image/jpeg",
  "image/gif",
  "image/webp",
  "image/svg+xml",
  "image/bmp",
  "image/tiff"
]);
var _TimelineRenderer = class _TimelineRenderer {
  constructor(container, app, component, settings) {
    this.turnEls = [];
    this.sessionStartDate = "";
    this.mermaidObserver = null;
    this.mermaidRafId = 0;
    this.container = container;
    this.ctx = { app, component, settings };
    this.delegate = {
      renderAssistantBlocks: this.renderAssistantBlocks.bind(this),
      renderTextContent: this.renderTextContent.bind(this),
      buildAnsiDom: this.buildAnsiDom.bind(this),
      openImageModal: this.openImageModal.bind(this),
      taskState: /* @__PURE__ */ new Map()
    };
  }
  updateSettings(settings) {
    this.ctx.settings = settings;
  }
  getTurnElements() {
    return this.turnEls;
  }
  /**
   * Render the full timeline of all turns into the container.
   * Returns the array of turn DOM elements.
   */
  renderTimeline(turns, _sessionStartMs = 0, session) {
    this.container.empty();
    this.turnEls = [];
    this.sessionStartDate = "";
    this.delegate.taskState.clear();
    if (session) {
      const hookMap = /* @__PURE__ */ new Map();
      for (const evt of session.systemEvents) {
        if ((evt.type === "hook_success" || evt.type === "async_hook_response") && evt.toolUseId) {
          const existing = hookMap.get(evt.toolUseId);
          if (existing) {
            existing.push(evt);
          } else {
            hookMap.set(evt.toolUseId, [evt]);
          }
        }
      }
      this.ctx.hookEventsByToolId = hookMap;
      renderSummary(session, this.container, this.ctx);
      renderSystemEvents(session, this.container);
    }
    for (const turn of turns) {
      const el = this.renderTurn(turn);
      this.container.appendChild(el);
      this.turnEls.push(el);
    }
    this.setupMermaidObserver();
    this.processMermaidBlocks(this.container);
    return this.turnEls;
  }
  /**
   * Update the hook events map from the session's system events.
   * Call this before refreshing or appending turns during live watch.
   */
  updateHookEvents(session) {
    const hookMap = /* @__PURE__ */ new Map();
    for (const evt of session.systemEvents) {
      if ((evt.type === "hook_success" || evt.type === "async_hook_response") && evt.toolUseId) {
        const existing = hookMap.get(evt.toolUseId);
        if (existing) {
          existing.push(evt);
        } else {
          hookMap.set(evt.toolUseId, [evt]);
        }
      }
    }
    this.ctx.hookEventsByToolId = hookMap;
  }
  /**
   * Append only new turns to the existing timeline without clearing the container.
   * Returns the newly created DOM elements.
   */
  appendTurns(newTurns) {
    const appended = [];
    for (const turn of newTurns) {
      const el = this.renderTurn(turn);
      this.container.appendChild(el);
      this.turnEls.push(el);
      appended.push(el);
    }
    this.processMermaidBlocks(this.container);
    return appended;
  }
  /**
   * Re-render the last turn element in place (e.g. when streaming adds content).
   * Preserves the rest of the timeline untouched.
   */
  refreshLastTurn(turn) {
    this.refreshTurnAt(this.turnEls.length - 1, turn);
  }
  /**
   * Re-render a specific turn by index.
   * Returns the new element, or null if the index is out of range.
   */
  refreshTurnAt(idx, turn) {
    if (idx < 0 || idx >= this.turnEls.length) return null;
    const oldEl = this.turnEls[idx];
    const newEl = this.renderTurn(turn);
    this.container.replaceChild(newEl, oldEl);
    this.turnEls[idx] = newEl;
    return newEl;
  }
  /**
   * Rebuild just the summary/pinned-heroes panel without touching turn DOM.
   * Called during incremental reload when stats have changed.
   */
  refreshSummary(session) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k;
    const wasPinned = (_b = (_a = this.container.querySelector(".claude-sessions-pinned-heroes")) == null ? void 0 : _a.hasClass("is-pinned")) != null ? _b : false;
    const wasOpen = (_d = (_c = this.container.querySelector(".claude-sessions-summary")) == null ? void 0 : _c.hasClass("open")) != null ? _d : false;
    const wasEventsOpen = (_f = (_e = this.container.querySelector(".claude-sessions-system-events")) == null ? void 0 : _e.hasClass("open")) != null ? _f : false;
    (_g = this.container.querySelector(".claude-sessions-pinned-heroes")) == null ? void 0 : _g.remove();
    (_h = this.container.querySelector(".claude-sessions-summary")) == null ? void 0 : _h.remove();
    (_i = this.container.querySelector(".claude-sessions-system-events")) == null ? void 0 : _i.remove();
    const firstTurn = this.turnEls[0];
    if (firstTurn) {
      const frag = activeDocument.createDocumentFragment();
      const tempContainer = createDiv();
      renderSummary(session, tempContainer, this.ctx);
      renderSystemEvents(session, tempContainer);
      while (tempContainer.firstChild) {
        frag.appendChild(tempContainer.firstChild);
      }
      this.container.insertBefore(frag, firstTurn);
    } else {
      renderSummary(session, this.container, this.ctx);
      renderSystemEvents(session, this.container);
    }
    if (wasPinned) {
      (_j = this.container.querySelector(".claude-sessions-pinned-heroes")) == null ? void 0 : _j.addClass("is-pinned");
      (_k = this.container.querySelector(".claude-sessions-heroes-pin")) == null ? void 0 : _k.addClass("is-active");
    }
    if (wasOpen) {
      const summaryEl = this.container.querySelector(".claude-sessions-summary");
      summaryEl == null ? void 0 : summaryEl.addClass("open");
      const header = summaryEl == null ? void 0 : summaryEl.querySelector(".claude-sessions-summary-header");
      header == null ? void 0 : header.setAttribute("aria-expanded", "true");
    }
    if (wasEventsOpen) {
      const eventsEl = this.container.querySelector(".claude-sessions-system-events");
      eventsEl == null ? void 0 : eventsEl.addClass("open");
      const header = eventsEl == null ? void 0 : eventsEl.querySelector(".claude-sessions-system-events-header");
      header == null ? void 0 : header.setAttribute("aria-expanded", "true");
    }
  }
  /**
   * Clean up the MutationObserver for mermaid detection.
   * Called by the view on close.
   */
  destroyMermaidObserver() {
    if (this.mermaidRafId) {
      cancelAnimationFrame(this.mermaidRafId);
      this.mermaidRafId = 0;
    }
    if (this.mermaidObserver) {
      this.mermaidObserver.disconnect();
      this.mermaidObserver = null;
    }
  }
  /**
   * Get all block wrapper elements within a specific turn element.
   */
  getBlockWrappers(turnIndex) {
    const turnEl = this.turnEls[turnIndex];
    if (!turnEl) return [];
    return Array.from(turnEl.querySelectorAll(".claude-sessions-block-wrapper"));
  }
  setupMermaidObserver() {
    this.destroyMermaidObserver();
    this.mermaidObserver = new MutationObserver(() => {
      if (!this.mermaidRafId) {
        this.mermaidRafId = window.requestAnimationFrame(() => {
          this.mermaidRafId = 0;
          this.processMermaidBlocks(this.container);
        });
      }
    });
    this.mermaidObserver.observe(this.container, { childList: true, subtree: true });
  }
  processMermaidBlocks(root) {
    const els = root.querySelectorAll("div.mermaid:not(.claude-sessions-mermaid-processed)");
    for (const el of Array.from(els)) {
      const svg = el.querySelector("svg");
      if (!svg) continue;
      el.addClass("claude-sessions-mermaid-processed");
      const parent = el.parentElement;
      if (!parent) continue;
      const wrapper = parent.createDiv({ cls: "claude-sessions-mermaid-container" });
      parent.insertBefore(wrapper, el);
      wrapper.appendChild(el);
      const expandIcon = wrapper.createDiv({ cls: "claude-sessions-mermaid-expand" });
      (0, import_obsidian9.setIcon)(expandIcon, "maximize-2");
      makeClickable(wrapper, { label: "View full diagram" });
      wrapper.addEventListener("click", () => {
        new MermaidPreviewModal(this.ctx.app, svg).open();
      });
    }
  }
  renderTurn(turn) {
    var _a;
    const turnEl = this.container.createDiv({
      cls: "claude-sessions-turn",
      attr: { "data-turn-index": String(turn.index) }
    });
    const roleClass = turn.role === "user" ? "claude-sessions-turn-role-user" : "claude-sessions-turn-role-assistant";
    const header = turnEl.createDiv({ cls: "claude-sessions-turn-header" });
    header.createSpan({ cls: "claude-sessions-turn-chevron", text: "\u25B6" });
    header.createSpan({ cls: `claude-sessions-turn-role ${roleClass}`, text: turn.role === "user" ? "USER" : "CLAUDE" });
    header.createSpan({ cls: "claude-sessions-turn-label", text: `(Turn #${turn.index + 1})` });
    if (turn.timestamp) {
      const d = new Date(turn.timestamp);
      if (!isNaN(d.getTime())) {
        const dateStr = d.toDateString();
        const time = d.toLocaleTimeString(void 0, { hour: "2-digit", minute: "2-digit", hour12: false });
        let label;
        if (this.sessionStartDate && dateStr !== this.sessionStartDate) {
          const date = d.toLocaleDateString(void 0, { month: "long", day: "numeric" });
          label = `${date}, ${time}`;
        } else {
          label = time;
        }
        if (!this.sessionStartDate) this.sessionStartDate = dateStr;
        header.createSpan({ cls: "claude-sessions-turn-ts", text: label });
      }
    }
    if (turn.model) {
      const main = shortModelName(turn.model);
      const subModels = /* @__PURE__ */ new Set();
      for (const b of turn.contentBlocks) {
        if (b.type === "tool_use" && b.subAgentSession) {
          for (const t of b.subAgentSession.turns) {
            if (t.model && shortModelName(t.model) !== main) {
              subModels.add(shortModelName(t.model));
              break;
            }
          }
        }
      }
      const label = subModels.size > 0 ? `${main} \u2192 ${[...subModels].join(", ")}` : main;
      header.createSpan({ cls: "claude-sessions-turn-model", text: label });
    }
    if (turn.stopReason === "max_tokens") {
      header.createSpan({
        cls: "claude-sessions-turn-stop-reason",
        attr: { "aria-label": "Response truncated (max tokens)", "data-tooltip-position": "top" },
        text: "max tokens"
      });
    }
    if (turn.isApiError) {
      const errorLabel = turn.errorType === "rate_limit" ? "rate limit" : (_a = turn.errorType) != null ? _a : "error";
      header.createSpan({
        cls: "claude-sessions-turn-api-error",
        attr: { "aria-label": `API error: ${errorLabel}`, "data-tooltip-position": "top" },
        text: errorLabel
      });
      turnEl.addClass("claude-sessions-turn-error");
    }
    makeClickable(header, { label: `Toggle turn ${turn.index + 1}`, expanded: true });
    header.addEventListener("click", () => {
      const willCollapse = !turnEl.hasClass("collapsed");
      turnEl.toggleClass("collapsed", willCollapse);
      header.setAttribute("aria-expanded", String(!willCollapse));
    });
    const body = turnEl.createDiv({ cls: "claude-sessions-turn-body" });
    const userBlocks = turn.role === "user" ? turn.contentBlocks : [];
    const assistantBlocks = turn.role === "assistant" ? turn.contentBlocks : [];
    let blockIdx = 0;
    if (userBlocks.length > 0) {
      const userSection = body.createDiv({ cls: "claude-sessions-role-section claude-sessions-role-user" });
      for (const block of userBlocks) {
        const contentBlockIdx = blockIdx;
        const wrapper = userSection.createDiv({
          cls: "claude-sessions-block-wrapper",
          attr: {
            "data-block-idx": String(blockIdx++),
            "data-content-block-idx": String(contentBlockIdx)
          }
        });
        if (block.type === "text") {
          this.renderTextContent(block.text, wrapper, "claude-sessions-user-text");
        } else if (block.type === "slash_command") {
          this.renderSlashCommandBlock(block, wrapper);
        } else if (block.type === "bash_command") {
          this.renderBashCommandBlock(block, wrapper);
        } else if (block.type === "ansi") {
          this.renderAnsiBlock(block, wrapper);
        } else if (block.type === "image") {
          const dataUri = `data:${block.mediaType};base64,${block.data}`;
          const img = wrapper.createEl("img", {
            cls: "claude-sessions-image-thumbnail",
            attr: { src: dataUri, alt: "User attachment" }
          });
          makeClickable(img, { label: "View image attachment" });
          img.addEventListener("click", () => {
            this.openImageModal(dataUri, block.mediaType);
          });
        }
      }
    }
    if (assistantBlocks.length > 0) {
      const assistantSection = body.createDiv({ cls: "claude-sessions-role-section claude-sessions-role-assistant" });
      this.renderAssistantBlocks(assistantBlocks, assistantSection, blockIdx);
    }
    return turnEl;
  }
  renderAssistantBlocks(blocks, container, startBlockIdx = 0, groupThreshold) {
    const segments = [];
    let toolRun = [];
    let toolRunIndices = [];
    const flushTools = () => {
      if (toolRun.length > 0) {
        segments.push({ type: "tools", blocks: [...toolRun], contentBlockIndices: [...toolRunIndices] });
        toolRun = [];
        toolRunIndices = [];
      }
    };
    for (let i = 0; i < blocks.length; i++) {
      const block = blocks[i];
      if (block.type === "tool_use" || block.type === "tool_result") {
        toolRun.push(block);
        toolRunIndices.push(i);
      } else {
        flushTools();
        segments.push({ type: "single", block, contentBlockIdx: i });
      }
    }
    flushTools();
    let blockIdx = startBlockIdx;
    for (const seg of segments) {
      if (seg.type === "single") {
        const wrapper = container.createDiv({
          cls: "claude-sessions-block-wrapper",
          attr: {
            "data-block-idx": String(blockIdx++),
            "data-content-block-idx": String(seg.contentBlockIdx)
          }
        });
        this.renderSingleBlock(seg.block, wrapper);
      } else {
        const wrapper = container.createDiv({
          cls: "claude-sessions-block-wrapper",
          attr: { "data-block-idx": String(blockIdx++) }
        });
        renderToolGroup(seg.blocks, wrapper, this.ctx, this.delegate, groupThreshold, seg.contentBlockIndices);
      }
    }
  }
  renderSingleBlock(block, container) {
    switch (block.type) {
      case "text":
        this.renderTextContent(block.text, container, "claude-sessions-assistant-text");
        break;
      case "thinking":
        if (this.ctx.settings.showThinkingBlocks) {
          this.renderThinkingBlock(block.thinking, container);
        }
        break;
      case "compaction":
        this.renderCompactionBlock(block, container);
        break;
    }
  }
  renderTextContent(text, container, cls) {
    var _a;
    text = normalizeMarkdown(text);
    const lines = text.split("\n").length;
    const wrapEl = container.createDiv({ cls: "claude-sessions-text-block" });
    const trimmed = text.trim();
    const isJustCodeBlock = trimmed.startsWith("```") && trimmed.endsWith("```") && ((_a = trimmed.match(/^```/gm)) == null ? void 0 : _a.length) === 2;
    if (!isJustCodeBlock) {
      addCopyButton(wrapEl, text, "Copy to clipboard", "claude-sessions-text-copy");
    }
    if (lines > COLLAPSE_THRESHOLD) {
      wrapEl.addClass("claude-sessions-collapsible-wrap", "is-collapsed");
      const contentEl = wrapEl.createDiv({ cls: "claude-sessions-collapsible-content" });
      const mdEl = contentEl.createDiv({ cls });
      void import_obsidian9.MarkdownRenderer.render(this.ctx.app, text, mdEl, "", this.ctx.component);
      wrapEl.createDiv({ cls: "claude-sessions-collapsible-fade" });
      const toggleBtn = wrapEl.createEl("button", {
        cls: "claude-sessions-collapsible-toggle",
        text: `Show more (${lines} lines)`,
        attr: { "data-line-count": String(lines) }
      });
      makeClickable(toggleBtn, { label: `Show more (${lines} lines)`, expanded: false });
      toggleBtn.addEventListener("click", () => {
        const collapsed = wrapEl.hasClass("is-collapsed");
        wrapEl.toggleClass("is-collapsed", !collapsed);
        toggleBtn.setText(collapsed ? "Show less" : `Show more (${lines} lines)`);
        toggleBtn.setAttribute("aria-expanded", String(collapsed));
      });
    } else {
      const mdEl = wrapEl.createDiv({ cls });
      void import_obsidian9.MarkdownRenderer.render(this.ctx.app, text, mdEl, "", this.ctx.component);
    }
  }
  renderThinkingBlock(text, container) {
    const isRedacted = !text.trim();
    const el = container.createDiv({ cls: "claude-sessions-thinking-block" });
    const header = el.createDiv({ cls: "claude-sessions-thinking-header" });
    const icon = header.createSpan({ cls: "claude-sessions-thinking-icon" });
    (0, import_obsidian9.setIcon)(icon, "brain");
    header.createSpan({ cls: "claude-sessions-thinking-name", text: "Thinking" });
    if (isRedacted) {
      header.createSpan({ cls: "claude-sessions-thinking-redacted", text: "content encrypted" });
    }
    header.createSpan({ cls: "claude-sessions-thinking-chevron", text: "\u25B6" });
    const body = el.createDiv({ cls: "claude-sessions-thinking-body" });
    if (isRedacted) {
      body.createDiv({ cls: "claude-sessions-thinking-redacted-body", text: "Thinking content is not available \u2014 encrypted by Claude Code." });
    } else {
      void import_obsidian9.MarkdownRenderer.render(this.ctx.app, text, body, "", this.ctx.component);
    }
    makeClickable(header, { label: "Toggle thinking block", expanded: false });
    header.addEventListener("click", () => {
      const willOpen = !el.hasClass("open");
      el.toggleClass("open", willOpen);
      header.setAttribute("aria-expanded", String(willOpen));
    });
  }
  renderSlashCommandBlock(block, container) {
    const el = container.createDiv({ cls: "claude-sessions-slash-command-block" });
    const header = el.createDiv({ cls: "claude-sessions-slash-command-header" });
    const icon = header.createSpan({ cls: "claude-sessions-slash-command-icon" });
    (0, import_obsidian9.setIcon)(icon, "file-text");
    header.createSpan({ cls: "claude-sessions-slash-command-name", text: "Slash output" });
    addCopyButton(header, block.text, "Copy slash command output");
    header.createSpan({ cls: "claude-sessions-slash-command-chevron", text: "\u25B6" });
    const body = el.createDiv({ cls: "claude-sessions-slash-command-body" });
    void import_obsidian9.MarkdownRenderer.render(this.ctx.app, block.text, body, "", this.ctx.component);
    makeClickable(header, { label: "Toggle slash command output", expanded: false });
    header.addEventListener("click", () => {
      const willOpen = !el.hasClass("open");
      el.toggleClass("open", willOpen);
      header.setAttribute("aria-expanded", String(willOpen));
    });
  }
  renderBashCommandBlock(block, container) {
    const el = container.createDiv({ cls: "claude-sessions-bash-command-block" });
    const commandEl = el.createDiv({ cls: "claude-sessions-bash-command-input" });
    commandEl.createDiv({ cls: "claude-sessions-tool-section-label", text: "INPUT" });
    void import_obsidian9.MarkdownRenderer.render(this.ctx.app, fence(block.command, "bash"), commandEl, "", this.ctx.component);
    const resultEl = el.createDiv({ cls: "claude-sessions-bash-command-stdout" });
    resultEl.createDiv({ cls: "claude-sessions-tool-section-label", text: "RESULT" });
    if (block.stdout.trim()) {
      void import_obsidian9.MarkdownRenderer.render(this.ctx.app, fence(block.stdout), resultEl, "", this.ctx.component);
    }
    if (block.stderr.trim()) {
      const stderrEl = el.createDiv({ cls: "claude-sessions-bash-command-stderr" });
      stderrEl.createDiv({ cls: "claude-sessions-tool-section-label", text: "STDERR" });
      void import_obsidian9.MarkdownRenderer.render(this.ctx.app, fence(block.stderr), stderrEl, "", this.ctx.component);
    }
  }
  buildAnsiDom(text, parent) {
    const stack = [];
    let current = parent;
    const closeAll = () => {
      while (stack.length) {
        stack.pop();
        current = stack.length ? stack[stack.length - 1].el : parent;
      }
    };
    const closeTo = (tag) => {
      const idx = stack.map((s) => s.tag).lastIndexOf(tag);
      if (idx === -1) return;
      while (stack.length > idx) {
        stack.pop();
        current = stack.length ? stack[stack.length - 1].el : parent;
      }
    };
    const re = new RegExp(ANSI_PARSE_RE.source, "g");
    let last = 0;
    let match;
    while ((match = re.exec(text)) !== null) {
      if (match.index > last) {
        current.appendText(text.slice(last, match.index));
      }
      last = re.lastIndex;
      const params = match[1].split(";").map(Number);
      let i = 0;
      while (i < params.length) {
        const code = params[i];
        if (code === 0 || isNaN(code) && match[1] === "") {
          closeAll();
        } else if (code === 1) {
          const span = current.createSpan({ cls: "ansi-bold" });
          stack.push({ tag: "bold", el: span });
          current = span;
        } else if (code === 2) {
          const span = current.createSpan({ cls: "ansi-dim" });
          stack.push({ tag: "dim", el: span });
          current = span;
        } else if (code === 3) {
          const span = current.createSpan({ cls: "ansi-italic" });
          stack.push({ tag: "italic", el: span });
          current = span;
        } else if (code === 4) {
          const span = current.createSpan({ cls: "ansi-underline" });
          stack.push({ tag: "underline", el: span });
          current = span;
        } else if (code === 22) {
          closeTo("bold");
          closeTo("dim");
        } else if (code === 23) {
          closeTo("italic");
        } else if (code === 24) {
          closeTo("underline");
        } else if (code >= 30 && code <= 37 || code >= 90 && code <= 97) {
          closeTo("fg");
          const idx = code >= 90 ? code - 90 + 8 : code - 30;
          const span = current.createSpan({ cls: "ansi-fg" });
          span.style.color = _TimelineRenderer.ANSI_COLORS[idx];
          stack.push({ tag: "fg", el: span });
          current = span;
        } else if (code === 38 && params[i + 1] === 2 && i + 4 < params.length) {
          closeTo("fg");
          const r = params[i + 2], g = params[i + 3], b = params[i + 4];
          const span = current.createSpan({ cls: "ansi-fg" });
          span.style.color = `rgb(${r},${g},${b})`;
          stack.push({ tag: "fg", el: span });
          current = span;
          i += 4;
        } else if (code === 39) {
          closeTo("fg");
        } else if (code >= 40 && code <= 47 || code >= 100 && code <= 107) {
          closeTo("bg");
          const idx = code >= 100 ? code - 100 + 8 : code - 40;
          const span = current.createSpan({ cls: "ansi-bg" });
          span.style.backgroundColor = _TimelineRenderer.ANSI_COLORS[idx];
          stack.push({ tag: "bg", el: span });
          current = span;
        } else if (code === 48 && params[i + 1] === 2 && i + 4 < params.length) {
          closeTo("bg");
          const r = params[i + 2], g = params[i + 3], b = params[i + 4];
          const span = current.createSpan({ cls: "ansi-bg" });
          span.style.backgroundColor = `rgb(${r},${g},${b})`;
          stack.push({ tag: "bg", el: span });
          current = span;
          i += 4;
        } else if (code === 49) {
          closeTo("bg");
        }
        i++;
      }
    }
    if (last < text.length) {
      current.appendText(text.slice(last));
    }
    closeAll();
  }
  renderAnsiBlock(block, container) {
    const pre = container.createEl("pre", { cls: "claude-sessions-ansi-block" });
    this.buildAnsiDom(block.text, pre);
  }
  renderCompactionBlock(block, container) {
    const el = container.createDiv({ cls: "claude-sessions-compaction-block" });
    const divider = el.createDiv({ cls: "claude-sessions-compaction-divider" });
    const icon = divider.createSpan({ cls: "claude-sessions-compaction-icon" });
    (0, import_obsidian9.setIcon)(icon, "scissors");
    divider.createSpan({ text: "Context compacted" });
    if (block.preTokens) {
      const fmt = (n) => n >= 1e3 ? `${(n / 1e3).toFixed(1)}k` : String(n);
      divider.createSpan({ cls: "claude-sessions-compaction-tokens", text: fmt(block.preTokens) });
    }
    if (block.summary) {
      const header = el.createDiv({ cls: "claude-sessions-compaction-summary-header" });
      header.createSpan({ cls: "claude-sessions-compaction-summary-chevron", text: "\u25B6" });
      header.createSpan({ text: "Continuation summary" });
      addCopyButton(header, block.summary, "Copy continuation summary");
      const summaryEl = el.createDiv({ cls: "claude-sessions-compaction-summary" });
      void import_obsidian9.MarkdownRenderer.render(this.ctx.app, block.summary, summaryEl, "", this.ctx.component);
      makeClickable(header, { label: "Toggle continuation summary", expanded: false });
      header.addEventListener("click", () => {
        const willOpen = !el.hasClass("open");
        el.toggleClass("open", willOpen);
        header.setAttribute("aria-expanded", String(willOpen));
      });
    }
  }
  openImageModal(dataUri, mediaType) {
    const modal = new ImagePreviewModal(this.ctx.app, dataUri, mediaType);
    modal.open();
  }
};
/**
 * Build ANSI-styled DOM nodes directly into a parent element.
 */
// Standard 4-bit ANSI color palette (indices 0-7 for codes 30-37/40-47, 8-15 for 90-97/100-107)
_TimelineRenderer.ANSI_COLORS = [
  "#555555",
  "#ff5555",
  "#55ff55",
  "#ffff55",
  "#5555ff",
  "#ff55ff",
  "#55ffff",
  "#aaaaaa",
  // standard (dimmed for dark bg)
  "#888888",
  "#ff5555",
  "#55ff55",
  "#ffff55",
  "#5555ff",
  "#ff55ff",
  "#55ffff",
  "#ffffff"
  // bright
];
var TimelineRenderer = _TimelineRenderer;
var ImagePreviewModal = class extends import_obsidian9.Modal {
  constructor(app, dataUri, mediaType) {
    super(app);
    this.dataUri = dataUri;
    this.mediaType = SAFE_IMAGE_TYPES.has(mediaType) ? mediaType : "image/png";
  }
  onOpen() {
    const { contentEl } = this;
    this.modalEl.addClass("claude-sessions-image-preview-modal");
    contentEl.empty();
    const scrollWrap = contentEl.createDiv({ cls: "claude-sessions-image-preview-scroll" });
    scrollWrap.createEl("img", {
      cls: "claude-sessions-image-preview",
      attr: { src: this.dataUri, alt: "Image attachment" }
    });
    const actions = contentEl.createDiv({ cls: "claude-sessions-image-preview-actions" });
    const downloadBtn = actions.createEl("button", {
      cls: "mod-cta",
      text: "Download",
      attr: { "aria-label": "Download image" }
    });
    downloadBtn.addEventListener("click", () => {
      const ext = this.mediaType.split("/")[1] || "png";
      const a = contentEl.createEl("a", {
        attr: { href: this.dataUri, download: `attachment.${ext}` }
      });
      a.click();
      a.remove();
    });
    const copyBtn = actions.createEl("button", {
      cls: "mod-cta",
      text: "Copy",
      attr: { "aria-label": "Copy image to clipboard" }
    });
    copyBtn.addEventListener("click", () => {
      void (async () => {
        const parts = this.dataUri.split(",");
        const byteString = atob(parts[1]);
        const bytes = new Uint8Array(byteString.length);
        for (let i = 0; i < byteString.length; i++) {
          bytes[i] = byteString.charCodeAt(i);
        }
        const blob = new Blob([bytes], { type: this.mediaType });
        await navigator.clipboard.write([
          new ClipboardItem({ [this.mediaType]: blob })
        ]);
        copyBtn.setText("Copied!");
        window.setTimeout(() => copyBtn.setText("Copy"), 1500);
      })();
    });
  }
  onClose() {
    this.contentEl.empty();
  }
};
var MermaidPreviewModal = class extends import_obsidian9.Modal {
  constructor(app, svgEl) {
    super(app);
    this.svgEl = svgEl;
  }
  onOpen() {
    const { contentEl } = this;
    this.modalEl.addClass("claude-sessions-mermaid-preview-modal");
    contentEl.empty();
    const scrollWrap = contentEl.createDiv({ cls: "claude-sessions-mermaid-preview-scroll markdown-rendered" });
    const origId = this.svgEl.id;
    let svgString = new XMLSerializer().serializeToString(this.svgEl);
    if (origId) {
      svgString = svgString.split(origId).join("mermaid-preview-" + Date.now());
    }
    const doc = new DOMParser().parseFromString(svgString, "image/svg+xml");
    const svgNode = activeDocument.importNode(doc.documentElement, true);
    svgNode.style.removeProperty("max-width");
    const mermaidWrap = scrollWrap.createDiv({ cls: "mermaid" });
    mermaidWrap.appendChild(svgNode);
    const actions = contentEl.createDiv({ cls: "claude-sessions-mermaid-preview-actions" });
    const downloadBtn = actions.createEl("button", {
      cls: "mod-cta",
      text: "Download SVG",
      attr: { "aria-label": "Download as SVG file" }
    });
    downloadBtn.addEventListener("click", () => {
      const svgString2 = new XMLSerializer().serializeToString(this.svgEl);
      const blob = new Blob([svgString2], { type: "image/svg+xml" });
      const url = URL.createObjectURL(blob);
      const a = contentEl.createEl("a", {
        attr: { href: url, download: "diagram.svg" }
      });
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
    });
    const copyBtn = actions.createEl("button", {
      cls: "mod-cta",
      text: "Copy SVG",
      attr: { "aria-label": "Copy SVG to clipboard" }
    });
    copyBtn.addEventListener("click", () => {
      void (async () => {
        const svgString2 = new XMLSerializer().serializeToString(this.svgEl);
        await navigator.clipboard.writeText(svgString2);
        copyBtn.setText("Copied!");
        window.setTimeout(() => copyBtn.setText("Copy SVG"), 1500);
      })();
    });
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/views/timeline-view.ts
init_streaming_reader();
init_detect();
init_claude_subagent();
init_constants();
var VIEW_TYPE_TIMELINE = "claude-sessions-timeline";
var TimelineView = class extends import_obsidian11.ItemView {
  constructor(leaf, settings) {
    super(leaf);
    this.session = null;
    this.renderer = null;
    this.controlsEl = null;
    this.timelineEl = null;
    this.statusEl = null;
    this.progressFill = null;
    this.progressBar = null;
    this.progressTooltip = null;
    this.observer = null;
    this.activeTurnIndex = 0;
    // Timing data
    this.sessionStartMs = 0;
    this.sessionTotalMs = 0;
    this.turnStartMs = [];
    this.turnEndMs = [];
    this.displayedTimeMs = 0;
    // File watcher
    this.watcher = null;
    this.debounceTimer = null;
    this.isWatching = false;
    this.isFollowing = true;
    this.watchBtn = null;
    // Search highlight
    this.activeHighlights = [];
    this.highlightTimer = null;
    // Pending tool notification dedup
    this.lastNotifiedToolId = null;
    // Content filters
    this.filters = {
      user: true,
      assistant: true,
      userText: true,
      userImages: true,
      assistantText: true,
      thinking: true,
      toolCalls: true,
      toolResults: true
    };
    this.settings = settings;
  }
  getViewType() {
    return VIEW_TYPE_TIMELINE;
  }
  getDisplayText() {
    var _a;
    const meta = (_a = this.session) == null ? void 0 : _a.metadata;
    return (meta == null ? void 0 : meta.customTitle) || (meta == null ? void 0 : meta.project) || "Claude sessions";
  }
  getIcon() {
    return "claude-sparkle";
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("claude-sessions-timeline-container");
    this.timelineEl = contentEl.createDiv({ cls: "claude-sessions-timeline markdown-rendered" });
    this.renderer = new TimelineRenderer(this.timelineEl, this.app, this, this.settings);
    this.applyMaxWidth();
    this.controlsEl = contentEl.createDiv({ cls: "claude-sessions-controls" });
    this.buildControls(this.controlsEl);
    if (!this.session) {
      this.timelineEl.createDiv({
        cls: "claude-sessions-empty",
        text: 'No session loaded. Use "Browse sessions" or "Import session file" to load one.'
      });
    }
    return Promise.resolve();
  }
  onClose() {
    var _a;
    this.clearHighlight();
    this.stopWatching();
    this.destroyObserver();
    (_a = this.renderer) == null ? void 0 : _a.destroyMermaidObserver();
    return Promise.resolve();
  }
  getState() {
    var _a;
    return {
      sessionPath: (_a = this.session) == null ? void 0 : _a.rawPath,
      turnIndex: this.activeTurnIndex
    };
  }
  async setState(state) {
    if (state.sessionPath && !this.session) {
      try {
        const content = await readFileContent(state.sessionPath);
        const parser = detectParser(content);
        if (parser) {
          const session = parser.parse(content, state.sessionPath);
          await resolveSubAgentSessions(session, readFileContent, listDirectoryFiles);
          this.loadSession(session);
        }
      } catch (e) {
      }
    }
    if (state.turnIndex !== void 0 && this.session) {
      this.scrollToTurn(state.turnIndex);
    }
  }
  loadSession(session, opts) {
    var _a, _b, _c;
    const savedIndex = this.activeTurnIndex;
    this.session = session;
    if ((opts == null ? void 0 : opts.scrollToEnd) && session.turns.length > 0) {
      this.activeTurnIndex = session.turns.length - 1;
    } else {
      this.activeTurnIndex = Math.min(savedIndex, session.turns.length - 1);
    }
    if (this.renderer) {
      this.renderer.updateSettings(this.settings);
    }
    this.computeTiming();
    (_b = (_a = this.leaf).updateHeader) == null ? void 0 : _b.call(_a);
    const titleEl = (_c = this.containerEl.parentElement) == null ? void 0 : _c.querySelector(".view-header-title");
    if (titleEl) titleEl.textContent = this.getDisplayText();
    this.renderFullTimeline();
    this.renderTurnDots();
    this.syncTimer();
    this.updateControls();
    if ((opts == null ? void 0 : opts.scrollToEnd) && this.timelineEl) {
      window.requestAnimationFrame(() => {
        if (this.timelineEl) {
          this.timelineEl.scrollTop = this.timelineEl.scrollHeight;
        }
      });
    }
  }
  updateSettings(settings) {
    this.settings = settings;
    this.applyMaxWidth();
    if (this.renderer) {
      this.renderer.updateSettings(settings);
      if (this.session) {
        const savedIndex = this.activeTurnIndex;
        this.renderFullTimeline();
        this.renderTurnDots();
        this.scrollToTurn(savedIndex);
        this.updateControls();
      }
    }
  }
  /** Update the --max-content-width CSS variable without re-rendering. */
  applyMaxWidth() {
    const w = this.settings.maxContentWidth;
    this.contentEl.style.setProperty("--max-content-width", w > 0 ? `${w}px` : "none");
  }
  getSession() {
    return this.session;
  }
  getTimelineEl() {
    return this.timelineEl;
  }
  async reloadSession() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m;
    const filePath = (_a = this.session) == null ? void 0 : _a.rawPath;
    if (!filePath) {
      new import_obsidian11.Notice("No session file path to reload from.");
      return;
    }
    try {
      const content = await readFileContent(filePath);
      const parser = detectParser(content);
      if (!parser) {
        new import_obsidian11.Notice("Could not detect session format on reload.");
        return;
      }
      const session = parser.parse(content, filePath);
      await resolveSubAgentSessions(session, readFileContent, listDirectoryFiles);
      const prevCount = (_c = (_b = this.session) == null ? void 0 : _b.turns.length) != null ? _c : 0;
      const newCount = session.turns.length;
      const pendingBgAgents = /* @__PURE__ */ new Set();
      if (this.session) {
        for (const turn of this.session.turns) {
          for (const block of turn.contentBlocks) {
            if (block.type === BT_TOOL_USE && SUBAGENT_TOOL_NAMES.has(block.name) && ((_d = block.subAgentSession) == null ? void 0 : _d.isBackground) && !block.subAgentSession.durationMs) {
              pendingBgAgents.add(block.id);
            }
          }
        }
      }
      if (this.renderer && this.observer && prevCount > 0 && newCount >= prevCount) {
        this.renderer.updateHookEvents(session);
        if (pendingBgAgents.size > 0) {
          for (let ti = 0; ti < prevCount - 1; ti++) {
            const turn = session.turns[ti];
            const hasCompleted = turn.contentBlocks.some((b) => {
              var _a2;
              return b.type === BT_TOOL_USE && pendingBgAgents.has(b.id) && ((_a2 = b.subAgentSession) == null ? void 0 : _a2.durationMs);
            });
            if (hasCompleted) {
              const oldEl = this.renderer.getTurnElements()[ti];
              const state = oldEl ? this.captureTurnState(oldEl) : null;
              const newEl = this.renderer.refreshTurnAt(ti, turn);
              if (state && newEl) this.restoreTurnState(newEl, state);
              if (newEl) this.observer.observe(newEl);
            }
          }
        }
        const lastTurnEl = this.renderer.getTurnElements()[prevCount - 1];
        const lastTurnState = lastTurnEl ? this.captureTurnState(lastTurnEl) : null;
        this.renderer.refreshLastTurn(session.turns[prevCount - 1]);
        if (lastTurnState) {
          const newEl = this.renderer.getTurnElements()[prevCount - 1];
          this.restoreTurnState(newEl, lastTurnState);
        }
        this.observer.observe(this.renderer.getTurnElements()[prevCount - 1]);
        if (newCount > prevCount) {
          const appended = this.renderer.appendTurns(session.turns.slice(prevCount));
          for (const el of appended) {
            this.observer.observe(el);
          }
          for (const el of appended) {
            this.applyFilters(el);
          }
        }
        const oldTitle = ((_f = (_e = this.session) == null ? void 0 : _e.metadata) == null ? void 0 : _f.customTitle) || ((_h = (_g = this.session) == null ? void 0 : _g.metadata) == null ? void 0 : _h.project);
        this.session = session;
        this.computeTiming();
        if (newCount > prevCount) {
          this.activeTurnIndex = session.turns.length - 1;
        }
        this.renderer.refreshSummary(session);
        this.renderTurnDots();
        this.updateControls();
        const newTitle = ((_i = session.metadata) == null ? void 0 : _i.customTitle) || ((_j = session.metadata) == null ? void 0 : _j.project);
        if (newTitle !== oldTitle) {
          (_l = (_k = this.leaf).updateHeader) == null ? void 0 : _l.call(_k);
          const titleEl = (_m = this.containerEl.parentElement) == null ? void 0 : _m.querySelector(".view-header-title");
          if (titleEl) titleEl.textContent = this.getDisplayText();
        }
        if (this.settings.autoScrollOnUpdate && this.timelineEl) {
          window.requestAnimationFrame(() => {
            if (this.timelineEl) {
              this.timelineEl.scrollTop = this.timelineEl.scrollHeight;
            }
          });
        }
        this.checkPendingToolNotification(session);
        return;
      }
      this.loadSession(session, { scrollToEnd: this.settings.autoScrollOnUpdate });
      this.checkPendingToolNotification(session);
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      new import_obsidian11.Notice(`Failed to reload session: ${msg}`);
      this.stopWatching();
    }
  }
  checkPendingToolNotification(session) {
    if (!this.isWatching || !this.settings.notifyOnPendingTool) return;
    const turns = session.turns;
    if (turns.length === 0) return;
    const lastTurn = turns[turns.length - 1];
    if (lastTurn.role !== "assistant") {
      this.lastNotifiedToolId = null;
      return;
    }
    const pendingTool = lastTurn.contentBlocks.find(
      (b) => b.type === "tool_use" && b.isPending
    );
    if (!pendingTool || pendingTool.type !== "tool_use") {
      this.lastNotifiedToolId = null;
      return;
    }
    if (pendingTool.id === this.lastNotifiedToolId) return;
    this.lastNotifiedToolId = pendingTool.id;
    const displayName = session.metadata.customTitle || session.metadata.project || "Claude sessions";
    const toolName = pendingTool.name;
    const title = `\u2726 ${displayName}`;
    const body = `"${toolName}" is waiting for permission`;
    new import_obsidian11.Notice(`${title}: ${body}`, 8e3);
    const icon = "data:image/svg+xml;base64,";
    const sendNotification = () => {
      const n = new Notification(title, {
        body,
        icon,
        tag: "claude-sessions-pending",
        requireInteraction: true
      });
      n.onclick = () => {
        window.focus();
        n.close();
      };
    };
    if ("Notification" in window && Notification.permission === "granted") {
      sendNotification();
    } else if ("Notification" in window && Notification.permission !== "denied") {
      void Notification.requestPermission().then((perm) => {
        if (perm === "granted") sendNotification();
      });
    }
  }
  toggleWatch() {
    if (this.isWatching) {
      this.stopWatching();
    } else {
      this.startWatching();
    }
  }
  stopWatching() {
    if (this.debounceTimer !== null) {
      window.clearTimeout(this.debounceTimer);
      this.debounceTimer = null;
    }
    if (this.watcher) {
      this.watcher.close();
      this.watcher = null;
    }
    this.isWatching = false;
    this.updateWatchIndicator();
  }
  startWatching() {
    var _a;
    const filePath = (_a = this.session) == null ? void 0 : _a.rawPath;
    if (!filePath) {
      new import_obsidian11.Notice("No session file path to watch.");
      return;
    }
    const onChange = () => {
      if (this.debounceTimer !== null) {
        window.clearTimeout(this.debounceTimer);
      }
      this.debounceTimer = window.setTimeout(() => {
        this.debounceTimer = null;
        void this.reloadSession();
      }, 1500);
    };
    try {
      this.watcher = fs3.watch(filePath, { persistent: false }, (eventType) => {
        if (eventType === "change") onChange();
      });
      this.watcher.on("error", () => {
        new import_obsidian11.Notice("Session file watcher error \u2014 stopping.");
        this.stopWatching();
      });
    } catch (e) {
      try {
        fs3.watchFile(filePath, { interval: 2e3, persistent: false }, (curr, prev) => {
          if (curr.mtimeMs !== prev.mtimeMs) onChange();
        });
        this.watcher = {
          close: () => fs3.unwatchFile(filePath),
          on: () => this
        };
      } catch (e2) {
        new import_obsidian11.Notice("Could not watch session file.");
        return;
      }
    }
    this.isWatching = true;
    this.updateWatchIndicator();
    void this.reloadSession();
  }
  updateWatchIndicator() {
    if (!this.watchBtn) return;
    if (this.isWatching) {
      this.watchBtn.addClass("claude-sessions-watch-active");
      this.watchBtn.setAttribute("aria-label", "Stop live watch");
    } else {
      this.watchBtn.removeClass("claude-sessions-watch-active");
      this.watchBtn.setAttribute("aria-label", "Start live watch");
    }
  }
  getSessionStartMs() {
    return this.sessionStartMs;
  }
  // Navigation — scroll to a specific turn
  scrollToTurn(index) {
    var _a;
    if (!this.session) return;
    const target = Math.max(0, Math.min(index, this.session.turns.length - 1));
    const turnEls = ((_a = this.renderer) == null ? void 0 : _a.getTurnElements()) || [];
    const el = turnEls[target];
    if (el) {
      el.scrollIntoView({ behavior: "smooth", block: "start" });
    }
    this.activeTurnIndex = target;
    this.syncTimer();
    this.updateControls();
  }
  expandAll() {
    if (!this.timelineEl) return;
    for (const el of Array.from(this.timelineEl.querySelectorAll(".claude-sessions-turn.collapsed"))) {
      el.removeClass("collapsed");
      const h = el.querySelector(".claude-sessions-turn-header");
      if (h) h.setAttribute("aria-expanded", "true");
    }
  }
  collapseAll() {
    if (!this.timelineEl) return;
    for (const el of Array.from(this.timelineEl.querySelectorAll(".claude-sessions-turn:not(.collapsed)"))) {
      el.addClass("collapsed");
      const h = el.querySelector(".claude-sessions-turn-header");
      if (h) h.setAttribute("aria-expanded", "false");
    }
  }
  /** Expand all collapsible blocks: tools, thinking, summary, sub-agents, slash commands, compaction */
  expandAllBlocks() {
    if (!this.timelineEl) return;
    const selectors = [
      { block: ".claude-sessions-tool-block:not(.open)", header: ".claude-sessions-tool-header" },
      { block: ".claude-sessions-tool-group:not(.open)", header: ".claude-sessions-tool-group-header" },
      { block: ".claude-sessions-thinking-block:not(.open)", header: ".claude-sessions-thinking-header" },
      { block: ".claude-sessions-subagent-prompt:not(.open)", header: ".claude-sessions-subagent-prompt-header" },
      { block: ".claude-sessions-slash-command-block:not(.open)", header: ".claude-sessions-slash-command-header" },
      { block: ".claude-sessions-compaction-block:not(.open)", header: ".claude-sessions-compaction-summary-header" },
      { block: ".claude-sessions-summary:not(.open)", header: ".claude-sessions-summary-header" }
    ];
    for (const { block, header } of selectors) {
      for (const el of Array.from(this.timelineEl.querySelectorAll(block))) {
        el.addClass("open");
        const h = el.querySelector(header);
        if (h) h.setAttribute("aria-expanded", "true");
      }
    }
    for (const wrap of Array.from(this.timelineEl.querySelectorAll(".claude-sessions-collapsible-wrap.is-collapsed"))) {
      wrap.removeClass("is-collapsed");
      const btn = wrap.querySelector(".claude-sessions-collapsible-toggle");
      if (btn) {
        btn.setText("Show less");
        btn.setAttribute("aria-expanded", "true");
      }
    }
  }
  /** Collapse all collapsible blocks: tools, thinking, summary, sub-agents, slash commands, compaction, text */
  collapseAllBlocks() {
    var _a;
    if (!this.timelineEl) return;
    const selectors = [
      { block: ".claude-sessions-tool-block.open", header: ".claude-sessions-tool-header" },
      { block: ".claude-sessions-tool-group.open", header: ".claude-sessions-tool-group-header" },
      { block: ".claude-sessions-thinking-block.open", header: ".claude-sessions-thinking-header" },
      { block: ".claude-sessions-subagent-prompt.open", header: ".claude-sessions-subagent-prompt-header" },
      { block: ".claude-sessions-slash-command-block.open", header: ".claude-sessions-slash-command-header" },
      { block: ".claude-sessions-compaction-block.open", header: ".claude-sessions-compaction-summary-header" },
      { block: ".claude-sessions-summary.open", header: ".claude-sessions-summary-header" }
    ];
    for (const { block, header } of selectors) {
      for (const el of Array.from(this.timelineEl.querySelectorAll(block))) {
        el.removeClass("open");
        const h = el.querySelector(header);
        if (h) h.setAttribute("aria-expanded", "false");
      }
    }
    for (const wrap of Array.from(this.timelineEl.querySelectorAll(".claude-sessions-collapsible-wrap:not(.is-collapsed)"))) {
      wrap.addClass("is-collapsed");
      const btn = wrap.querySelector(".claude-sessions-collapsible-toggle");
      if (btn) {
        const lineCount = (_a = btn.getAttribute("data-line-count")) != null ? _a : "";
        btn.setText(lineCount ? `Show more (${lineCount} lines)` : "Show more");
        btn.setAttribute("aria-expanded", "false");
      }
    }
  }
  // ── In-session search ──
  async openInSessionSearch() {
    var _a;
    if (!((_a = this.session) == null ? void 0 : _a.rawPath)) return;
    const SEARCH_TYPE = "claude-sessions-search";
    const existing = this.app.workspace.getLeavesOfType(SEARCH_TYPE);
    let leaf;
    if (existing.length > 0) {
      leaf = existing[0];
    } else {
      const right = this.app.workspace.getRightLeaf(false);
      if (!right) return;
      leaf = right;
      await leaf.setViewState({ type: SEARCH_TYPE, active: true });
    }
    void this.app.workspace.revealLeaf(leaf);
    const view = leaf.view;
    if (view && "setMode" in view) {
      view.setMode("in-session");
    }
  }
  navigateToMatch(turnIndex, contentBlockIndex, needle, occurrenceInBlock) {
    this.clearHighlight();
    this.scrollToTurn(turnIndex);
    window.requestAnimationFrame(() => {
      window.requestAnimationFrame(() => {
        this.highlightMatchInBlock(turnIndex, contentBlockIndex, needle, occurrenceInBlock);
      });
    });
  }
  highlightMatchInBlock(turnIndex, contentBlockIndex, needle, occurrenceInBlock) {
    var _a, _b;
    const turnEls = ((_a = this.renderer) == null ? void 0 : _a.getTurnElements()) || [];
    const turnEl = turnEls[turnIndex];
    if (!turnEl || !needle) return;
    if (turnEl.hasClass("collapsed")) {
      turnEl.removeClass("collapsed");
      const header = turnEl.querySelector(".claude-sessions-turn-header");
      if (header) header.setAttribute("aria-expanded", "true");
    }
    const blockEl = turnEl.querySelector(
      `[data-content-block-idx="${contentBlockIndex}"]`
    );
    if (!blockEl) return;
    this.expandAncestors(blockEl, turnEl);
    const textNodes = [];
    const walker = activeDocument.createTreeWalker(blockEl, NodeFilter.SHOW_TEXT);
    let n;
    while (n = walker.nextNode()) textNodes.push(n);
    const combined = textNodes.map((t) => t.textContent || "").join("");
    const lowerCombined = combined.toLowerCase();
    const lowerNeedle = needle.toLowerCase();
    const occurrences = [];
    let from = 0;
    while (true) {
      const idx = lowerCombined.indexOf(lowerNeedle, from);
      if (idx === -1) break;
      occurrences.push(idx);
      from = idx + 1;
    }
    if (occurrences.length === 0) return;
    const targetIdx = Math.min(occurrenceInBlock, occurrences.length - 1);
    const matchStart = occurrences[targetIdx];
    const matchEnd = matchStart + needle.length;
    const marks = [];
    let consumed = 0;
    for (const node of textNodes) {
      const text = node.textContent || "";
      const nodeStart = consumed;
      const nodeEnd = consumed + text.length;
      consumed = nodeEnd;
      if (nodeEnd <= matchStart) continue;
      if (nodeStart >= matchEnd) break;
      const localStart = Math.max(0, matchStart - nodeStart);
      const localEnd = Math.min(text.length, matchEnd - nodeStart);
      if (localEnd <= localStart) continue;
      const middle = localStart > 0 ? node.splitText(localStart) : node;
      middle.splitText(localEnd - localStart);
      const mark = createEl("mark", { cls: "claude-sessions-search-highlight" });
      (_b = middle.parentNode) == null ? void 0 : _b.replaceChild(mark, middle);
      mark.appendText(middle.textContent || "");
      marks.push(mark);
    }
    if (marks.length === 0) return;
    this.expandAncestors(marks[0], turnEl);
    this.activeHighlights = marks;
    marks[0].scrollIntoView({ behavior: "smooth", block: "center" });
    this.highlightTimer = window.setTimeout(() => this.clearHighlight(), 5e3);
  }
  expandAncestors(node, boundary) {
    let el = node.parentElement;
    while (el && el !== boundary) {
      if (el.hasClass("claude-sessions-tool-block") && !el.hasClass("open")) {
        el.addClass("open");
        const h = el.querySelector(".claude-sessions-tool-header");
        if (h) h.setAttribute("aria-expanded", "true");
      }
      if (el.hasClass("claude-sessions-tool-group") && !el.hasClass("open")) {
        el.addClass("open");
        const h = el.querySelector(".claude-sessions-tool-group-header");
        if (h) h.setAttribute("aria-expanded", "true");
      }
      if (el.hasClass("claude-sessions-thinking-block") && !el.hasClass("open")) {
        el.addClass("open");
        const h = el.querySelector(".claude-sessions-thinking-header");
        if (h) h.setAttribute("aria-expanded", "true");
      }
      if (el.hasClass("claude-sessions-collapsible-wrap") && el.hasClass("is-collapsed")) {
        el.removeClass("is-collapsed");
        const btn = el.querySelector(".claude-sessions-collapsible-toggle");
        if (btn) btn.setAttribute("aria-expanded", "true");
      }
      if (el.hasClass("claude-sessions-subagent-prompt") && !el.hasClass("open")) {
        el.addClass("open");
        const h = el.querySelector(".claude-sessions-subagent-prompt-header");
        if (h) h.setAttribute("aria-expanded", "true");
      }
      if (el.hasClass("claude-sessions-slash-command-block") && !el.hasClass("open")) {
        el.addClass("open");
        const h = el.querySelector(".claude-sessions-slash-command-header");
        if (h) h.setAttribute("aria-expanded", "true");
      }
      if (el.hasClass("claude-sessions-compaction-block") && !el.hasClass("open")) {
        el.addClass("open");
        const h = el.querySelector(".claude-sessions-compaction-summary-header");
        if (h) h.setAttribute("aria-expanded", "true");
      }
      if (el.hasClass("claude-sessions-read-md-hidden")) {
        el.removeClass("claude-sessions-read-md-hidden");
        const parent = el.parentElement;
        if (parent) {
          const sibling = el.hasClass("claude-sessions-read-md-code") ? parent.querySelector(".claude-sessions-read-md-preview") : parent.querySelector(".claude-sessions-read-md-code");
          if (sibling) sibling.addClass("claude-sessions-read-md-hidden");
        }
      }
      el = el.parentElement;
    }
  }
  clearHighlight() {
    for (const mark of this.activeHighlights) {
      const parent = mark.parentNode;
      if (!parent) continue;
      const text = mark.textContent || "";
      parent.replaceChild(activeDocument.createTextNode(text), mark);
      parent.normalize();
    }
    this.activeHighlights = [];
    if (this.highlightTimer !== null) {
      window.clearTimeout(this.highlightTimer);
      this.highlightTimer = null;
    }
  }
  // Timing computation
  computeTiming() {
    this.turnStartMs = [];
    this.turnEndMs = [];
    this.sessionStartMs = 0;
    this.sessionTotalMs = 0;
    this.displayedTimeMs = 0;
    if (!this.session || this.session.turns.length === 0) return;
    const startStr = this.session.metadata.startTime || this.session.turns[0].timestamp;
    if (!startStr) return;
    const startMs = new Date(startStr).getTime();
    if (isNaN(startMs)) return;
    this.sessionStartMs = startMs;
    let hasAnyTimestamp = false;
    for (let i = 0; i < this.session.turns.length; i++) {
      const turn = this.session.turns[i];
      const tsMs = turn.timestamp ? new Date(turn.timestamp).getTime() : NaN;
      const endMs = turn.endTimestamp ? new Date(turn.endTimestamp).getTime() : tsMs;
      if (!isNaN(tsMs)) {
        this.turnStartMs[i] = tsMs - this.sessionStartMs;
        this.turnEndMs[i] = (!isNaN(endMs) ? endMs : tsMs) - this.sessionStartMs;
        hasAnyTimestamp = true;
      } else {
        this.turnStartMs[i] = 0;
        this.turnEndMs[i] = 0;
      }
    }
    if (!hasAnyTimestamp) {
      this.turnStartMs = [];
      this.turnEndMs = [];
      this.sessionStartMs = 0;
      return;
    }
    const last = this.session.turns.length - 1;
    this.sessionTotalMs = this.turnEndMs[last] || this.turnStartMs[last] || 0;
  }
  syncTimer() {
    if (this.turnStartMs.length > this.activeTurnIndex) {
      const isLast = this.session && this.activeTurnIndex === this.session.turns.length - 1;
      this.displayedTimeMs = isLast ? this.turnEndMs[this.activeTurnIndex] || this.turnStartMs[this.activeTurnIndex] || 0 : this.turnStartMs[this.activeTurnIndex] || 0;
    }
  }
  /** Map a progress bar percentage to a turn index (equally spaced). */
  turnFromPct(pct) {
    if (!this.session || this.session.turns.length === 0) return 0;
    return Math.min(Math.floor(pct * this.session.turns.length), this.session.turns.length - 1);
  }
  formatTime(ms) {
    if (ms <= 0) return "0:00";
    const totalSec = Math.round(ms / 1e3);
    const h = Math.floor(totalSec / 3600);
    const m = Math.floor(totalSec % 3600 / 60);
    const s = totalSec % 60;
    if (h > 0) return `${h}:${String(m).padStart(2, "0")}:${String(s).padStart(2, "0")}`;
    return `${m}:${String(s).padStart(2, "0")}`;
  }
  /** Capture full UI state: collapse, expanded tools/text, scroll position. */
  captureUIState() {
    var _a, _b, _c, _d, _e, _f, _g;
    const collapsedTurns = /* @__PURE__ */ new Set();
    const turnEls = ((_a = this.renderer) == null ? void 0 : _a.getTurnElements()) || [];
    for (let i = 0; i < turnEls.length; i++) {
      if (turnEls[i].hasClass("collapsed")) {
        collapsedTurns.add(i);
      }
    }
    const summaryEl = (_b = this.timelineEl) == null ? void 0 : _b.querySelector(".claude-sessions-summary");
    const summaryOpen = (_c = summaryEl == null ? void 0 : summaryEl.hasClass("open")) != null ? _c : false;
    const pinnedEl = (_d = this.timelineEl) == null ? void 0 : _d.querySelector(".claude-sessions-pinned-heroes");
    const heroesPinned = (_e = pinnedEl == null ? void 0 : pinnedEl.hasClass("is-pinned")) != null ? _e : false;
    const openTools = /* @__PURE__ */ new Set();
    for (let t = 0; t < turnEls.length; t++) {
      const toolBlocks = turnEls[t].querySelectorAll(".claude-sessions-tool-block");
      for (let b = 0; b < toolBlocks.length; b++) {
        if (toolBlocks[b].hasClass("open")) {
          openTools.add(`${t}-${b}`);
        }
      }
    }
    const openToolGroups = /* @__PURE__ */ new Set();
    for (let t = 0; t < turnEls.length; t++) {
      const groups = turnEls[t].querySelectorAll(".claude-sessions-tool-group");
      for (let g = 0; g < groups.length; g++) {
        if (groups[g].hasClass("open")) {
          openToolGroups.add(`${t}-${g}`);
        }
      }
    }
    const openThinking = /* @__PURE__ */ new Set();
    for (let t = 0; t < turnEls.length; t++) {
      const thinking = turnEls[t].querySelectorAll(".claude-sessions-thinking-block");
      for (let k = 0; k < thinking.length; k++) {
        if (thinking[k].hasClass("open")) {
          openThinking.add(`${t}-${k}`);
        }
      }
    }
    const expandedText = /* @__PURE__ */ new Set();
    for (let t = 0; t < turnEls.length; t++) {
      const wraps = turnEls[t].querySelectorAll(".claude-sessions-collapsible-wrap");
      for (let w = 0; w < wraps.length; w++) {
        if (!wraps[w].hasClass("is-collapsed")) {
          expandedText.add(`${t}-${w}`);
        }
      }
    }
    const previewToggles = /* @__PURE__ */ new Set();
    for (let t = 0; t < turnEls.length; t++) {
      const toggles = turnEls[t].querySelectorAll(".claude-sessions-read-md-toggle");
      for (let m = 0; m < toggles.length; m++) {
        const codeView = toggles[m].querySelector(".claude-sessions-read-md-code");
        if (codeView == null ? void 0 : codeView.hasClass("claude-sessions-read-md-hidden")) {
          previewToggles.add(`${t}-${m}`);
        }
      }
    }
    const scrollTop = (_g = (_f = this.timelineEl) == null ? void 0 : _f.scrollTop) != null ? _g : 0;
    return { collapsedTurns, summaryOpen, heroesPinned, openTools, openToolGroups, openThinking, expandedText, previewToggles, scrollTop };
  }
  /** Restore full UI state after re-render. */
  restoreUIState(state) {
    var _a, _b, _c, _d;
    const turnEls = ((_a = this.renderer) == null ? void 0 : _a.getTurnElements()) || [];
    for (const idx of state.collapsedTurns) {
      if (idx < turnEls.length) {
        turnEls[idx].addClass("collapsed");
        const chevron = turnEls[idx].querySelector(".claude-sessions-turn-chevron");
        if (chevron) chevron.textContent = "\u25B6";
        const header = turnEls[idx].querySelector(".claude-sessions-turn-header");
        header == null ? void 0 : header.setAttribute("aria-expanded", "false");
      }
    }
    if (state.summaryOpen) {
      const summaryEl = (_b = this.timelineEl) == null ? void 0 : _b.querySelector(".claude-sessions-summary");
      summaryEl == null ? void 0 : summaryEl.addClass("open");
      const chevron = summaryEl == null ? void 0 : summaryEl.querySelector(".claude-sessions-summary-chevron");
      if (chevron) chevron.textContent = "\u25BC";
      const header = summaryEl == null ? void 0 : summaryEl.querySelector(".claude-sessions-summary-header");
      header == null ? void 0 : header.setAttribute("aria-expanded", "true");
    }
    if (state.heroesPinned) {
      const pinnedEl = (_c = this.timelineEl) == null ? void 0 : _c.querySelector(".claude-sessions-pinned-heroes");
      pinnedEl == null ? void 0 : pinnedEl.addClass("is-pinned");
      const pinBtn = (_d = this.timelineEl) == null ? void 0 : _d.querySelector(".claude-sessions-heroes-pin");
      pinBtn == null ? void 0 : pinBtn.addClass("is-active");
    }
    for (const key of state.openTools) {
      const [t, b] = key.split("-").map(Number);
      if (t < turnEls.length) {
        const toolBlocks = turnEls[t].querySelectorAll(".claude-sessions-tool-block");
        if (b < toolBlocks.length) {
          toolBlocks[b].addClass("open");
          const header = toolBlocks[b].querySelector(".claude-sessions-tool-header");
          header == null ? void 0 : header.setAttribute("aria-expanded", "true");
        }
      }
    }
    for (const key of state.openToolGroups) {
      const [t, g] = key.split("-").map(Number);
      if (t < turnEls.length) {
        const groups = turnEls[t].querySelectorAll(".claude-sessions-tool-group");
        if (g < groups.length) {
          groups[g].addClass("open");
          const header = groups[g].querySelector(".claude-sessions-tool-group-header");
          header == null ? void 0 : header.setAttribute("aria-expanded", "true");
        }
      }
    }
    for (const key of state.openThinking) {
      const [t, k] = key.split("-").map(Number);
      if (t < turnEls.length) {
        const thinking = turnEls[t].querySelectorAll(".claude-sessions-thinking-block");
        if (k < thinking.length) {
          thinking[k].addClass("open");
          const header = thinking[k].querySelector(".claude-sessions-thinking-header");
          header == null ? void 0 : header.setAttribute("aria-expanded", "true");
        }
      }
    }
    for (const key of state.expandedText) {
      const [t, w] = key.split("-").map(Number);
      if (t < turnEls.length) {
        const wraps = turnEls[t].querySelectorAll(".claude-sessions-collapsible-wrap");
        if (w < wraps.length) {
          wraps[w].removeClass("is-collapsed");
          const btn = wraps[w].querySelector(".claude-sessions-collapsible-toggle");
          if (btn) {
            btn.textContent = "Show less";
            btn.setAttribute("aria-expanded", "true");
          }
        }
      }
    }
    for (const key of state.previewToggles) {
      const [t, m] = key.split("-").map(Number);
      if (t < turnEls.length) {
        const toggles = turnEls[t].querySelectorAll(".claude-sessions-read-md-toggle");
        if (m < toggles.length) {
          const previewBtn = toggles[m].querySelector(".claude-sessions-read-md-btn:last-child");
          previewBtn == null ? void 0 : previewBtn.click();
        }
      }
    }
    if (this.timelineEl) {
      this.timelineEl.scrollTop = state.scrollTop;
    }
  }
  /** Capture expand/collapse state for a single turn element. */
  captureTurnState(el) {
    const openTools = [];
    el.querySelectorAll(".claude-sessions-tool-block").forEach((b, i) => {
      if (b.hasClass("open")) openTools.push(i);
    });
    const openGroups = [];
    el.querySelectorAll(".claude-sessions-tool-group").forEach((g, i) => {
      if (g.hasClass("open")) openGroups.push(i);
    });
    const openThinking = [];
    el.querySelectorAll(".claude-sessions-thinking-block").forEach((t, i) => {
      if (t.hasClass("open")) openThinking.push(i);
    });
    const expandedWraps = [];
    el.querySelectorAll(".claude-sessions-collapsible-wrap").forEach((w, i) => {
      if (!w.hasClass("is-collapsed")) expandedWraps.push(i);
    });
    const previewToggles = [];
    el.querySelectorAll(".claude-sessions-read-md-toggle").forEach((t, i) => {
      const code = t.querySelector(".claude-sessions-read-md-code");
      if (code == null ? void 0 : code.hasClass("claude-sessions-read-md-hidden")) previewToggles.push(i);
    });
    return {
      collapsed: el.hasClass("collapsed"),
      openTools,
      openGroups,
      openThinking,
      expandedWraps,
      previewToggles
    };
  }
  /** Restore expand/collapse state onto a (re-rendered) turn element. */
  restoreTurnState(el, state) {
    var _a, _b, _c, _d, _e;
    if (state.collapsed) {
      el.addClass("collapsed");
      const chevron = el.querySelector(".claude-sessions-turn-chevron");
      if (chevron) chevron.textContent = "\u25B6";
      (_a = el.querySelector(".claude-sessions-turn-header")) == null ? void 0 : _a.setAttribute("aria-expanded", "false");
    }
    const toolBlocks = el.querySelectorAll(".claude-sessions-tool-block");
    for (const i of state.openTools) {
      if (i < toolBlocks.length) {
        toolBlocks[i].addClass("open");
        (_b = toolBlocks[i].querySelector(".claude-sessions-tool-header")) == null ? void 0 : _b.setAttribute("aria-expanded", "true");
      }
    }
    const groups = el.querySelectorAll(".claude-sessions-tool-group");
    for (const i of state.openGroups) {
      if (i < groups.length) {
        groups[i].addClass("open");
        (_c = groups[i].querySelector(".claude-sessions-tool-group-header")) == null ? void 0 : _c.setAttribute("aria-expanded", "true");
      }
    }
    const thinking = el.querySelectorAll(".claude-sessions-thinking-block");
    for (const i of state.openThinking) {
      if (i < thinking.length) {
        thinking[i].addClass("open");
        (_d = thinking[i].querySelector(".claude-sessions-thinking-header")) == null ? void 0 : _d.setAttribute("aria-expanded", "true");
      }
    }
    const wraps = el.querySelectorAll(".claude-sessions-collapsible-wrap");
    for (const i of state.expandedWraps) {
      if (i < wraps.length) {
        wraps[i].removeClass("is-collapsed");
        const btn = wraps[i].querySelector(".claude-sessions-collapsible-toggle");
        if (btn) {
          btn.textContent = "Show less";
          btn.setAttribute("aria-expanded", "true");
        }
      }
    }
    const toggles = el.querySelectorAll(".claude-sessions-read-md-toggle");
    for (const i of state.previewToggles) {
      if (i < toggles.length) {
        (_e = toggles[i].querySelector(".claude-sessions-read-md-btn:last-child")) == null ? void 0 : _e.click();
      }
    }
  }
  // Rendering
  renderFullTimeline() {
    if (!this.session || !this.renderer || !this.timelineEl) return;
    const uiState = this.captureUIState();
    this.destroyObserver();
    if (this.session.turns.length === 0) {
      this.timelineEl.empty();
      this.timelineEl.createDiv({
        cls: "claude-sessions-empty",
        text: "This session has no turns."
      });
      return;
    }
    this.renderer.renderTimeline(this.session.turns, this.sessionStartMs, this.session);
    this.restoreUIState(uiState);
    this.setupScrollObserver();
  }
  setupScrollObserver() {
    var _a;
    if (!this.timelineEl) return;
    const turnEls = ((_a = this.renderer) == null ? void 0 : _a.getTurnElements()) || [];
    if (turnEls.length === 0) return;
    this.observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) {
            entry.target.addClass("visible");
          } else {
            entry.target.removeClass("visible");
          }
        }
        const scrollEl = this.timelineEl;
        const atBottom = scrollEl ? scrollEl.scrollTop + scrollEl.clientHeight >= scrollEl.scrollHeight - 50 : false;
        this.isFollowing = atBottom;
        let active = -1;
        if (atBottom) {
          active = turnEls.length - 1;
        } else {
          for (let i = turnEls.length - 1; i >= 0; i--) {
            if (turnEls[i].hasClass("visible")) {
              active = i;
              break;
            }
          }
        }
        if (active >= 0) {
          this.activeTurnIndex = active;
          this.syncTimer();
          this.updateControls();
        }
      },
      {
        root: this.timelineEl,
        rootMargin: "0px",
        threshold: 0.1
      }
    );
    for (const el of turnEls) {
      this.observer.observe(el);
    }
  }
  destroyObserver() {
    if (this.observer) {
      this.observer.disconnect();
      this.observer = null;
    }
  }
  // Turn separator dots on progress bar — placed at boundaries between turns
  renderTurnDots() {
    if (!this.session || !this.progressBar) return;
    const total = this.session.turns.length;
    const existingDots = this.progressBar.querySelectorAll(".claude-sessions-turn-dot");
    if (total <= 1) {
      existingDots.forEach((d) => d.remove());
      return;
    }
    const needed = total - 1;
    for (let i = 0; i < needed; i++) {
      const dot = i < existingDots.length ? existingDots[i] : this.progressBar.createDiv({ cls: "claude-sessions-turn-dot" });
      const pct = (i + 1) / total * 100;
      dot.style.left = `${pct}%`;
      dot.dataset.turnIndex = String(i + 1);
    }
    for (let i = needed; i < existingDots.length; i++) {
      existingDots[i].remove();
    }
  }
  updateTurnDots() {
    if (!this.progressBar) return;
    const dots = this.progressBar.querySelectorAll(".claude-sessions-turn-dot");
    dots.forEach((dot) => {
      const idx = parseInt(dot.dataset.turnIndex || "0", 10);
      dot.toggleClass("reached", idx <= this.activeTurnIndex);
    });
  }
  // Controls
  buildControls(container) {
    const row = container.createDiv({ cls: "claude-sessions-controls-row" });
    this.statusEl = row.createSpan({ cls: "claude-sessions-progress-text" });
    const searchBtn = row.createEl("button", {
      cls: "claude-sessions-ctrl-btn claude-sessions-search-btn",
      attr: { "aria-label": "Search in session", "data-tooltip-position": "top" }
    });
    (0, import_obsidian11.setIcon)(searchBtn, "search");
    searchBtn.addEventListener("click", () => void this.openInSessionSearch());
    const filterBtn = row.createEl("button", {
      cls: "claude-sessions-ctrl-btn claude-sessions-filter-btn",
      attr: { "aria-label": "Filter content", "data-tooltip-position": "top" },
      text: "\u22EF"
    });
    makeClickable(filterBtn, { label: "Filter content", expanded: false });
    filterBtn.addEventListener("click", (e) => {
      this.showFilterMenu(e);
    });
    this.watchBtn = row.createEl("button", {
      cls: "claude-sessions-ctrl-btn claude-sessions-watch-btn",
      attr: { "aria-label": "Start live watch", "data-tooltip-position": "top" }
    });
    (0, import_obsidian11.setIcon)(this.watchBtn, "radio");
    this.watchBtn.addEventListener("click", () => this.toggleWatch());
    const progressWrap = container.createDiv({ cls: "claude-sessions-progress-wrap" });
    this.progressBar = progressWrap.createDiv({ cls: "claude-sessions-progress-bar" });
    this.progressBar.setAttribute("role", "progressbar");
    this.progressBar.setAttribute("aria-label", "Session progress");
    this.progressBar.setAttribute("aria-valuemin", "0");
    this.progressBar.setAttribute("aria-valuemax", "0");
    this.progressBar.setAttribute("aria-valuenow", "0");
    this.progressBar.setAttribute("aria-valuetext", "No session loaded");
    this.progressFill = this.progressBar.createDiv({ cls: "claude-sessions-progress-fill" });
    this.progressTooltip = this.progressBar.createDiv({ cls: "claude-sessions-progress-tooltip" });
    this.progressBar.addEventListener("mousemove", (e) => {
      if (!this.session || !this.progressTooltip) return;
      const rect = this.progressBar.getBoundingClientRect();
      const pct = Math.max(0, Math.min(1, (e.clientX - rect.left) / rect.width));
      const turnIdx = this.turnFromPct(pct);
      let label = `#${turnIdx + 1}`;
      if (this.turnStartMs[turnIdx] !== void 0 && this.sessionTotalMs > 0) {
        label += ` \xB7 ${this.formatTime(this.turnStartMs[turnIdx])}`;
      }
      this.progressTooltip.setText(label);
      const left = Math.max(20, Math.min(rect.width - 20, e.clientX - rect.left));
      this.progressTooltip.style.left = `${left}px`;
    });
    this.progressBar.addEventListener("click", (e) => {
      if (!this.session) return;
      const rect = this.progressBar.getBoundingClientRect();
      const pct = Math.max(0, Math.min(1, (e.clientX - rect.left) / rect.width));
      this.scrollToTurn(this.turnFromPct(pct));
    });
  }
  showFilterMenu(e) {
    const menu = new import_obsidian11.Menu();
    const f = this.filters;
    menu.addItem((item) => item.setTitle("User").setIcon("user").setIsLabel(true));
    menu.addItem((item) => item.setTitle("Text").setChecked(f.user && f.userText).setDisabled(!f.user).onClick(() => {
      f.userText = !f.userText;
      this.applyFilters();
    }));
    menu.addItem((item) => item.setTitle("Images").setChecked(f.user && f.userImages).setDisabled(!f.user).onClick(() => {
      f.userImages = !f.userImages;
      this.applyFilters();
    }));
    menu.addItem((item) => item.setTitle(f.user ? "Hide all" : "Show all").setIcon(f.user ? "eye-off" : "eye").onClick(() => {
      f.user = !f.user;
      this.applyFilters();
    }));
    menu.addSeparator();
    menu.addItem((item) => item.setTitle("Assistant").setIcon("message-square").setIsLabel(true));
    menu.addItem((item) => item.setTitle("Text").setChecked(f.assistant && f.assistantText).setDisabled(!f.assistant).onClick(() => {
      f.assistantText = !f.assistantText;
      this.applyFilters();
    }));
    menu.addItem((item) => item.setTitle("Thinking").setIcon("brain").setChecked(f.assistant && f.thinking).setDisabled(!f.assistant).onClick(() => {
      f.thinking = !f.thinking;
      this.applyFilters();
    }));
    menu.addItem((item) => item.setTitle("Tool calls").setIcon("wrench").setChecked(f.assistant && f.toolCalls).setDisabled(!f.assistant).onClick(() => {
      f.toolCalls = !f.toolCalls;
      this.applyFilters();
    }));
    menu.addItem((item) => item.setTitle("Tool results").setIcon("file-output").setChecked(f.assistant && f.toolResults).setDisabled(!f.assistant).onClick(() => {
      f.toolResults = !f.toolResults;
      this.applyFilters();
    }));
    menu.addItem((item) => item.setTitle(f.assistant ? "Hide all" : "Show all").setIcon(f.assistant ? "eye-off" : "eye").onClick(() => {
      f.assistant = !f.assistant;
      this.applyFilters();
    }));
    if (e instanceof MouseEvent && (e.clientX !== 0 || e.clientY !== 0)) {
      menu.showAtMouseEvent(e);
    } else {
      const rect = e.target.getBoundingClientRect();
      menu.showAtPosition({ x: rect.left, y: rect.top });
    }
  }
  applyFilters(root) {
    var _a, _b;
    const scope = root != null ? root : this.timelineEl;
    if (!scope) return;
    const f = this.filters;
    scope.querySelectorAll(".claude-sessions-role-user").forEach((el) => {
      el.toggleClass("claude-sessions-filtered", !f.user);
    });
    if (f.user) {
      scope.querySelectorAll(".claude-sessions-user-text").forEach((el) => {
        const wrapper = el.closest(".claude-sessions-block-wrapper");
        wrapper == null ? void 0 : wrapper.toggleClass("claude-sessions-filtered", !f.userText);
      });
      scope.querySelectorAll(".claude-sessions-slash-command-block").forEach((el) => {
        const wrapper = el.closest(".claude-sessions-block-wrapper");
        wrapper == null ? void 0 : wrapper.toggleClass("claude-sessions-filtered", !f.userText);
      });
      scope.querySelectorAll(".claude-sessions-bash-command-block").forEach((el) => {
        const wrapper = el.closest(".claude-sessions-block-wrapper");
        wrapper == null ? void 0 : wrapper.toggleClass("claude-sessions-filtered", !f.userText);
      });
      scope.querySelectorAll(".claude-sessions-image-thumbnail").forEach((el) => {
        const wrapper = el.closest(".claude-sessions-block-wrapper");
        wrapper == null ? void 0 : wrapper.toggleClass("claude-sessions-filtered", !f.userImages);
      });
    }
    scope.querySelectorAll(".claude-sessions-role-assistant").forEach((el) => {
      el.toggleClass("claude-sessions-filtered", !f.assistant);
    });
    if (f.assistant) {
      scope.querySelectorAll(".claude-sessions-assistant-text").forEach((el) => {
        const wrapper = el.closest(".claude-sessions-block-wrapper");
        wrapper == null ? void 0 : wrapper.toggleClass("claude-sessions-filtered", !f.assistantText);
      });
      scope.querySelectorAll(".claude-sessions-thinking-block").forEach((el) => {
        const wrapper = el.closest(".claude-sessions-block-wrapper");
        wrapper == null ? void 0 : wrapper.toggleClass("claude-sessions-filtered", !f.thinking);
      });
      scope.querySelectorAll(".claude-sessions-tool-block, .claude-sessions-tool-group").forEach((el) => {
        const wrapper = el.closest(".claude-sessions-block-wrapper");
        wrapper == null ? void 0 : wrapper.toggleClass("claude-sessions-filtered", !f.toolCalls);
      });
      scope.querySelectorAll(".claude-sessions-tool-result").forEach((el) => {
        el.toggleClass("claude-sessions-filtered", !f.toolResults);
      });
    }
    if (!root) {
      const allOn = f.user && f.assistant && f.userText && f.userImages && f.assistantText && f.thinking && f.toolCalls && f.toolResults;
      (_b = (_a = this.controlsEl) == null ? void 0 : _a.querySelector(".claude-sessions-filter-btn")) == null ? void 0 : _b.toggleClass("claude-sessions-filter-active", !allOn);
    }
  }
  updateControls() {
    if (!this.session) return;
    const total = this.session.turns.length;
    if (this.statusEl) {
      if (this.sessionTotalMs > 0) {
        this.statusEl.setText(
          `#${this.activeTurnIndex + 1}/${total} \xB7 ${this.formatTime(this.displayedTimeMs)} / ${this.formatTime(this.sessionTotalMs)}`
        );
      } else {
        this.statusEl.setText(`#${this.activeTurnIndex + 1} / ${total}`);
      }
    }
    if (this.progressFill && total > 0) {
      const pct = (this.activeTurnIndex + 1) / total * 100;
      this.progressFill.style.width = `${pct}%`;
    }
    if (this.progressBar) {
      this.progressBar.setAttribute("aria-valuemax", String(total));
      this.progressBar.setAttribute("aria-valuenow", String(this.activeTurnIndex + 1));
      if (this.sessionTotalMs > 0) {
        this.progressBar.setAttribute(
          "aria-valuetext",
          `Turn ${this.activeTurnIndex + 1} of ${total} \xB7 ${this.formatTime(this.displayedTimeMs)} / ${this.formatTime(this.sessionTotalMs)}`
        );
      } else {
        this.progressBar.setAttribute(
          "aria-valuetext",
          `Turn ${this.activeTurnIndex + 1} of ${total}`
        );
      }
    }
    this.updateTurnDots();
  }
};

// src/views/search-view.ts
var import_obsidian14 = require("obsidian");

// src/utils/session-search.ts
var import_obsidian12 = require("obsidian");
var fs4 = __toESM(require("fs"));
var readline2 = __toESM(require("readline"));

// src/utils/bm25.ts
var K1 = 1.2;
var B = 0.75;
var STOP_WORDS = /* @__PURE__ */ new Set([
  "a",
  "an",
  "the",
  "and",
  "or",
  "but",
  "in",
  "on",
  "at",
  "to",
  "for",
  "of",
  "with",
  "by",
  "is",
  "it",
  "as",
  "be",
  "was",
  "are",
  "were",
  "been",
  "has",
  "had",
  "do",
  "does",
  "did",
  "will",
  "would",
  "could",
  "should",
  "may",
  "might",
  "can",
  "this",
  "that",
  "these",
  "those",
  "i",
  "you",
  "he",
  "she",
  "we",
  "they",
  "me",
  "him",
  "her",
  "us",
  "them",
  "my",
  "your",
  "his",
  "its",
  "our",
  "their",
  "not",
  "no",
  "from",
  "if",
  "so",
  "than",
  "then",
  "when",
  "what",
  "which",
  "who",
  "how",
  "all",
  "each",
  "every",
  "both",
  "few",
  "more",
  "most",
  "other",
  "some",
  "such",
  "only",
  "own",
  "same",
  "too",
  "very",
  "just"
]);
function tokenize2(text) {
  return text.toLowerCase().replace(/[^a-z0-9\s_-]/g, " ").split(/\s+/).filter((t) => t.length > 1 && !STOP_WORDS.has(t));
}
function stem(word) {
  if (word.length < 4) return word;
  if (word.endsWith("ies") && word.length > 4) return word.slice(0, -3) + "y";
  if (word.endsWith("sses")) return word.slice(0, -2);
  if (word.endsWith("ness")) return word.slice(0, -4);
  if (word.endsWith("ing") && word.length > 5) {
    const base = word.slice(0, -3);
    if (base.endsWith("e")) return base;
    return base;
  }
  if (word.endsWith("ed") && word.length > 4) {
    const base = word.slice(0, -2);
    if (base.endsWith("e")) return base;
    return base;
  }
  if (word.endsWith("tion")) return word.slice(0, -4) + "t";
  if (word.endsWith("ment") && word.length > 6) return word.slice(0, -4);
  if (word.endsWith("able") && word.length > 6) return word.slice(0, -4);
  if (word.endsWith("ible") && word.length > 6) return word.slice(0, -4);
  if (word.endsWith("ally") && word.length > 5) return word.slice(0, -4);
  if (word.endsWith("ful")) return word.slice(0, -3);
  if (word.endsWith("ous") && word.length > 5) return word.slice(0, -3);
  if (word.endsWith("ive") && word.length > 5) return word.slice(0, -3);
  if (word.endsWith("ly") && word.length > 4) return word.slice(0, -2);
  if (word.endsWith("er") && word.length > 4) return word.slice(0, -2);
  if (word.endsWith("es") && word.length > 4 && /(?:s|x|z|ch|sh)es$/.test(word)) return word.slice(0, -2);
  if (word.endsWith("s") && !word.endsWith("ss") && word.length > 3) return word.slice(0, -1);
  return word;
}
function analyze(text) {
  return tokenize2(text).map(stem);
}
var BM25Index = class {
  constructor() {
    this.docs = /* @__PURE__ */ new Map();
    /** Number of documents containing each term. */
    this.docFreqs = /* @__PURE__ */ new Map();
    this.totalDocLength = 0;
  }
  get size() {
    return this.docs.size;
  }
  get avgDocLength() {
    return this.docs.size > 0 ? this.totalDocLength / this.docs.size : 0;
  }
  /** Add or replace a document. */
  add(id, text, data) {
    var _a, _b;
    if (this.docs.has(id)) {
      this.remove(id);
    }
    const terms = analyze(text);
    const termFreqs = /* @__PURE__ */ new Map();
    for (const term of terms) {
      termFreqs.set(term, ((_a = termFreqs.get(term)) != null ? _a : 0) + 1);
    }
    const doc = { id, termFreqs, length: terms.length, data };
    this.docs.set(id, doc);
    this.totalDocLength += doc.length;
    for (const term of termFreqs.keys()) {
      this.docFreqs.set(term, ((_b = this.docFreqs.get(term)) != null ? _b : 0) + 1);
    }
  }
  /** Remove a document by ID. */
  remove(id) {
    const doc = this.docs.get(id);
    if (!doc) return false;
    this.totalDocLength -= doc.length;
    for (const term of doc.termFreqs.keys()) {
      const df = this.docFreqs.get(term);
      if (df !== void 0) {
        if (df <= 1) this.docFreqs.delete(term);
        else this.docFreqs.set(term, df - 1);
      }
    }
    this.docs.delete(id);
    return true;
  }
  /** Clear all documents. */
  clear() {
    this.docs.clear();
    this.docFreqs.clear();
    this.totalDocLength = 0;
  }
  /**
   * Query the index. Returns documents ranked by BM25 score (descending).
   * @param queryText  Raw query string (will be tokenized + stemmed)
   * @param limit      Max results to return (default 50)
   */
  search(queryText, limit = 50) {
    var _a, _b;
    const queryTerms = analyze(queryText);
    if (queryTerms.length === 0) return [];
    const N = this.docs.size;
    const avgDl = this.avgDocLength;
    const results = [];
    for (const doc of this.docs.values()) {
      let score = 0;
      for (const term of queryTerms) {
        const tf = (_a = doc.termFreqs.get(term)) != null ? _a : 0;
        if (tf === 0) continue;
        const df = (_b = this.docFreqs.get(term)) != null ? _b : 0;
        const idf = Math.log((N - df + 0.5) / (df + 0.5) + 1);
        const tfNorm = tf * (K1 + 1) / (tf + K1 * (1 - B + B * (doc.length / avgDl)));
        score += idf * tfNorm;
      }
      if (score > 0) {
        results.push({ id: doc.id, score, data: doc.data });
      }
    }
    results.sort((a, b) => b.score - a.score);
    return results.slice(0, limit);
  }
};

// src/utils/session-search.ts
init_constants();
function extractToolInputText(toolName, input) {
  var _a, _b, _c, _d;
  if (!input) return "";
  const str = (key) => {
    const val = input[key];
    return typeof val === "string" ? val : "";
  };
  switch (toolName) {
    case "Bash":
      return str("command") || str("description");
    case "Read":
    case "Write":
    case "Edit":
      return str("file_path");
    case "Grep":
    case "Glob":
      return str("pattern");
    case "Agent":
    case "Task":
      return [str("description"), str("prompt")].filter(Boolean).join(" ");
    case "WebFetch":
      return str("url");
    case "WebSearch":
      return str("query");
    case "AskUserQuestion": {
      const qs = input["questions"];
      if (qs == null ? void 0 : qs.length) {
        return qs.map((q) => typeof q["question"] === "string" ? q["question"] : "").join(" ");
      }
      return "";
    }
    default: {
      const nameField = (_d = (_c = (_b = (_a = input["name"]) != null ? _a : input["path"]) != null ? _b : input["file"]) != null ? _c : input["query"]) != null ? _d : input["command"];
      if (typeof nameField === "string") return nameField;
      return Object.entries(input).filter(([, v]) => typeof v === "string").map(([, v]) => v).join(" ").slice(0, 200);
    }
  }
}
var RE_LINE_NUMBERS = /^\d+[\u2192\t]/gm;
var RE_EDIT_WRITE_SUCCESS = /^The file .+ has been (?:updated|created) successfully\.?$/;
function isEditWriteSuccessMessage(content) {
  return RE_EDIT_WRITE_SUCCESS.test(content.trim());
}
function cleanReadResult(content) {
  return content.replace(RE_LINE_NUMBERS, "").replace(RE_SYSTEM_REMINDER, "").trim();
}
function indexTextForBlock(block) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
  switch (block.type) {
    case "text":
      return (_a = block.text) != null ? _a : "";
    case "thinking":
      return (_b = block.thinking) != null ? _b : "";
    case "tool_use":
      return extractToolInputText(block.name, block.input);
    case "tool_result":
      if (block.toolName === "Read" && !block.isError) {
        return cleanReadResult((_c = block.content) != null ? _c : "");
      }
      if ((block.toolName === "Edit" || block.toolName === "Write") && !block.isError) {
        if (isEditWriteSuccessMessage((_d = block.content) != null ? _d : "")) {
          return "";
        }
      }
      return (_e = block.content) != null ? _e : "";
    case "slash_command":
      return (_f = block.text) != null ? _f : "";
    case "bash_command": {
      const parts = [];
      if (block.command) parts.push(block.command);
      const stdout = ((_g = block.stdout) != null ? _g : "").replace(ANSI_STRIP_RE, "");
      if (stdout) parts.push(stdout);
      const stderr = ((_h = block.stderr) != null ? _h : "").replace(ANSI_STRIP_RE, "");
      if (stderr) parts.push(stderr);
      return parts.join("\n");
    }
    case "ansi":
      return ((_i = block.text) != null ? _i : "").replace(ANSI_STRIP_RE, "");
    case "compaction":
      return (_j = block.summary) != null ? _j : "";
    case "image":
      return "";
  }
}
function extractSearchableContent(line) {
  const trimmed = line.trim();
  if (!trimmed) return null;
  const head = trimmed.length > 200 ? trimmed.slice(0, 200) : trimmed;
  for (const sub of SKIP_TYPE_STRINGS) {
    if (head.includes(sub)) return null;
  }
  let record;
  try {
    record = JSON.parse(trimmed);
  } catch (e) {
    return null;
  }
  if (record["isSidechain"] || record["isMeta"]) return null;
  const recordType = record["type"];
  const timestamp = record["timestamp"];
  const message = record["message"];
  if (recordType === "assistant") {
    if (!message) return null;
    if (message["model"] === "<synthetic>") return null;
    const content = message["content"];
    if (!Array.isArray(content)) return null;
    for (const block of content) {
      const blockType = block["type"];
      if (blockType === "text" && typeof block["text"] === "string") {
        return { role: "assistant", blockType: "text", text: block["text"], timestamp };
      }
      if (blockType === "thinking" && typeof block["thinking"] === "string" && block["thinking"]) {
        return { role: "assistant", blockType: "thinking", text: block["thinking"], timestamp };
      }
      if (blockType === "tool_use") {
        const name = block["name"] || "";
        const input = block["input"];
        const inputText = extractToolInputText(name, input);
        return { role: "assistant", blockType: "tool_use", text: `${name} ${inputText}`, toolName: name, timestamp };
      }
    }
    return null;
  }
  if (recordType === "user") {
    if (!message) return null;
    const content = message["content"];
    if (typeof content === "string") {
      return { role: "user", blockType: "text", text: content, timestamp };
    }
    if (Array.isArray(content)) {
      const texts = [];
      for (const block of content) {
        if (block["type"] === "tool_result") {
          const c = block["content"];
          if (typeof c === "string" && !isEditWriteSuccessMessage(c)) {
            texts.push(c);
          }
        }
      }
      if (texts.length > 0) {
        return { role: "assistant", blockType: "tool_result", text: texts.join("\n"), timestamp };
      }
    }
    return null;
  }
  if (recordType === "system") {
    const subtype = record["subtype"];
    const content = record["content"];
    if (subtype === SUBTYPE_LOCAL_COMMAND && content) {
      const cmdMatch = content.match(RE_COMMAND_NAME);
      if (cmdMatch) {
        const commandName = cmdMatch[1];
        const argsMatch = content.match(RE_COMMAND_ARGS);
        const args = argsMatch ? argsMatch[1].trim() : "";
        const text = args ? `${commandName} ${args}` : commandName;
        return { role: "user", blockType: "slash_command", text, timestamp };
      }
    }
    return null;
  }
  return null;
}
var CONTEXT_CHARS = 75;
var DEFAULT_MAX_MATCHES = 20;
async function searchFile(filePath, query, maxMatches = DEFAULT_MAX_MATCHES, signal) {
  if (!import_obsidian12.Platform.isDesktop) return { matches: [], totalMatches: 0 };
  const searchText = query.caseSensitive ? query.text : query.text.toLowerCase();
  const matches = [];
  let totalMatches = 0;
  let approxTurnIndex = 0;
  let lastRole = null;
  return new Promise((resolve2) => {
    let stream;
    try {
      stream = fs4.createReadStream(filePath, { encoding: "utf-8" });
    } catch (e) {
      resolve2({ matches, totalMatches });
      return;
    }
    const rl = readline2.createInterface({ input: stream, crlfDelay: Infinity });
    const cleanup = () => {
      rl.close();
      stream.destroy();
    };
    if (signal) {
      signal.addEventListener("abort", cleanup, { once: true });
    }
    rl.on("line", (line) => {
      if (signal == null ? void 0 : signal.aborted) return;
      const extracted = extractSearchableContent(line);
      if (!extracted) return;
      if (extracted.role !== lastRole) {
        if (lastRole !== null) approxTurnIndex++;
        lastRole = extracted.role;
      }
      if (query.roleFilter && query.roleFilter !== "all" && extracted.role !== query.roleFilter) {
        return;
      }
      const haystack = query.caseSensitive ? extracted.text : extracted.text.toLowerCase();
      let searchFrom = 0;
      while (true) {
        const idx = haystack.indexOf(searchText, searchFrom);
        if (idx === -1) break;
        totalMatches++;
        if (matches.length < maxMatches) {
          const matchEnd = idx + searchText.length;
          matches.push({
            turnIndex: approxTurnIndex,
            role: extracted.role,
            blockType: extracted.blockType,
            toolName: extracted.toolName,
            matchText: extracted.text.slice(idx, matchEnd),
            contextBefore: extracted.text.slice(Math.max(0, idx - CONTEXT_CHARS), idx),
            contextAfter: extracted.text.slice(matchEnd, matchEnd + CONTEXT_CHARS),
            timestamp: extracted.timestamp
          });
        }
        searchFrom = idx + 1;
      }
    });
    rl.on("close", () => {
      resolve2({ matches, totalMatches });
    });
    stream.on("error", () => {
      resolve2({ matches, totalMatches });
    });
  });
}
function resolveMatchTurn(match, turns) {
  if (!turns.length) return 0;
  if (match.timestamp) {
    const matchMs = new Date(match.timestamp).getTime();
    if (!isNaN(matchMs)) {
      for (let i = 0; i < turns.length; i++) {
        const turnMs = turns[i].timestamp ? new Date(turns[i].timestamp).getTime() : NaN;
        const nextMs = i + 1 < turns.length && turns[i + 1].timestamp ? new Date(turns[i + 1].timestamp).getTime() : Infinity;
        if (!isNaN(turnMs) && matchMs >= turnMs && matchMs < nextMs) {
          return i;
        }
      }
    }
  }
  return Math.min(match.turnIndex, turns.length - 1);
}
async function searchSessions(entries, query, onResult, onProgress, signal) {
  for (let i = 0; i < entries.length; i++) {
    if (signal == null ? void 0 : signal.aborted) return;
    const entry = entries[i];
    const { matches, totalMatches } = await searchFile(entry.path, query, DEFAULT_MAX_MATCHES, signal);
    if (matches.length > 0) {
      onResult({ entry, matches, totalMatches });
    }
    onProgress(i + 1, entries.length);
  }
}
async function searchFileRanked(filePath, query, maxMatches = DEFAULT_MAX_MATCHES, signal) {
  if (!import_obsidian12.Platform.isDesktop) return { matches: [], totalMatches: 0 };
  const searchText = query.caseSensitive ? query.text : query.text.toLowerCase();
  const index = new BM25Index();
  const matches = [];
  let totalMatches = 0;
  let approxTurnIndex = 0;
  let lastRole = null;
  let docId = 0;
  await new Promise((resolve2) => {
    let stream;
    try {
      stream = fs4.createReadStream(filePath, { encoding: "utf-8" });
    } catch (e) {
      resolve2();
      return;
    }
    const rl = readline2.createInterface({ input: stream, crlfDelay: Infinity });
    const cleanup = () => {
      rl.close();
      stream.destroy();
    };
    if (signal) {
      signal.addEventListener("abort", cleanup, { once: true });
    }
    rl.on("line", (line) => {
      if (signal == null ? void 0 : signal.aborted) return;
      const extracted = extractSearchableContent(line);
      if (!extracted) return;
      if (extracted.role !== lastRole) {
        if (lastRole !== null) approxTurnIndex++;
        lastRole = extracted.role;
      }
      if (query.roleFilter && query.roleFilter !== "all" && extracted.role !== query.roleFilter) {
        return;
      }
      const id = String(docId++);
      index.add(id, extracted.text, null);
      const haystack = query.caseSensitive ? extracted.text : extracted.text.toLowerCase();
      let searchFrom = 0;
      while (true) {
        const idx = haystack.indexOf(searchText, searchFrom);
        if (idx === -1) break;
        totalMatches++;
        const matchEnd = idx + searchText.length;
        matches.push({
          docId: id,
          turnIndex: approxTurnIndex,
          role: extracted.role,
          blockType: extracted.blockType,
          toolName: extracted.toolName,
          matchText: extracted.text.slice(idx, matchEnd),
          contextBefore: extracted.text.slice(Math.max(0, idx - CONTEXT_CHARS), idx),
          contextAfter: extracted.text.slice(matchEnd, matchEnd + CONTEXT_CHARS),
          timestamp: extracted.timestamp
        });
        searchFrom = idx + 1;
      }
    });
    rl.on("close", () => resolve2());
    stream.on("error", () => resolve2());
  });
  if (signal == null ? void 0 : signal.aborted) return { matches: [], totalMatches: 0 };
  const scoreMap = /* @__PURE__ */ new Map();
  const ranked = index.search(query.text, index.size);
  for (const r of ranked) {
    scoreMap.set(r.id, r.score);
  }
  matches.sort((a, b) => {
    var _a, _b;
    return ((_a = scoreMap.get(b.docId)) != null ? _a : 0) - ((_b = scoreMap.get(a.docId)) != null ? _b : 0);
  });
  const result = matches.slice(0, maxMatches).map(({ docId: _id, ...m }) => {
    var _a;
    return {
      ...m,
      score: (_a = scoreMap.get(_id)) != null ? _a : 0
    };
  });
  return { matches: result, totalMatches };
}
async function searchSessionsRanked(entries, query, onResult, onProgress, signal) {
  for (let i = 0; i < entries.length; i++) {
    if (signal == null ? void 0 : signal.aborted) return;
    const entry = entries[i];
    const { matches, totalMatches } = await searchFileRanked(entry.path, query, DEFAULT_MAX_MATCHES, signal);
    if (matches.length > 0) {
      onResult({ entry, matches, totalMatches });
    }
    onProgress(i + 1, entries.length);
  }
}
function buildInSessionMatch(turn, block, contentBlockIndex, indexedText, idx, needleLen, occurrenceInBlock) {
  const matchEnd = idx + needleLen;
  const toolName = block.type === "tool_use" ? block.name : block.type === "tool_result" ? block.toolName : void 0;
  return {
    turnIndex: turn.index,
    role: turn.role,
    blockType: block.type,
    toolName,
    matchText: indexedText.slice(idx, matchEnd),
    contextBefore: indexedText.slice(Math.max(0, idx - CONTEXT_CHARS), idx),
    contextAfter: indexedText.slice(matchEnd, matchEnd + CONTEXT_CHARS),
    timestamp: turn.timestamp,
    contentBlockIndex,
    charOffset: idx,
    charLength: needleLen,
    occurrenceInBlock
  };
}
function searchTurns(turns, query, maxMatches = DEFAULT_MAX_MATCHES) {
  var _a;
  if (!query.text) return { matches: [], totalMatches: 0 };
  const needle = query.caseSensitive ? query.text : query.text.toLowerCase();
  const roleFilter = (_a = query.roleFilter) != null ? _a : "all";
  const matches = [];
  let totalMatches = 0;
  for (const turn of turns) {
    if (roleFilter !== "all" && turn.role !== roleFilter) continue;
    for (let i = 0; i < turn.contentBlocks.length; i++) {
      const block = turn.contentBlocks[i];
      const indexedText = indexTextForBlock(block);
      if (!indexedText) continue;
      const haystack = query.caseSensitive ? indexedText : indexedText.toLowerCase();
      let searchFrom = 0;
      let occurrenceInBlock = 0;
      while (true) {
        const idx = haystack.indexOf(needle, searchFrom);
        if (idx === -1) break;
        totalMatches++;
        if (matches.length < maxMatches) {
          matches.push(buildInSessionMatch(turn, block, i, indexedText, idx, needle.length, occurrenceInBlock));
        }
        occurrenceInBlock++;
        searchFrom = idx + 1;
      }
    }
  }
  return { matches, totalMatches };
}
function searchTurnsRanked(turns, query, maxMatches = DEFAULT_MAX_MATCHES) {
  var _a;
  if (!query.text) return { matches: [], totalMatches: 0 };
  const needle = query.caseSensitive ? query.text : query.text.toLowerCase();
  const roleFilter = (_a = query.roleFilter) != null ? _a : "all";
  const index = new BM25Index();
  const matchesWithDocId = [];
  let totalMatches = 0;
  for (const turn of turns) {
    if (roleFilter !== "all" && turn.role !== roleFilter) continue;
    for (let i = 0; i < turn.contentBlocks.length; i++) {
      const block = turn.contentBlocks[i];
      const indexedText = indexTextForBlock(block);
      if (!indexedText) continue;
      const docId = `${turn.index}:${i}`;
      index.add(docId, indexedText, null);
      const haystack = query.caseSensitive ? indexedText : indexedText.toLowerCase();
      let searchFrom = 0;
      let occurrenceInBlock = 0;
      while (true) {
        const idx = haystack.indexOf(needle, searchFrom);
        if (idx === -1) break;
        totalMatches++;
        matchesWithDocId.push({
          docId,
          ...buildInSessionMatch(turn, block, i, indexedText, idx, needle.length, occurrenceInBlock)
        });
        occurrenceInBlock++;
        searchFrom = idx + 1;
      }
    }
  }
  const scoreMap = /* @__PURE__ */ new Map();
  const ranked = index.search(query.text, index.size);
  for (const r of ranked) {
    scoreMap.set(r.id, r.score);
  }
  matchesWithDocId.sort((a, b) => {
    var _a2, _b;
    return ((_a2 = scoreMap.get(b.docId)) != null ? _a2 : 0) - ((_b = scoreMap.get(a.docId)) != null ? _b : 0);
  });
  const result = matchesWithDocId.slice(0, maxMatches).map(({ docId: id, ...m }) => {
    var _a2;
    return {
      ...m,
      score: (_a2 = scoreMap.get(id)) != null ? _a2 : 0
    };
  });
  return { matches: result, totalMatches };
}

// src/views/search-view.ts
init_streaming_reader();
init_detect();
init_claude_subagent();
init_path_utils();
init_session_browser_modal();
var VIEW_TYPE_SEARCH = "claude-sessions-search";
var DEBOUNCE_MS = 300;
var VISIBLE_MATCHES_PER_SESSION = 5;
var SINGLE_SESSION_MAX_MATCHES = 100;
var SINGLE_SESSION_VISIBLE = 20;
var SearchView = class _SearchView extends import_obsidian14.ItemView {
  constructor(leaf, plugin) {
    super(leaf);
    // Mode
    this.mode = "cross-session";
    this.sortMode = "relevance";
    // Cross-session state
    this.entries = [];
    this.entriesLoaded = false;
    this.cachedCrossResults = null;
    this.cachedCrossProgress = "";
    this.cachedCrossKey = "";
    // In-session state
    this.trackedTimelineLeaf = null;
    this.trackedSession = null;
    this.trackedFilePath = null;
    // Search state
    this.abortController = null;
    this.debounceTimer = null;
    // Active result tracking
    this.activeRowEl = null;
    // Filter state
    this.roleFilter = "all";
    this.plugin = plugin;
  }
  getViewType() {
    return VIEW_TYPE_SEARCH;
  }
  getDisplayText() {
    return "Session search";
  }
  getIcon() {
    return "search";
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("claude-sessions-search-view");
    const modeRow = contentEl.createDiv({ cls: "claude-sessions-search-mode-toggle" });
    this.crossBtn = modeRow.createEl("button", {
      cls: "claude-sessions-search-mode-btn",
      text: "All sessions",
      attr: { "aria-label": "Search all sessions", "data-tooltip-position": "bottom" }
    });
    this.crossBtn.addEventListener("click", () => this.setMode("cross-session"));
    this.inSessionBtn = modeRow.createEl("button", {
      cls: "claude-sessions-search-mode-btn",
      text: "Current session",
      attr: { "aria-label": "Search current session", "data-tooltip-position": "bottom" }
    });
    this.inSessionBtn.addEventListener("click", () => this.setMode("in-session"));
    const scopeRow = contentEl.createDiv({ cls: "claude-sessions-search-scope-row" });
    this.scopeLabel = scopeRow.createDiv({ cls: "claude-sessions-search-scope-label" });
    this.refreshBtn = scopeRow.createEl("button", {
      cls: "claude-sessions-search-refresh-btn clickable-icon",
      attr: { "aria-label": "Refresh session list" }
    });
    (0, import_obsidian14.setIcon)(this.refreshBtn, "refresh-cw");
    this.refreshBtn.addEventListener("click", () => this.refreshEntries());
    const inputRow = contentEl.createDiv({ cls: "claude-sessions-search-input-row" });
    const inputContainer = inputRow.createDiv({ cls: "claude-sessions-search-input-container" });
    const searchIcon = inputContainer.createSpan({ cls: "claude-sessions-search-icon" });
    (0, import_obsidian14.setIcon)(searchIcon, "search");
    this.inputEl = inputContainer.createEl("input", {
      type: "text",
      cls: "claude-sessions-search-input",
      attr: {
        placeholder: "Search...",
        "aria-label": "Search query"
      }
    });
    this.clearBtn = inputContainer.createEl("button", {
      cls: "claude-sessions-search-clear-btn",
      attr: {
        "aria-label": "Clear search",
        "data-tooltip-position": "top"
      }
    });
    (0, import_obsidian14.setIcon)(this.clearBtn, "x");
    this.clearBtn.addEventListener("click", () => {
      this.inputEl.value = "";
      this.syncClearButton();
      this.onQueryChange();
      this.inputEl.focus();
    });
    this.optionsBtn = inputRow.createEl("button", {
      cls: "claude-sessions-search-options-btn clickable-icon",
      attr: {
        "aria-label": "Search options",
        "data-tooltip-position": "top"
      }
    });
    (0, import_obsidian14.setIcon)(this.optionsBtn, "more-vertical");
    this.optionsBtn.addEventListener("click", (e) => this.showOptionsMenu(e));
    this.progressEl = contentEl.createDiv({ cls: "claude-sessions-search-progress" });
    this.resultsEl = contentEl.createDiv({ cls: "claude-sessions-search-results" });
    this.inputEl.addEventListener("input", () => {
      this.syncClearButton();
      this.onQueryChange();
    });
    this.syncClearButton();
    this.inputEl.addEventListener("keydown", (e) => {
      if (e.key === "ArrowDown") {
        e.preventDefault();
        const first = this.resultsEl.querySelector(".claude-sessions-search-match-row");
        first == null ? void 0 : first.focus();
      }
    });
    this.resultsEl.addEventListener("keydown", (e) => {
      const target = e.target;
      if (!target.hasClass("claude-sessions-search-match-row")) return;
      if (e.key === "ArrowDown") {
        e.preventDefault();
        const next = nextFocusable(target, this.resultsEl);
        next == null ? void 0 : next.focus();
      } else if (e.key === "ArrowUp") {
        e.preventDefault();
        const prev = prevFocusable(target, this.resultsEl);
        if (prev) prev.focus();
        else this.inputEl.focus();
      } else if (e.key === "Enter") {
        e.preventDefault();
        target.click();
      }
    });
    this.syncModeUI();
    this.resolveTrackedSession();
    this.updateScopeLabel();
    window.requestAnimationFrame(() => this.inputEl.focus());
    return Promise.resolve();
  }
  onClose() {
    this.abortSearch();
    if (this.debounceTimer !== null) {
      window.clearTimeout(this.debounceTimer);
    }
    return Promise.resolve();
  }
  getState() {
    var _a, _b;
    return {
      mode: this.mode,
      query: (_b = (_a = this.inputEl) == null ? void 0 : _a.value) != null ? _b : "",
      roleFilter: this.roleFilter,
      sortMode: this.sortMode
    };
  }
  setState(state) {
    var _a;
    if (state.mode) {
      this.mode = state.mode;
      this.syncModeUI();
    }
    if (state.sortMode) {
      this.sortMode = state.sortMode;
    }
    if (state.roleFilter) {
      this.roleFilter = state.roleFilter;
    }
    if (state.query && this.inputEl) {
      this.inputEl.value = state.query;
      this.syncClearButton();
    }
    this.resolveTrackedSession();
    this.updateScopeLabel();
    if (((_a = this.inputEl) == null ? void 0 : _a.value.trim().length) >= 2) {
      this.onQueryChange();
    }
    return Promise.resolve();
  }
  // ── Public API ──
  setMode(mode) {
    if (this.mode === mode) return;
    if (this.mode === "cross-session" && this.resultsEl.childNodes.length > 0) {
      this.cachedCrossResults = activeDocument.createDocumentFragment();
      while (this.resultsEl.firstChild) {
        this.cachedCrossResults.appendChild(this.resultsEl.firstChild);
      }
      this.cachedCrossProgress = this.progressEl.getText();
    }
    this.mode = mode;
    this.abortSearch();
    this.resultsEl.empty();
    this.progressEl.setText("");
    this.activeRowEl = null;
    this.syncModeUI();
    this.resolveTrackedSession();
    this.updateScopeLabel();
    if (this.inputEl.value.trim().length >= 2) {
      if (mode === "cross-session" && this.cachedCrossResults && this.crossCacheKey() === this.cachedCrossKey) {
        this.resultsEl.appendChild(this.cachedCrossResults);
        this.cachedCrossResults = null;
        this.progressEl.setText(this.cachedCrossProgress);
      } else {
        this.onQueryChange();
      }
    }
  }
  /** Invalidate cached entries, forcing a re-scan on next cross-session search. */
  refreshEntries() {
    var _a;
    this.entriesLoaded = false;
    this.entries = [];
    this.cachedCrossResults = null;
    this.cachedCrossKey = "";
    this.updateScopeLabel();
    if (this.mode === "cross-session" && ((_a = this.inputEl) == null ? void 0 : _a.value.trim().length) >= 2) {
      this.onQueryChange();
    }
  }
  onActiveLeafChanged(leaf) {
    if ((leaf == null ? void 0 : leaf.view) instanceof _SearchView) return;
    if (this.mode !== "in-session") return;
    if ((leaf == null ? void 0 : leaf.view) instanceof TimelineView) {
      const session = leaf.view.getSession();
      if ((session == null ? void 0 : session.rawPath) && session.rawPath !== this.trackedFilePath) {
        this.trackedTimelineLeaf = leaf;
        this.trackedSession = session;
        this.trackedFilePath = session.rawPath;
        this.updateScopeLabel();
        this.resultsEl.empty();
        this.progressEl.setText("");
        this.activeRowEl = null;
        if (this.inputEl.value.trim().length >= 2) {
          this.onQueryChange();
        }
      }
    }
  }
  onLayoutChanged() {
    if (this.mode !== "in-session") return;
    if (!this.trackedTimelineLeaf) return;
    const allLeaves = this.app.workspace.getLeavesOfType(VIEW_TYPE_TIMELINE);
    const stillExists = allLeaves.some((l) => l === this.trackedTimelineLeaf);
    if (!stillExists) {
      this.trackedTimelineLeaf = null;
      this.trackedSession = null;
      this.trackedFilePath = null;
      this.updateScopeLabel();
      this.resultsEl.empty();
      this.progressEl.setText("");
      this.activeRowEl = null;
    }
  }
  // ── Mode UI ──
  syncModeUI() {
    if (!this.crossBtn) return;
    this.crossBtn.toggleClass("active", this.mode === "cross-session");
    this.inSessionBtn.toggleClass("active", this.mode === "in-session");
    this.inputEl.setAttribute(
      "placeholder",
      this.mode === "in-session" ? "Search in session..." : "Search across sessions..."
    );
    if (this.refreshBtn) {
      this.refreshBtn.style.display = this.mode === "cross-session" ? "" : "none";
    }
  }
  syncClearButton() {
    if (!this.clearBtn) return;
    const hasContent = this.inputEl.value.length > 0;
    this.clearBtn.toggleClass("is-visible", hasContent);
  }
  showOptionsMenu(e) {
    const menu = new import_obsidian14.Menu();
    menu.addItem((item) => {
      item.setTitle("Filter by role").setIcon("filter").setDisabled(true);
    });
    for (const [value, label] of [["all", "All roles"], ["user", "User only"], ["assistant", "Assistant only"]]) {
      menu.addItem((item) => {
        item.setTitle(label).setChecked(this.roleFilter === value).onClick(() => {
          this.roleFilter = value;
          if (this.inputEl.value.trim().length >= 2) {
            this.onQueryChange();
          }
        });
      });
    }
    menu.addSeparator();
    menu.addItem((item) => {
      item.setTitle("Sort by").setIcon("arrow-up-down").setDisabled(true);
    });
    menu.addItem((item) => {
      item.setTitle("Relevance").setIcon("star").setChecked(this.sortMode === "relevance").onClick(() => {
        this.sortMode = "relevance";
        if (this.inputEl.value.trim().length >= 2) {
          this.onQueryChange();
        }
      });
    });
    menu.addItem((item) => {
      item.setTitle("Chronological").setIcon("clock").setChecked(this.sortMode === "chronological").onClick(() => {
        this.sortMode = "chronological";
        if (this.inputEl.value.trim().length >= 2) {
          this.onQueryChange();
        }
      });
    });
    if (e.clientX === 0 && e.clientY === 0) {
      const rect = this.optionsBtn.getBoundingClientRect();
      menu.showAtPosition({ x: rect.left, y: rect.bottom });
    } else {
      menu.showAtMouseEvent(e);
    }
  }
  resolveTrackedSession() {
    if (this.mode !== "in-session") return;
    const timelineView = this.app.workspace.getActiveViewOfType(TimelineView);
    if (timelineView) {
      const session = timelineView.getSession();
      if (session == null ? void 0 : session.rawPath) {
        this.trackedTimelineLeaf = timelineView.leaf;
        this.trackedSession = session;
        this.trackedFilePath = session.rawPath;
        return;
      }
    }
    if (!this.trackedSession) {
      const leaves = this.app.workspace.getLeavesOfType("claude-sessions-timeline");
      for (const l of leaves) {
        if (l.view instanceof TimelineView) {
          const s = l.view.getSession();
          if (s == null ? void 0 : s.rawPath) {
            this.trackedTimelineLeaf = l;
            this.trackedSession = s;
            this.trackedFilePath = s.rawPath;
            return;
          }
        }
      }
    }
  }
  updateScopeLabel() {
    if (!this.scopeLabel) return;
    if (this.mode === "cross-session") {
      this.scopeLabel.setText(this.entriesLoaded ? `All sessions (${this.entries.length})` : "All sessions");
    } else {
      if (this.trackedSession) {
        const displayName = this.trackedSession.metadata.customTitle || this.trackedSession.metadata.project;
        this.scopeLabel.setText(`Session: ${displayName}`);
      } else {
        this.scopeLabel.setText("No session open");
      }
    }
  }
  // ── Query handling ──
  onQueryChange() {
    this.abortSearch();
    if (this.debounceTimer !== null) {
      window.clearTimeout(this.debounceTimer);
    }
    const text = this.inputEl.value.trim();
    if (text.length < 2) {
      this.resultsEl.empty();
      this.progressEl.setText(text.length === 1 ? "Type at least 2 characters..." : "");
      this.activeRowEl = null;
      return;
    }
    this.debounceTimer = window.setTimeout(() => {
      this.debounceTimer = null;
      if (this.mode === "in-session") {
        void this.executeInSessionSearch(text);
      } else {
        void this.executeCrossSessionSearch(text);
      }
    }, DEBOUNCE_MS);
  }
  abortSearch() {
    if (this.abortController) {
      this.abortController.abort();
      this.abortController = null;
    }
  }
  // ── In-session search ──
  executeInSessionSearch(text) {
    var _a;
    this.abortSearch();
    this.resultsEl.empty();
    this.activeRowEl = null;
    const freshSession = ((_a = this.trackedTimelineLeaf) == null ? void 0 : _a.view) instanceof TimelineView ? this.trackedTimelineLeaf.view.getSession() : null;
    if (freshSession) {
      this.trackedSession = freshSession;
    }
    if (!this.trackedSession) {
      this.progressEl.setText("No session open.");
      return;
    }
    const query = {
      text,
      caseSensitive: false,
      roleFilter: this.roleFilter
    };
    const searchFn = this.sortMode === "relevance" ? searchTurnsRanked : searchTurns;
    const { matches, totalMatches } = searchFn(
      this.trackedSession.turns,
      query,
      SINGLE_SESSION_MAX_MATCHES
    );
    if (matches.length === 0) {
      this.progressEl.setText("No matches found.");
      return;
    }
    const countText = totalMatches === matches.length ? `${totalMatches} match${totalMatches !== 1 ? "es" : ""}` : `${totalMatches} matches (showing ${matches.length})`;
    this.progressEl.setText(countText);
    const visibleCount = Math.min(SINGLE_SESSION_VISIBLE, matches.length);
    for (let i = 0; i < visibleCount; i++) {
      this.renderInSessionMatchRow(matches[i]);
    }
    if (matches.length > SINGLE_SESSION_VISIBLE) {
      const remaining = matches.length - SINGLE_SESSION_VISIBLE;
      const moreBtn = this.resultsEl.createDiv({
        cls: "claude-sessions-search-more-btn",
        text: `+${remaining} more`
      });
      makeClickable(moreBtn, { label: `Show ${remaining} more matches` });
      moreBtn.addEventListener("click", () => {
        moreBtn.remove();
        for (let i = SINGLE_SESSION_VISIBLE; i < matches.length; i++) {
          this.renderInSessionMatchRow(matches[i]);
        }
      });
    }
  }
  renderInSessionMatchRow(match) {
    const session = this.trackedSession;
    if (!session) return;
    const row = this.resultsEl.createDiv({ cls: "claude-sessions-search-match-row" });
    makeClickable(row, { label: "Go to match" });
    row.createSpan({ cls: "claude-sessions-search-match-turn", text: `#${match.turnIndex + 1}` });
    if (match.score !== void 0) {
      const pct = Math.min(100, Math.round(match.score * 20));
      const scoreEl = row.createSpan({
        cls: "claude-sessions-search-match-score",
        attr: { "aria-label": `Relevance: ${pct}%` }
      });
      const bar = scoreEl.createSpan({ cls: "claude-sessions-search-score-bar" });
      bar.style.setProperty("--score-pct", `${pct}%`);
    }
    const meta = row.createSpan({ cls: "claude-sessions-search-match-meta" });
    const roleIcon = match.role === "user" ? "U" : "A";
    const typeLabel = match.toolName || match.blockType;
    meta.setText(`${roleIcon} \xB7 ${typeLabel}`);
    if (match.timestamp) {
      const ts = new Date(match.timestamp);
      if (!isNaN(ts.getTime())) {
        const time = ts.toLocaleTimeString(void 0, { hour: "2-digit", minute: "2-digit", hour12: false });
        row.createSpan({ cls: "claude-sessions-search-match-time", text: time });
      }
    }
    const snippet = row.createDiv({ cls: "claude-sessions-search-snippet" });
    if (match.contextBefore) snippet.createSpan({ text: match.contextBefore });
    snippet.createEl("mark", { text: match.matchText });
    if (match.contextAfter) snippet.createSpan({ text: match.contextAfter });
    row.addEventListener("click", () => {
      this.setActiveRow(row);
      this.navigateToInSessionMatch(match);
    });
  }
  navigateToInSessionMatch(match) {
    if (!this.trackedTimelineLeaf) return;
    const view = this.trackedTimelineLeaf.view;
    if (view instanceof TimelineView) {
      void this.app.workspace.revealLeaf(this.trackedTimelineLeaf);
      view.navigateToMatch(match.turnIndex, match.contentBlockIndex, match.matchText, match.occurrenceInBlock);
    }
  }
  // ── Cross-session search ──
  crossCacheKey() {
    return `${this.inputEl.value.trim()}\0${this.roleFilter}\0${this.sortMode}`;
  }
  async executeCrossSessionSearch(text) {
    this.abortSearch();
    this.resultsEl.empty();
    this.activeRowEl = null;
    if (!this.entriesLoaded) {
      this.progressEl.setText("Scanning session directories...");
      const result = await scanSessionDirs(this.plugin);
      this.entries = result.entries;
      this.entriesLoaded = true;
      this.updateScopeLabel();
    }
    if (this.entries.length === 0) {
      this.progressEl.setText("No sessions found. Check session directories in settings.");
      return;
    }
    const controller = new AbortController();
    this.abortController = controller;
    this.cachedCrossKey = this.crossCacheKey();
    this.cachedCrossResults = null;
    const query = {
      text,
      caseSensitive: false,
      roleFilter: this.roleFilter
    };
    let resultCount = 0;
    const searchFn = this.sortMode === "relevance" ? searchSessionsRanked : searchSessions;
    void searchFn(
      this.entries,
      query,
      (result) => {
        if (controller.signal.aborted) return;
        resultCount++;
        this.renderSessionResult(result, query);
      },
      (searched, total) => {
        if (controller.signal.aborted) return;
        if (searched < total) {
          this.progressEl.setText(`Searching ${searched}/${total} sessions... (${resultCount} with matches)`);
        } else {
          this.progressEl.setText(
            resultCount > 0 ? `Done \u2014 ${resultCount} session${resultCount !== 1 ? "s" : ""} with matches` : "No matches found."
          );
        }
      },
      controller.signal
    );
  }
  renderSessionResult(result, query) {
    const group = this.resultsEl.createDiv({ cls: "claude-sessions-search-session-group" });
    const header = group.createDiv({ cls: "claude-sessions-search-session-header" });
    const displayName = result.entry.customTitle || result.entry.project;
    header.createSpan({ cls: "claude-sessions-search-session-project", text: displayName });
    if (result.entry.date) {
      header.createSpan({ cls: "claude-sessions-search-session-date", text: result.entry.date });
    }
    const pathText = result.entry.cwd ? shortenPath(result.entry.cwd) : "";
    if (pathText) {
      header.createDiv({ cls: "claude-sessions-search-session-path", text: pathText });
    }
    const countText = result.totalMatches === result.matches.length ? `${result.totalMatches} match${result.totalMatches !== 1 ? "es" : ""}` : `${result.totalMatches} matches (showing ${result.matches.length})`;
    header.createSpan({ cls: "claude-sessions-search-match-count", text: countText });
    const visibleCount = Math.min(VISIBLE_MATCHES_PER_SESSION, result.matches.length);
    for (let i = 0; i < visibleCount; i++) {
      this.renderMultiMatchRow(group, result, result.matches[i], query);
    }
    if (result.matches.length > VISIBLE_MATCHES_PER_SESSION) {
      const remaining = result.matches.length - VISIBLE_MATCHES_PER_SESSION;
      const moreBtn = group.createDiv({
        cls: "claude-sessions-search-more-btn",
        text: `+${remaining} more`
      });
      makeClickable(moreBtn, { label: `Show ${remaining} more matches` });
      moreBtn.addEventListener("click", () => {
        moreBtn.remove();
        for (let i = VISIBLE_MATCHES_PER_SESSION; i < result.matches.length; i++) {
          this.renderMultiMatchRow(group, result, result.matches[i], query);
        }
      });
    }
  }
  renderMultiMatchRow(container, result, match, query) {
    const row = container.createDiv({ cls: "claude-sessions-search-match-row" });
    makeClickable(row, { label: "Open match in session" });
    if (match.score !== void 0) {
      const pct = Math.min(100, Math.round(match.score * 20));
      const scoreEl = row.createSpan({
        cls: "claude-sessions-search-match-score",
        attr: { "aria-label": `Relevance: ${pct}%` }
      });
      const bar = scoreEl.createSpan({ cls: "claude-sessions-search-score-bar" });
      bar.style.setProperty("--score-pct", `${pct}%`);
    }
    const meta = row.createSpan({ cls: "claude-sessions-search-match-meta" });
    const roleIcon = match.role === "user" ? "U" : "A";
    const typeLabel = match.toolName || match.blockType;
    meta.setText(`${roleIcon} \xB7 ${typeLabel}`);
    if (match.timestamp) {
      const ts = new Date(match.timestamp);
      if (!isNaN(ts.getTime())) {
        const time = ts.toLocaleTimeString(void 0, { hour: "2-digit", minute: "2-digit", hour12: false });
        row.createSpan({ cls: "claude-sessions-search-match-time", text: time });
      }
    }
    const snippet = row.createDiv({ cls: "claude-sessions-search-snippet" });
    if (match.contextBefore) snippet.createSpan({ text: match.contextBefore });
    snippet.createEl("mark", { text: match.matchText });
    if (match.contextAfter) snippet.createSpan({ text: match.contextAfter });
    row.addEventListener("click", () => {
      this.setActiveRow(row);
      void this.openCrossSessionResult(result.entry, match, query.text);
    });
  }
  async openCrossSessionResult(entry, match, query) {
    try {
      const content = await readFileContent(entry.path);
      const parser = detectParser(content);
      if (!parser) {
        new import_obsidian14.Notice("Could not detect session format.");
        return;
      }
      const session = parser.parse(content, entry.path);
      await resolveSubAgentSessions(session, readFileContent, listDirectoryFiles);
      const turnIndex = resolveMatchTurn(match, session.turns);
      const { matches: precise } = searchTurns(session.turns, { text: query, caseSensitive: false });
      const best = pickBestMatch(precise, turnIndex, match.contextBefore);
      const highlight = best ? { contentBlockIndex: best.contentBlockIndex, text: best.matchText, occurrenceInBlock: best.occurrenceInBlock } : void 0;
      await this.plugin.openSession(session, turnIndex, highlight);
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      new import_obsidian14.Notice(`Failed to load session: ${msg}`);
    }
  }
  // ── Active row tracking ──
  setActiveRow(row) {
    if (this.activeRowEl) {
      this.activeRowEl.removeClass("active");
    }
    row.addClass("active");
    this.activeRowEl = row;
  }
};
function nextFocusable(current, container) {
  const all = Array.from(container.querySelectorAll(".claude-sessions-search-match-row"));
  const idx = all.indexOf(current);
  return idx >= 0 && idx < all.length - 1 ? all[idx + 1] : null;
}
function prevFocusable(current, container) {
  const all = Array.from(container.querySelectorAll(".claude-sessions-search-match-row"));
  const idx = all.indexOf(current);
  return idx > 0 ? all[idx - 1] : null;
}
function pickBestMatch(matches, preferredTurnIndex, contextBefore) {
  if (matches.length === 0) return void 0;
  const sameTurn = matches.filter((m) => m.turnIndex === preferredTurnIndex);
  if (sameTurn.length === 0) return matches[0];
  if (sameTurn.length === 1) return sameTurn[0];
  const suffix = contextBefore.slice(-20).toLowerCase();
  if (suffix) {
    const byContext = sameTurn.find((m) => m.contextBefore.toLowerCase().endsWith(suffix));
    if (byContext) return byContext;
  }
  return sameTurn[0];
}

// src/main.ts
init_session_browser_modal();

// src/views/file-picker-modal.ts
var import_obsidian15 = require("obsidian");
var fs6 = __toESM(require("fs"));
var path3 = __toESM(require("path"));
init_path_utils();
init_streaming_reader();
init_detect();
init_claude_subagent();
var FilePickerModal = class extends import_obsidian15.Modal {
  constructor(app, plugin) {
    super(app);
    this.plugin = plugin;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    this.modalEl.addClass("claude-sessions-file-picker-modal");
    contentEl.createEl("h3", { text: "Import session file" });
    let filePath = "";
    if (!import_obsidian15.Platform.isMobile) {
      const dropZone = contentEl.createDiv({ cls: "claude-sessions-drop-zone" });
      dropZone.setAttribute("tabindex", "0");
      dropZone.setAttribute("role", "button");
      dropZone.setAttribute("aria-label", "Drop or click to select a session file");
      dropZone.addEventListener("keydown", (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          dropZone.click();
        }
      });
      const fileInput = dropZone.createEl("input", { type: "file" });
      fileInput.accept = ".jsonl,.json";
      fileInput.addClass("claude-sessions-drop-zone-input");
      fileInput.addClass("claude-sessions-hidden");
      const label = dropZone.createDiv({ cls: "claude-sessions-drop-zone-label" });
      label.createSpan({ text: "Drop a session file here, or " });
      const browseLink = label.createEl("button", {
        text: "Browse",
        cls: "claude-sessions-drop-zone-browse",
        attr: { "aria-label": "Browse for session file", type: "button" }
      });
      label.createSpan({ text: "." });
      dropZone.createDiv({
        text: ".jsonl or .json",
        cls: "claude-sessions-drop-zone-hint"
      });
      browseLink.addEventListener("click", (e) => {
        e.stopPropagation();
        fileInput.click();
      });
      dropZone.addEventListener("click", () => {
        fileInput.click();
      });
      fileInput.addEventListener("change", () => {
        var _a;
        const file = (_a = fileInput.files) == null ? void 0 : _a[0];
        if (file) void this.importFromFile(file);
      });
      dropZone.addEventListener("dragover", (e) => {
        e.preventDefault();
        e.stopPropagation();
        dropZone.addClass("drag-over");
      });
      dropZone.addEventListener("dragleave", (e) => {
        e.preventDefault();
        e.stopPropagation();
        dropZone.removeClass("drag-over");
      });
      dropZone.addEventListener("drop", (e) => {
        var _a;
        e.preventDefault();
        e.stopPropagation();
        dropZone.removeClass("drag-over");
        const file = (_a = e.dataTransfer) == null ? void 0 : _a.files[0];
        if (file) void this.importFromFile(file);
      });
    }
    if (!import_obsidian15.Platform.isMobile) {
      const divider = contentEl.createDiv({ cls: "claude-sessions-drop-zone-divider" });
      divider.createSpan({ text: "or enter a path" });
    }
    new import_obsidian15.Setting(contentEl).setName("File path").setDesc("Path to a .jsonl session file (supports ~ for home directory).").addText((text) => {
      text.setPlaceholder("~/.claude/projects/myproject/session.jsonl");
      text.onChange((value) => {
        filePath = value;
      });
      text.inputEl.addEventListener("keydown", (e) => {
        if (e.key === "Enter") {
          void this.importFile(filePath);
        }
      });
      window.setTimeout(() => text.inputEl.focus(), 50);
    });
    new import_obsidian15.Setting(contentEl).addButton((btn) => btn.setButtonText("Import").setCta().onClick(() => this.importFile(filePath)));
    if (import_obsidian15.Platform.isMobile) {
      contentEl.createEl("p", {
        text: "On mobile, copy session files into your vault and open them from there.",
        cls: "claude-sessions-mobile-notice"
      });
    }
  }
  onClose() {
    this.contentEl.empty();
  }
  async importFromFile(file) {
    var _a;
    const electronPath = file.path;
    if (electronPath) {
      return this.importFile(electronPath);
    }
    try {
      new import_obsidian15.Notice("Reading file...");
      const content = await file.text();
      const parser = detectParser(content);
      if (!parser) {
        new import_obsidian15.Notice("Could not detect session format. Ensure this is a valid session file.");
        return;
      }
      const fullPath = (_a = this.resolveSessionPath(file.name)) != null ? _a : file.name;
      const session = parser.parse(content, fullPath);
      await resolveSubAgentSessions(session, readFileContent, listDirectoryFiles);
      new import_obsidian15.Notice(`Loaded session with ${session.turns.length} turns.`);
      this.close();
      await this.plugin.openSession(session);
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      new import_obsidian15.Notice(`Import failed: ${msg}`);
    }
  }
  /** Search configured session directories for a file by name. */
  resolveSessionPath(fileName) {
    if (!import_obsidian15.Platform.isDesktop) return null;
    const safeName = path3.basename(fileName);
    for (const dir of this.plugin.settings.sessionDirs) {
      const expanded = expandHome(dir);
      try {
        const subdirs = fs6.readdirSync(expanded, { withFileTypes: true });
        for (const entry of subdirs) {
          if (entry.isDirectory()) {
            const candidate = path3.join(expanded, entry.name, safeName);
            if (fs6.existsSync(candidate)) return candidate;
          }
        }
        const rootCandidate = path3.join(expanded, safeName);
        if (fs6.existsSync(rootCandidate)) return rootCandidate;
      } catch (e) {
      }
    }
    return null;
  }
  async importFile(rawPath) {
    const trimmed = rawPath.trim();
    if (!trimmed) {
      new import_obsidian15.Notice("Please enter a file path.");
      return;
    }
    const expanded = expandHome(trimmed);
    try {
      new import_obsidian15.Notice("Reading file...");
      const content = await readFileContent(expanded);
      const parser = detectParser(content);
      if (!parser) {
        new import_obsidian15.Notice("Could not detect session format. Ensure this is a valid session file.");
        return;
      }
      const session = parser.parse(content, expanded);
      await resolveSubAgentSessions(session, readFileContent, listDirectoryFiles);
      new import_obsidian15.Notice(`Loaded session with ${session.turns.length} turns.`);
      this.close();
      await this.plugin.openSession(session);
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      new import_obsidian15.Notice(`Import failed: ${msg}`);
    }
  }
};

// src/exporters/markdown-exporter.ts
var import_obsidian16 = require("obsidian");
init_constants();
async function exportToMarkdown(app, session, settings) {
  const folder = (0, import_obsidian16.normalizePath)(settings.exportFolder);
  const existing = app.vault.getAbstractFileByPath(folder);
  if (!existing) {
    await app.vault.createFolder(folder);
  } else if (!(existing instanceof import_obsidian16.TFolder)) {
    throw new Error(`${folder} exists but is not a folder.`);
  }
  const safeName = session.metadata.id.replace(/[^a-zA-Z0-9_-]/g, "_").substring(0, 80);
  const images = [];
  const content = buildMarkdown(session, settings, safeName, images);
  if (images.length > 0) {
    const imgFolder = (0, import_obsidian16.normalizePath)(`${folder}/${safeName}`);
    const imgFolderExists = app.vault.getAbstractFileByPath(imgFolder);
    if (!imgFolderExists) {
      await app.vault.createFolder(imgFolder);
    }
    for (const img of images) {
      const imgPath = (0, import_obsidian16.normalizePath)(`${imgFolder}/${img.fileName}`);
      const bytes = base64ToBytes(img.data);
      const existingImg = app.vault.getAbstractFileByPath(imgPath);
      if (existingImg) {
        await app.vault.adapter.writeBinary(imgPath, bytes.buffer);
      } else {
        await app.vault.createBinary(imgPath, bytes.buffer);
      }
    }
  }
  const fileName = (0, import_obsidian16.normalizePath)(`${folder}/${safeName}.md`);
  const existingFile = app.vault.getAbstractFileByPath(fileName);
  if (existingFile) {
    await app.vault.adapter.write(fileName, content);
  } else {
    await app.vault.create(fileName, content);
  }
  return fileName;
}
function base64ToBytes(base64) {
  const binary = atob(base64);
  const bytes = new Uint8Array(binary.length);
  for (let i = 0; i < binary.length; i++) {
    bytes[i] = binary.charCodeAt(i);
  }
  return bytes;
}
function buildMarkdown(session, settings, safeName, images) {
  const lines = [];
  const meta = session.metadata;
  lines.push("---");
  lines.push(`session_id: "${meta.id}"`);
  if (meta.startTime) lines.push(`date: "${meta.startTime}"`);
  if (meta.customTitle) lines.push(`title: "${meta.customTitle}"`);
  lines.push(`project: "${meta.project}"`);
  if (meta.model) lines.push(`model: "${meta.model}"`);
  if (meta.branch) lines.push(`branch: "${meta.branch}"`);
  lines.push(`format: "${meta.format}"`);
  lines.push(`total_turns: ${meta.totalTurns}`);
  if (meta.cwd) lines.push(`cwd: "${meta.cwd}"`);
  if (meta.version) lines.push(`version: "${meta.version}"`);
  lines.push("---");
  lines.push("");
  const displayName = meta.customTitle || meta.project;
  lines.push(`# Session: ${displayName}`);
  lines.push("");
  const toolUseMap = /* @__PURE__ */ new Map();
  for (const turn of session.turns) {
    for (const block of turn.contentBlocks) {
      if (block.type === "tool_use") {
        toolUseMap.set(block.id, block);
      }
    }
  }
  for (const turn of session.turns) {
    lines.push(renderTurn(turn, settings, safeName, images, toolUseMap));
    lines.push("");
  }
  return lines.join("\n");
}
function renderTurn(turn, settings, safeName, images, toolUseMap) {
  const lines = [];
  const roleLabel = turn.role === "user" ? "User" : "Assistant";
  const timeStr = turn.timestamp ? ` (${new Date(turn.timestamp).toLocaleString()})` : "";
  lines.push(`## Turn ${turn.index + 1} \u2014 ${roleLabel}${timeStr}`);
  lines.push("");
  for (const block of turn.contentBlocks) {
    const rendered = renderBlock(block, settings, safeName, images, toolUseMap);
    if (rendered) {
      lines.push(rendered);
      lines.push("");
    }
  }
  return lines.join("\n");
}
function renderBlock(block, settings, safeName, images, toolUseMap) {
  switch (block.type) {
    case "text":
      return block.text;
    case "thinking":
      if (!settings.showThinkingBlocks) return null;
      return [
        "> [!note]- Thinking",
        ...block.thinking.split("\n").map((l) => `> ${l}`)
      ].join("\n");
    case "tool_use":
      if (!settings.showToolCalls) return null;
      return renderToolUse(block);
    case "tool_result":
      if (!settings.showToolResults) return null;
      return renderToolResult2(block, safeName, images, toolUseMap);
    case "image":
      return renderImage(block.mediaType, block.data, safeName, images);
    case "ansi":
      return "```\n" + block.text.replace(ANSI_STRIP_RE, "") + "\n```";
    case "compaction":
      return "---\n*Context compacted*" + (block.summary ? `
${block.summary}` : "") + "\n---";
    case "slash_command":
      return [
        `> [!info]- Slash command: ${block.commandName}`,
        ...block.text.split("\n").map((l) => `> ${l}`)
      ].join("\n");
    case "bash_command": {
      const parts = ["```bash", block.command, "```"];
      if (block.stdout.trim()) {
        parts.push("```", block.stdout, "```");
      }
      if (block.stderr.trim()) {
        parts.push("> **stderr**", ...block.stderr.split("\n").map((l) => `> ${l}`));
      }
      return parts.join("\n");
    }
    default:
      return null;
  }
}
function renderToolUse(block) {
  if (block.name === "Edit") {
    return renderEditToolUse(block);
  }
  return [
    `> [!example]- Tool: ${block.name}`,
    "> ```json",
    ...JSON.stringify(block.input, null, 2).split("\n").map((l) => `> ${l}`),
    "> ```"
  ].join("\n");
}
function renderEditToolUse(block) {
  const filePath = typeof block.input["file_path"] === "string" ? block.input["file_path"] : "";
  const oldStr = typeof block.input["old_string"] === "string" ? block.input["old_string"] : "";
  const newStr = typeof block.input["new_string"] === "string" ? block.input["new_string"] : "";
  const replaceAll = block.input["replace_all"] ? " (replace all)" : "";
  const changes = diffLines(oldStr, newStr);
  const outputLines = [];
  for (const change of changes) {
    const lines = change.value.replace(/\n$/, "").split("\n");
    const prefix = change.added ? "+ " : change.removed ? "- " : "  ";
    for (const line of lines) {
      outputLines.push(prefix + line);
    }
  }
  const header = filePath ? `Edit: ${filePath}${replaceAll}` : `Edit${replaceAll}`;
  return [
    `> [!example]- ${header}`,
    "> ```diff",
    ...outputLines.map((l) => `> ${l}`),
    "> ```"
  ].join("\n");
}
function renderToolResult2(block, safeName, images, toolUseMap) {
  var _a, _b;
  const toolUse = toolUseMap.get(block.toolUseId);
  const toolName = (_b = (_a = block.toolName) != null ? _a : toolUse == null ? void 0 : toolUse.name) != null ? _b : "";
  const label = toolName ? `Result: ${toolName}` : "Tool result";
  const calloutType = block.isError ? "danger" : "success";
  const parts = [];
  if (block.content.trim()) {
    let resultText = block.content.length > 5e3 ? block.content.substring(0, 5e3) + "\n... (truncated)" : block.content;
    if (toolName === "Read" && toolUse) {
      const filePath = typeof toolUse.input["file_path"] === "string" ? toolUse.input["file_path"] : "";
      const lang = langFromPath(filePath);
      resultText = stripLineNumbers(resultText);
      parts.push(
        `> [!${calloutType}]- ${label}`,
        `> ${fence(resultText, lang).split("\n").join("\n> ")}`
      );
    } else {
      parts.push(
        `> [!${calloutType}]- ${label}`,
        "> ```",
        ...resultText.split("\n").map((l) => `> ${l}`),
        "> ```"
      );
    }
  } else {
    parts.push(`> [!${calloutType}]- ${label}`);
  }
  if (block.images && block.images.length > 0) {
    for (const img of block.images) {
      const ref = renderImage(img.mediaType, img.data, safeName, images);
      if (ref) parts.push(">", `> ${ref}`);
    }
  }
  return parts.join("\n");
}
function renderImage(mediaType, data, safeName, images) {
  var _a;
  const ext = ((_a = mediaType.split("/")[1]) == null ? void 0 : _a.replace("jpeg", "jpg")) || "png";
  const idx = images.length + 1;
  const fileName = `image-${idx}.${ext}`;
  images.push({ fileName, data, mediaType });
  return `![image-${idx}](${safeName}/${fileName})`;
}

// src/exporters/html-exporter.ts
var import_obsidian17 = require("obsidian");
var fs7 = __toESM(require("fs"));
var electron = __toESM(require("electron"));

// src/exporters/css-capture.ts
var NEEDED_SELECTORS = [
  "markdown-rendered",
  "token",
  // PrismJS syntax highlighting
  "copy-code-button",
  "svg-icon",
  "language-"
  // code[class*="language-"]
];
function captureThemeVariables() {
  const rootStyle = getComputedStyle(activeDocument.documentElement);
  const bodyStyle = getComputedStyle(activeDocument.body);
  const varNames = /* @__PURE__ */ new Set();
  const sheets = Array.from(activeDocument.styleSheets);
  for (const sheet of sheets) {
    try {
      const rules = Array.from(sheet.cssRules);
      for (const rule of rules) {
        if (rule instanceof CSSStyleRule && rule.style) {
          for (let i = 0; i < rule.style.length; i++) {
            const prop = rule.style[i];
            if (prop.startsWith("--")) varNames.add(prop);
          }
        }
      }
    } catch (e) {
    }
  }
  const declarations = [];
  for (const name of varNames) {
    const val = rootStyle.getPropertyValue(name).trim() || bodyStyle.getPropertyValue(name).trim();
    if (val) {
      declarations.push(`  ${name}: ${val};`);
    }
  }
  return `:root {
${declarations.join("\n")}
}`;
}
function captureMarkdownStyles() {
  var _a, _b;
  const result = [];
  const sheets = Array.from(activeDocument.styleSheets);
  for (const sheet of sheets) {
    if (((_a = sheet.ownerNode) == null ? void 0 : _a.nodeName) !== "LINK") continue;
    if (!((_b = sheet.href) == null ? void 0 : _b.includes("app.css"))) continue;
    try {
      const rules = Array.from(sheet.cssRules);
      for (const rule of rules) {
        const text = rule.cssText;
        if (NEEDED_SELECTORS.some((s) => text.includes(s))) {
          result.push(text);
        }
      }
    } catch (e) {
    }
  }
  return result.join("\n\n");
}
function capturePluginStyles() {
  var _a;
  const result = [];
  const sheets = Array.from(activeDocument.styleSheets);
  for (const sheet of sheets) {
    try {
      const rules = Array.from(sheet.cssRules);
      if ((_a = rules[0]) == null ? void 0 : _a.cssText.includes("claude-sessions")) {
        for (const rule of rules) {
          result.push(rule.cssText);
        }
        break;
      }
    } catch (e) {
    }
  }
  return result.join("\n\n");
}
function captureFontFaces() {
  const result = [];
  const sheets = Array.from(activeDocument.styleSheets);
  for (const sheet of sheets) {
    try {
      const rules = Array.from(sheet.cssRules);
      for (const rule of rules) {
        if (rule instanceof CSSFontFaceRule) {
          const src = rule.style.getPropertyValue("src");
          if (src.includes("data:")) {
            result.push(rule.cssText);
          }
        }
      }
    } catch (e) {
    }
  }
  return result.join("\n\n");
}
function capturePluginThemeOverrides() {
  const container = activeDocument.querySelector(".claude-sessions-timeline-container");
  if (!container) return "";
  const computed = getComputedStyle(container);
  const csVarNames = /* @__PURE__ */ new Set();
  for (const sheet of Array.from(activeDocument.styleSheets)) {
    try {
      for (const rule of Array.from(sheet.cssRules)) {
        if (rule instanceof CSSStyleRule && rule.style) {
          for (let i = 0; i < rule.style.length; i++) {
            const prop = rule.style[i];
            if (prop.startsWith("--cs-")) csVarNames.add(prop);
          }
        }
      }
    } catch (e) {
    }
  }
  if (csVarNames.size === 0) return "";
  const declarations = [];
  for (const name of csVarNames) {
    const val = computed.getPropertyValue(name).trim();
    if (val) {
      declarations.push(`  ${name}: ${val};`);
    }
  }
  return `.claude-sessions-timeline-container {
${declarations.join("\n")}
}`;
}
function captureAllCSS() {
  const sections = [
    "/* === Embedded Fonts === */",
    captureFontFaces(),
    "",
    "/* === Theme Variables === */",
    captureThemeVariables(),
    "",
    "/* === Obsidian Markdown Rendering === */",
    captureMarkdownStyles(),
    "",
    "/* === Plugin Styles === */",
    capturePluginStyles(),
    "",
    "/* === Plugin Theme Overrides (resolved from live container) === */",
    capturePluginThemeOverrides()
  ];
  return sections.join("\n");
}

// src/exporters/standalone-player.ts
function getStandaloneScript() {
  return `(function() {
  'use strict';

  /* \u2500\u2500 Collapsible toggle (turns, tools, tool groups, thinking, sub-agents, summary, slash commands) \u2500\u2500 */
  /*
   * The live view uses CSS class toggling \u2014 NOT display manipulation:
   *   - Turn: "collapsed" class on .claude-sessions-turn
   *   - Everything else: "open" class on the parent container
   * The CSS handles show/hide via these classes.
   */
  function toggleCollapsible(header) {
    var expanded = header.getAttribute('aria-expanded') === 'true';
    header.setAttribute('aria-expanded', String(!expanded));

    /* Turn header: toggle "collapsed" class on turn element */
    var turn = header.closest('.claude-sessions-turn');
    if (turn && header.classList.contains('claude-sessions-turn-header')) {
      turn.classList.toggle('collapsed', expanded); /* expanded=true means collapse */
      return;
    }

    /* All other collapsibles: header is always a direct child of its container.
     * Toggle "open" class on the parent element. */
    var parent = header.parentElement;
    if (parent) {
      parent.classList.toggle('open', !expanded);
    }
  }

  /* \u2500\u2500 Show more / show less \u2500\u2500 */
  function toggleShowMore(btn) {
    var wrap = btn.closest('.claude-sessions-collapsible-wrap');
    if (!wrap) return;
    var collapsed = wrap.classList.toggle('is-collapsed');
    btn.setAttribute('aria-expanded', String(!collapsed));
    var lineCount = btn.getAttribute('data-line-count') || '';
    btn.textContent = collapsed ? 'Show more (' + lineCount + ' lines)' : 'Show less';
  }

  /* \u2500\u2500 Copy to clipboard \u2500\u2500 */
  function copyText(text, btn) {
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(text);
    } else {
      /* Fallback for file:// protocol */
      var ta = document.createElement('textarea');
      ta.value = text;
      ta.style.position = 'fixed';
      ta.style.opacity = '0';
      document.body.appendChild(ta);
      ta.select();
      document.execCommand('copy');
      document.body.removeChild(ta);
    }
    /* Visual feedback */
    if (btn) {
      var origNodes = Array.prototype.slice.call(btn.childNodes);
      btn.textContent = '';
      var svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
      svg.setAttribute('width', '14');
      svg.setAttribute('height', '14');
      svg.setAttribute('viewBox', '0 0 24 24');
      svg.setAttribute('fill', 'none');
      svg.setAttribute('stroke', 'currentColor');
      svg.setAttribute('stroke-width', '2');
      svg.setAttribute('stroke-linecap', 'round');
      svg.setAttribute('stroke-linejoin', 'round');
      var poly = document.createElementNS('http://www.w3.org/2000/svg', 'polyline');
      poly.setAttribute('points', '20 6 9 17 4 12');
      svg.appendChild(poly);
      btn.appendChild(svg);
      setTimeout(function() {
        btn.textContent = '';
        for (var i = 0; i < origNodes.length; i++) btn.appendChild(origNodes[i]);
      }, 1500);
    }
  }

  /* \u2500\u2500 Image modal \u2500\u2500 */
  var modalOverlay = null;

  function openImageModal(src, mime) {
    closeImageModal();
    modalOverlay = document.createElement('div');
    modalOverlay.className = 'claude-sessions-image-modal-overlay';
    modalOverlay.addEventListener('click', function(e) {
      if (e.target === modalOverlay) closeImageModal();
    });

    var container = document.createElement('div');
    container.className = 'claude-sessions-image-modal-container';

    var img = document.createElement('img');
    img.src = src;
    img.style.maxWidth = '100%';
    img.style.maxHeight = 'calc(85vh - 60px)';
    container.appendChild(img);

    var toolbar = document.createElement('div');
    toolbar.className = 'claude-sessions-image-modal-toolbar';

    var dlBtn = document.createElement('a');
    dlBtn.href = src;
    dlBtn.download = 'image.' + (mime || 'png').split('/').pop();
    dlBtn.textContent = 'Download';
    dlBtn.className = 'claude-sessions-image-modal-btn';
    toolbar.appendChild(dlBtn);

    var closeBtn = document.createElement('button');
    closeBtn.textContent = 'Close';
    closeBtn.className = 'claude-sessions-image-modal-btn';
    closeBtn.addEventListener('click', closeImageModal);
    toolbar.appendChild(closeBtn);

    container.appendChild(toolbar);
    modalOverlay.appendChild(container);
    document.body.appendChild(modalOverlay);
  }

  function closeImageModal() {
    if (modalOverlay) {
      modalOverlay.remove();
      modalOverlay = null;
    }
  }

  /* \u2500\u2500 Mermaid diagram modal \u2500\u2500 */
  function openMermaidModal(svgEl) {
    closeImageModal();
    modalOverlay = document.createElement('div');
    modalOverlay.className = 'claude-sessions-image-modal-overlay';
    modalOverlay.addEventListener('click', function(e) {
      if (e.target === modalOverlay) closeImageModal();
    });

    var container = document.createElement('div');
    container.className = 'claude-sessions-image-modal-container';
    container.style.width = '90vw';
    container.style.maxHeight = '90vh';

    /* Clone SVG with ID remapping to avoid style collisions */
    var scrollWrap = document.createElement('div');
    scrollWrap.style.overflow = 'auto';
    scrollWrap.style.flex = '1';
    scrollWrap.style.minHeight = '0';
    scrollWrap.style.width = '100%';
    scrollWrap.style.padding = '16px';
    scrollWrap.style.boxSizing = 'border-box';
    scrollWrap.style.background = 'var(--background-primary, #1e1e1e)';
    scrollWrap.style.borderRadius = 'var(--radius-m, 8px)';
    var serializer = new XMLSerializer();
    var svgString = serializer.serializeToString(svgEl);
    var origId = svgEl.id;
    if (origId) {
      svgString = svgString.split(origId).join('mermaid-preview-' + Date.now());
    }
    var parser = new DOMParser();
    var doc = parser.parseFromString(svgString, 'image/svg+xml');
    var svgNode = document.importNode(doc.documentElement, true);
    svgNode.style.removeProperty('max-width');
    svgNode.removeAttribute('width');
    svgNode.removeAttribute('height');
    svgNode.style.width = '100%';
    svgNode.style.height = 'auto';
    scrollWrap.appendChild(svgNode);
    container.appendChild(scrollWrap);

    var toolbar = document.createElement('div');
    toolbar.className = 'claude-sessions-image-modal-toolbar';

    var dlBtn = document.createElement('button');
    dlBtn.textContent = 'Download SVG';
    dlBtn.className = 'claude-sessions-image-modal-btn';
    dlBtn.addEventListener('click', function() {
      var raw = serializer.serializeToString(svgEl);
      var blob = new Blob([raw], { type: 'image/svg+xml' });
      var url = URL.createObjectURL(blob);
      var a = document.createElement('a');
      a.href = url;
      a.download = 'diagram.svg';
      a.click();
      URL.revokeObjectURL(url);
    });
    toolbar.appendChild(dlBtn);

    var cpBtn = document.createElement('button');
    cpBtn.textContent = 'Copy SVG';
    cpBtn.className = 'claude-sessions-image-modal-btn';
    cpBtn.addEventListener('click', function() {
      var raw = serializer.serializeToString(svgEl);
      copyText(raw, cpBtn);
    });
    toolbar.appendChild(cpBtn);

    var closeBtn = document.createElement('button');
    closeBtn.textContent = 'Close';
    closeBtn.className = 'claude-sessions-image-modal-btn';
    closeBtn.addEventListener('click', closeImageModal);
    toolbar.appendChild(closeBtn);

    container.appendChild(toolbar);
    modalOverlay.appendChild(container);
    document.body.appendChild(modalOverlay);
  }

  /* \u2500\u2500 Content filter menu \u2500\u2500 */
  function buildFilterMenu() {
    var btn = document.getElementById('as-filter-btn');
    if (!btn) return;

    var menu = document.getElementById('as-filter-menu');
    if (menu) { menu.remove(); return; }

    menu = document.createElement('div');
    menu.id = 'as-filter-menu';
    menu.className = 'claude-sessions-filter-menu';

    var filters = [
      { key: 'user', label: 'User', parent: true },
      { key: 'userText', label: 'Text', parent: false, parentKey: 'user' },
      { key: 'userImages', label: 'Images', parent: false, parentKey: 'user' },
      { key: 'assistant', label: 'Assistant', parent: true },
      { key: 'assistantText', label: 'Text', parent: false, parentKey: 'assistant' },
      { key: 'thinking', label: 'Thinking', parent: false, parentKey: 'assistant' },
      { key: 'toolCalls', label: 'Tool calls', parent: false, parentKey: 'assistant' },
      { key: 'toolResults', label: 'Tool results', parent: false, parentKey: 'assistant' },
    ];

    var state = JSON.parse(document.getElementById('as-export-root').getAttribute('data-filters') || '{}');

    filters.forEach(function(f) {
      var row = document.createElement('label');
      row.className = 'claude-sessions-filter-row' + (f.parent ? ' parent' : ' child');
      var cb = document.createElement('input');
      cb.type = 'checkbox';
      cb.checked = state[f.key] !== false;
      cb.addEventListener('change', function() {
        state[f.key] = cb.checked;
        document.getElementById('as-export-root').setAttribute('data-filters', JSON.stringify(state));
        applyFilters(state);
      });
      row.appendChild(cb);
      row.appendChild(document.createTextNode(' ' + f.label));
      menu.appendChild(row);
    });

    btn.parentElement.style.position = 'relative';
    btn.parentElement.appendChild(menu);

    /* Close on outside click */
    setTimeout(function() {
      document.addEventListener('click', function handler(e) {
        if (!menu.contains(e.target) && e.target !== btn) {
          menu.remove();
          document.removeEventListener('click', handler);
        }
      });
    }, 0);
  }

  function applyFilters(state) {
    var root = document.getElementById('as-export-root');
    if (!root) return;
    var FC = 'claude-sessions-filtered';

    function toggleFiltered(el, hidden) {
      if (hidden) el.classList.add(FC); else el.classList.remove(FC);
    }

    /* User role sections */
    root.querySelectorAll('.claude-sessions-role-user').forEach(function(el) {
      toggleFiltered(el, state.user === false);
    });
    if (state.user !== false) {
      root.querySelectorAll('.claude-sessions-user-text').forEach(function(el) {
        var wrapper = el.closest('.claude-sessions-block-wrapper');
        if (wrapper) toggleFiltered(wrapper, state.userText === false);
      });
      root.querySelectorAll('.claude-sessions-slash-command-block').forEach(function(el) {
        var wrapper = el.closest('.claude-sessions-block-wrapper');
        if (wrapper) toggleFiltered(wrapper, state.userText === false);
      });
      root.querySelectorAll('.claude-sessions-image-thumbnail').forEach(function(el) {
        var wrapper = el.closest('.claude-sessions-block-wrapper');
        if (wrapper) toggleFiltered(wrapper, state.userImages === false);
      });
    }

    /* Assistant role sections */
    root.querySelectorAll('.claude-sessions-role-assistant').forEach(function(el) {
      toggleFiltered(el, state.assistant === false);
    });
    if (state.assistant !== false) {
      root.querySelectorAll('.claude-sessions-assistant-text').forEach(function(el) {
        var wrapper = el.closest('.claude-sessions-block-wrapper');
        if (wrapper) toggleFiltered(wrapper, state.assistantText === false);
      });
      root.querySelectorAll('.claude-sessions-thinking-block').forEach(function(el) {
        var wrapper = el.closest('.claude-sessions-block-wrapper');
        if (wrapper) toggleFiltered(wrapper, state.thinking === false);
      });
      root.querySelectorAll('.claude-sessions-tool-block, .claude-sessions-tool-group').forEach(function(el) {
        var wrapper = el.closest('.claude-sessions-block-wrapper');
        if (wrapper) toggleFiltered(wrapper, state.toolCalls === false);
      });
      root.querySelectorAll('.claude-sessions-tool-result').forEach(function(el) {
        toggleFiltered(el, state.toolResults === false);
      });
    }
  }

  /* \u2500\u2500 Event delegation \u2500\u2500 */
  document.addEventListener('click', function(e) {
    var target = e.target;

    /* Mermaid diagram containers */
    var mermaidContainer = target.closest('.claude-sessions-mermaid-container');
    if (mermaidContainer) {
      var svg = mermaidContainer.querySelector('svg');
      if (svg) openMermaidModal(svg);
      return;
    }

    /* Copy buttons \u2014 check before collapsibles so copy buttons inside headers work */
    var copyBtn = target.closest('.claude-sessions-copy-btn, .claude-sessions-text-copy, .claude-sessions-summary-copy, .copy-code-button');
    if (copyBtn) {
      e.preventDefault();
      var text = copyBtn.getAttribute('data-copy-text');
      if (!text) {
        /* Code block copy \u2014 find the <code> sibling */
        var pre = copyBtn.closest('pre');
        if (pre) {
          var code = pre.querySelector('code');
          text = code ? code.textContent : pre.textContent;
        }
      }
      if (text) copyText(text, copyBtn);
      return;
    }

    /* Show more button \u2014 check BEFORE generic collapsible since it also has role="button" */
    var showBtn = target.closest('.claude-sessions-collapsible-toggle');
    if (showBtn) {
      e.preventDefault();
      toggleShowMore(showBtn);
      return;
    }

    /* Walk up to find interactive element */
    var el = target.closest('[role="button"][aria-expanded]');
    if (el && document.getElementById('as-export-root')?.contains(el)) {
      toggleCollapsible(el);
      return;
    }

    /* Image thumbnails \u2014 the thumbnail element IS the <img> */
    var thumb = target.closest('.claude-sessions-image-thumbnail');
    if (thumb) {
      var imgEl = thumb.tagName === 'IMG' ? thumb : thumb.querySelector('img');
      if (imgEl) openImageModal(imgEl.src, imgEl.getAttribute('data-mime'));
      return;
    }

    /* Markdown code/preview toggle buttons */
    var mdBtn = target.closest('.claude-sessions-read-md-btn');
    if (mdBtn) {
      var toggle = mdBtn.closest('.claude-sessions-read-md-toggle');
      if (toggle) {
        var codeView = toggle.querySelector('.claude-sessions-read-md-code');
        var previewView = toggle.querySelector('.claude-sessions-read-md-preview');
        var toggleRow = toggle.querySelector('.claude-sessions-read-md-toggle-row');
        if (codeView && previewView && toggleRow) {
          var btns = toggleRow.querySelectorAll('.claude-sessions-read-md-btn');
          var isCodeBtn = btns[0] === mdBtn;
          btns[0].classList.toggle('is-active', isCodeBtn);
          btns[1].classList.toggle('is-active', !isCodeBtn);
          btns[0].setAttribute('aria-pressed', String(isCodeBtn));
          btns[1].setAttribute('aria-pressed', String(!isCodeBtn));
          codeView.classList.toggle('claude-sessions-read-md-hidden', !isCodeBtn);
          previewView.classList.toggle('claude-sessions-read-md-hidden', isCodeBtn);
        }
      }
      return;
    }

    /* Filter button */
    if (target.closest('#as-filter-btn')) {
      buildFilterMenu();
      return;
    }
  });

  /* Keyboard: Enter/Space on role="button" */
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Enter' || e.key === ' ') {
      var el = e.target;
      if (el && el.getAttribute && el.getAttribute('role') === 'button') {
        e.preventDefault();
        el.click();
      }
    }
    /* Escape closes image modal */
    if (e.key === 'Escape') closeImageModal();
  });

  /* Make all turns visible (no IntersectionObserver in export) */
  document.querySelectorAll('.claude-sessions-turn').forEach(function(t) {
    t.classList.add('visible');
  });
})();`;
}

// src/exporters/html-exporter.ts
var EXPORT_OVERRIDES = `
/* === Standalone Export Overrides === */
html, body {
  margin: 0;
  padding: 0;
  background: var(--background-primary);
  color: var(--text-normal);
  font-family: var(--font-interface, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif);
}

/* Remove Obsidian viewport constraints \u2014 fill the browser window */
.claude-sessions-timeline-container {
  height: auto;
  min-height: 100vh;
}

.claude-sessions-timeline {
  overflow-y: visible;
  padding-bottom: 80px;
}

/* All turns visible (no IntersectionObserver) */
.claude-sessions-turn {
  opacity: 1 !important;
}

/* Export header bar */
.as-export-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 20px;
  background: var(--background-secondary);
  border-bottom: 1px solid var(--background-modifier-border);
  font-family: var(--font-interface, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif);
  font-size: 13px;
  color: var(--text-muted);
  flex-wrap: wrap;
  gap: 8px;
}

.as-export-header-title {
  font-weight: 600;
  color: var(--text-normal);
  font-size: 14px;
}

.as-export-header-meta {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
}

.as-export-header-meta span {
  white-space: nowrap;
}

/* Filter button in header */
#as-filter-btn {
  background: var(--interactive-normal);
  border: 1px solid var(--background-modifier-border);
  color: var(--text-muted);
  padding: 4px 10px;
  border-radius: var(--radius-s);
  cursor: pointer;
  font-size: 12px;
  font-family: var(--font-interface, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif);
}

#as-filter-btn:hover {
  background: var(--interactive-hover);
  color: var(--text-normal);
}

/* Filter dropdown menu */
.claude-sessions-filter-menu {
  position: absolute;
  top: 100%;
  right: 0;
  z-index: 100;
  background: var(--background-secondary);
  border: 1px solid var(--background-modifier-border);
  border-radius: var(--radius-m);
  padding: 8px;
  min-width: 160px;
  box-shadow: var(--shadow-s);
}

.claude-sessions-filter-row {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 4px 6px;
  font-size: 12px;
  color: var(--text-normal);
  cursor: pointer;
  border-radius: var(--radius-s);
}

.claude-sessions-filter-row:hover {
  background: var(--background-modifier-hover);
}

.claude-sessions-filter-row.parent {
  font-weight: 600;
  margin-top: 4px;
}

.claude-sessions-filter-row.parent:first-child {
  margin-top: 0;
}

.claude-sessions-filter-row.child {
  padding-left: 22px;
}

/* Image modal overlay */
.claude-sessions-image-modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.8);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
}

.claude-sessions-image-modal-container {
  max-width: 90vw;
  max-height: 85vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
}

.claude-sessions-image-modal-toolbar {
  display: flex;
  gap: 8px;
}

.claude-sessions-image-modal-btn {
  background: var(--interactive-normal);
  border: 1px solid var(--background-modifier-border);
  color: var(--text-normal);
  padding: 6px 16px;
  border-radius: var(--radius-s);
  cursor: pointer;
  font-size: 13px;
  text-decoration: none;
  font-family: var(--font-interface, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif);
}

.claude-sessions-image-modal-btn:hover {
  background: var(--interactive-hover);
}

/* Hide controls bar \u2014 not applicable in export */
.claude-sessions-controls {
  display: none !important;
}

/* Copy button: ensure the SVG inline icon renders */
.claude-sessions-copy-btn,
.claude-sessions-summary-copy {
  min-width: 24px;
  min-height: 24px;
}
`;
function buildHeaderHTML(session) {
  const m = session.metadata;
  const s = session.stats;
  const model = m.model ? shortModelName(m.model) : "";
  const duration = s.durationMs ? formatElapsed(s.durationMs) : "";
  const date = m.startTime ? new Date(m.startTime).toLocaleDateString(void 0, {
    year: "numeric",
    month: "short",
    day: "numeric",
    hour: "2-digit",
    minute: "2-digit"
  }) : "";
  const metaParts = [];
  if (model) metaParts.push(`<span>Model: <b>${escapeHtml(model)}</b></span>`);
  if (date) metaParts.push(`<span>${escapeHtml(date)}</span>`);
  if (duration) metaParts.push(`<span>Duration: ${escapeHtml(duration)}</span>`);
  metaParts.push(`<span>Turns: ${m.totalTurns}</span>`);
  if (s.costUSD > 0) {
    const cost = s.costUSD >= 1 ? `$${s.costUSD.toFixed(2)}` : `$${s.costUSD.toFixed(3)}`;
    metaParts.push(`<span>Cost: ~${escapeHtml(cost)}</span>`);
  }
  const displayName = m.customTitle || m.project;
  return `<div class="as-export-header">
  <div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap">
    <span class="as-export-header-title">${escapeHtml(displayName)}</span>
    <div class="as-export-header-meta">${metaParts.join("")}</div>
  </div>
  <div style="position:relative">
    <button id="as-filter-btn" aria-label="Content filters">&#x22EF; Filter</button>
  </div>
</div>`;
}
function escapeHtml(s) {
  return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
}
function snapshotTimeline(timelineEl) {
  const clone = timelineEl.cloneNode(true);
  clone.querySelectorAll(".claude-sessions-turn").forEach((turn) => {
    turn.classList.add("visible");
  });
  processCopyButtons(clone, timelineEl);
  clone.querySelectorAll(".claude-sessions-pinned-heroes").forEach((el) => el.remove());
  clone.querySelectorAll(".claude-sessions-heroes-pin").forEach((el) => el.remove());
  clone.querySelectorAll(".claude-sessions-progress-dot").forEach((el) => el.remove());
  return clone.innerHTML;
}
function processCopyButtons(clone, _original) {
  clone.querySelectorAll(".claude-sessions-text-copy").forEach((btn) => {
    var _a;
    const wrapper = btn.closest(".claude-sessions-text-block");
    if (wrapper) {
      const contentEl = wrapper.querySelector(
        ".claude-sessions-collapsible-content, .claude-sessions-user-text, .claude-sessions-assistant-text"
      );
      if (contentEl) {
        btn.setAttribute("data-copy-text", (_a = contentEl.textContent) != null ? _a : "");
        btn.classList.add("claude-sessions-copy-btn");
      }
    }
  });
  clone.querySelectorAll(".claude-sessions-summary-copy").forEach((btn) => {
    var _a;
    const row = btn.closest(".claude-sessions-dash-id-row");
    if (row) {
      const valueEl = row.querySelector(".claude-sessions-dash-id-value");
      if (valueEl && !btn.hasAttribute("data-copy-text")) {
        btn.setAttribute("data-copy-text", (_a = valueEl.textContent) != null ? _a : "");
      }
    }
  });
  clone.querySelectorAll(".claude-sessions-subagent-copy").forEach((btn) => {
    var _a;
    const output = btn.closest(".claude-sessions-subagent-output");
    if (output) {
      const content = output.querySelector(".claude-sessions-subagent-output-content");
      if (content) {
        btn.setAttribute("data-copy-text", (_a = content.textContent) != null ? _a : "");
        btn.classList.add("claude-sessions-copy-btn");
      }
    }
  });
  clone.querySelectorAll(".copy-code-button").forEach((btn) => {
    var _a, _b;
    const pre = btn.closest("pre");
    if (pre) {
      const code = pre.querySelector("code");
      btn.setAttribute("data-copy-text", (_b = (_a = code == null ? void 0 : code.textContent) != null ? _a : pre.textContent) != null ? _b : "");
      btn.classList.add("claude-sessions-copy-btn");
    }
  });
}
async function exportToHTML(timelineEl, session, settings) {
  var _a;
  const notice = new import_obsidian17.Notice("Exporting to HTML...", 0);
  try {
    const css = captureAllCSS();
    const timelineHTML = snapshotTimeline(timelineEl);
    const headerHTML = buildHeaderHTML(session);
    const script = getStandaloneScript();
    const isDark = activeDocument.body.classList.contains("theme-dark");
    const themeClass = isDark ? "theme-dark" : "theme-light";
    const sessionName = session.metadata.customTitle || session.metadata.project;
    const title = `Session: ${sessionName}`;
    const html = `<!DOCTYPE html>
<html lang="en" class="${themeClass}">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>${escapeHtml(title)}</title>
<style>
${css}

${EXPORT_OVERRIDES}
</style>
</head>
<body class="${themeClass}">
<div id="as-export-root" data-filters='{}'>
${headerHTML}
<div class="claude-sessions-timeline-container" style="--max-content-width: ${settings.maxContentWidth > 0 ? settings.maxContentWidth + "px" : "none"}">
<div class="claude-sessions-timeline markdown-rendered">
${timelineHTML}
</div>
</div>
</div>
<script>
${script}
<\/script>
</body>
</html>`;
    const safeName = session.metadata.id.replace(/[^a-zA-Z0-9_-]/g, "_").substring(0, 80);
    let savePath = null;
    try {
      const dialog = (_a = electron.remote) == null ? void 0 : _a.dialog;
      if (dialog) {
        const result = await dialog.showSaveDialog({
          title: "Export session as HTML",
          defaultPath: `${safeName}.html`,
          filters: [{ name: "HTML", extensions: ["html"] }]
        });
        if (result.canceled || !result.filePath) {
          notice.hide();
          return;
        }
        savePath = result.filePath;
      }
    } catch (e) {
    }
    if (!savePath) {
      const dir = session.rawPath.replace(/[/\\][^/\\]+$/, "");
      savePath = `${dir}/${safeName}.html`;
    }
    fs7.writeFileSync(savePath, html, "utf-8");
    notice.hide();
    new import_obsidian17.Notice(`Exported to ${savePath}`, 5e3);
  } catch (e) {
    notice.hide();
    const msg = e instanceof Error ? e.message : String(e);
    new import_obsidian17.Notice(`HTML export failed: ${msg}`);
    throw e;
  }
}

// src/main.ts
init_streaming_reader();
init_detect();
init_claude_subagent();
init_path_utils();

// src/utils/session-index.ts
var fs8 = __toESM(require("fs"));
var path4 = __toESM(require("path"));
var INDEX_VERSION = 2;
var INDEX_FILENAME = "session-index.json";
var SessionIndex = class {
  constructor(vaultBasePath, configDir) {
    this.entries = {};
    this.loaded = false;
    this.indexPath = path4.join(
      vaultBasePath,
      configDir,
      "plugins",
      "claude-sessions",
      INDEX_FILENAME
    );
  }
  load() {
    if (this.loaded) return;
    this.loaded = true;
    try {
      const raw = fs8.readFileSync(this.indexPath, "utf-8");
      const data = JSON.parse(raw);
      if (data.version === INDEX_VERSION && data.entries) {
        this.entries = data.entries;
      }
    } catch (e) {
      this.entries = {};
    }
  }
  get(filePath, mtime) {
    const cached2 = this.entries[filePath];
    if (cached2 && cached2.mtime === mtime) return cached2;
    return null;
  }
  set(filePath, meta) {
    this.entries[filePath] = meta;
  }
  prune(validPaths) {
    let pruned = 0;
    for (const key of Object.keys(this.entries)) {
      if (!validPaths.has(key)) {
        delete this.entries[key];
        pruned++;
      }
    }
    return pruned;
  }
  save() {
    const data = {
      version: INDEX_VERSION,
      entries: this.entries
    };
    try {
      fs8.writeFileSync(this.indexPath, JSON.stringify(data), "utf-8");
    } catch (e) {
    }
  }
};

// src/main.ts
init_logger();

// src/api.ts
var sessionParsedCallbacks = /* @__PURE__ */ new Set();
function buildAPI(plugin) {
  let ClaudeParser2 = null;
  return {
    getActiveSession() {
      var _a;
      const view = plugin.getActiveTimelineView();
      return (_a = view == null ? void 0 : view.getSession()) != null ? _a : null;
    },
    async parseSessionFile(path5) {
      if (!ClaudeParser2) {
        const mod = await Promise.resolve().then(() => (init_claude_parser(), claude_parser_exports));
        ClaudeParser2 = mod.ClaudeParser;
      }
      const content = await plugin.app.vault.adapter.read(path5);
      const parser = new ClaudeParser2();
      return parser.parse(content, path5);
    },
    onSessionParsed(callback) {
      sessionParsedCallbacks.add(callback);
      return () => {
        sessionParsedCallbacks.delete(callback);
      };
    },
    async getSessionIndex() {
      const { scanSessionDirs: scanSessionDirs2 } = await Promise.resolve().then(() => (init_session_browser_modal(), session_browser_modal_exports));
      const result = await scanSessionDirs2(plugin);
      return result.entries;
    }
  };
}

// src/distill/distill-session.ts
var import_obsidian19 = require("obsidian");

// src/distill/types.ts
var DISTILL_SCHEMA_VERSION = 1;

// src/distill/extract-frontmatter.ts
init_path_utils();
var FILE_PATH_TOOLS = ["Read", "Edit", "Write"];
var PATH_TOOLS = ["Glob", "Grep"];
function extractFrontmatter(session, vaultRoot) {
  const { metadata, stats } = session;
  const filesTouched = /* @__PURE__ */ new Set();
  let errorCount = 0;
  for (const turn of session.turns) {
    const result = collectFilesAndCountErrors(turn, metadata.cwd, vaultRoot, filesTouched);
    errorCount += result.errorCount;
  }
  const frontmatter = {
    tags: ["claude-session"],
    session_id: metadata.id,
    schema_version: DISTILL_SCHEMA_VERSION,
    project: metadata.project,
    cwd: metadata.cwd
  };
  if (metadata.branch) frontmatter.branch = metadata.branch;
  if (metadata.model) frontmatter.model = metadata.model;
  if (metadata.customTitle) frontmatter.title = metadata.customTitle;
  if (metadata.startTime) frontmatter.start_time = metadata.startTime;
  if (stats.durationMs > 0) {
    frontmatter.duration_min = Math.round(stats.durationMs / 6e4 * 10) / 10;
  }
  if (stats.costUSD > 0) {
    frontmatter.cost_usd = Math.round(stats.costUSD * 100) / 100;
  }
  if (stats.contextWindowTokens > 0) frontmatter.context_tokens = stats.contextWindowTokens;
  if (stats.inputTokens > 0) frontmatter.input_tokens = stats.inputTokens;
  if (stats.outputTokens > 0) frontmatter.output_tokens = stats.outputTokens;
  if (stats.cacheReadTokens > 0) frontmatter.cache_read_tokens = stats.cacheReadTokens;
  if (stats.userTurns > 0) frontmatter.user_turns = stats.userTurns;
  if (stats.assistantTurns > 0) frontmatter.assistant_turns = stats.assistantTurns;
  const toolNames = Object.keys(stats.toolUseCounts).sort();
  if (toolNames.length > 0) frontmatter.tools_used = toolNames;
  if (filesTouched.size > 0) {
    frontmatter.files_touched = Array.from(filesTouched).sort().map((f) => `[[${f}]]`);
  }
  if (errorCount > 0) {
    frontmatter.error_count = errorCount;
  }
  frontmatter.source_path = session.rawPath;
  frontmatter.obsidian_uri = `obsidian://claude-sessions?session=${encodeURIComponent(session.rawPath)}`;
  return frontmatter;
}
function collectFilesAndCountErrors(turn, cwd, vaultRoot, filesTouched) {
  let errorCount = 0;
  for (const block of turn.contentBlocks) {
    if (block.type === "tool_use") {
      collectFilesFromToolUse(block, cwd, vaultRoot, filesTouched);
    } else if (block.type === "tool_result" && block.isError) {
      errorCount++;
    }
  }
  return { errorCount };
}
function collectFilesFromToolUse(block, cwd, vaultRoot, filesTouched) {
  const input = block.input;
  if (FILE_PATH_TOOLS.includes(block.name)) {
    const filePath = input["file_path"];
    if (typeof filePath === "string" && filePath) {
      const relative = toVaultRelativePath(filePath, cwd, vaultRoot);
      filesTouched.add(relative);
    }
  }
  if (PATH_TOOLS.includes(block.name)) {
    const path5 = input["path"];
    if (typeof path5 === "string" && path5) {
      const relative = toVaultRelativePath(path5, cwd, vaultRoot);
      filesTouched.add(relative);
    }
  }
  if (block.name === "Write") {
    const filePath = input["file_path"];
    if (typeof filePath === "string" && filePath) {
      const relative = toVaultRelativePath(filePath, cwd, vaultRoot);
      filesTouched.add(relative);
    }
  }
}
function toVaultRelativePath(absolutePath, cwd, vaultRoot) {
  if (vaultRoot && absolutePath.startsWith(vaultRoot + "/")) {
    return absolutePath.slice(vaultRoot.length + 1);
  }
  if (absolutePath.startsWith(cwd + "/")) {
    return absolutePath.slice(cwd.length + 1);
  }
  return basename(absolutePath);
}

// src/distill/serialize-frontmatter.ts
function serializeFrontmatter(fm) {
  const lines = [];
  lines.push("tags:");
  for (const tag of fm.tags) {
    lines.push(`  - ${tag}`);
  }
  lines.push(`session_id: ${quote(fm.session_id)}`);
  lines.push(`schema_version: ${fm.schema_version}`);
  lines.push("");
  lines.push(`project: ${quote(fm.project)}`);
  lines.push(`cwd: ${quote(fm.cwd)}`);
  if (fm.branch) lines.push(`branch: ${quote(fm.branch)}`);
  if (fm.model) lines.push(`model: ${quote(fm.model)}`);
  if (fm.title) lines.push(`title: ${quote(fm.title)}`);
  if (fm.start_time || fm.duration_min !== void 0) {
    lines.push("");
    if (fm.start_time) lines.push(`start_time: ${fm.start_time}`);
    if (fm.duration_min !== void 0) lines.push(`duration_min: ${fm.duration_min}`);
  }
  if (fm.cost_usd !== void 0 || fm.input_tokens !== void 0) {
    lines.push("");
    if (fm.cost_usd !== void 0) lines.push(`cost_usd: ${fm.cost_usd}`);
    if (fm.context_tokens !== void 0) lines.push(`context_tokens: ${fm.context_tokens}`);
    if (fm.input_tokens !== void 0) lines.push(`input_tokens: ${fm.input_tokens}`);
    if (fm.output_tokens !== void 0) lines.push(`output_tokens: ${fm.output_tokens}`);
    if (fm.cache_read_tokens !== void 0) lines.push(`cache_read_tokens: ${fm.cache_read_tokens}`);
  }
  if (fm.user_turns !== void 0 || fm.assistant_turns !== void 0) {
    lines.push("");
    if (fm.user_turns !== void 0) lines.push(`user_turns: ${fm.user_turns}`);
    if (fm.assistant_turns !== void 0) lines.push(`assistant_turns: ${fm.assistant_turns}`);
  }
  if (fm.tools_used && fm.tools_used.length > 0) {
    lines.push(`tools_used: [${fm.tools_used.join(", ")}]`);
  }
  if (fm.files_touched && fm.files_touched.length > 0) {
    lines.push("files_touched:");
    for (const file of fm.files_touched) {
      lines.push(`  - ${quote(file)}`);
    }
  }
  if (fm.error_count !== void 0 && fm.error_count > 0) {
    lines.push(`error_count: ${fm.error_count}`);
  }
  if (fm.session_type && fm.session_type.length > 0) {
    lines.push("session_type:");
    for (const type of fm.session_type) {
      lines.push(`  - ${type}`);
    }
  }
  if (fm.source_path) {
    lines.push("");
    lines.push(`source_path: ${quote(fm.source_path)}`);
  }
  if (fm.obsidian_uri) {
    lines.push(`obsidian_uri: ${quote(fm.obsidian_uri)}`);
  }
  return lines.join("\n");
}
function quote(value) {
  const needsQuote = value === "" || value.startsWith(" ") || value.endsWith(" ") || /[:#[\]{}|>&*!?'"\n\r\t\\]/.test(value) || /^[\d.+-]/.test(value) || // Could be parsed as number
  ["true", "false", "null", "yes", "no", "on", "off"].includes(value.toLowerCase());
  if (!needsQuote) {
    return value;
  }
  const escaped = value.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/\t/g, "\\t");
  return `"${escaped}"`;
}
function wrapFrontmatter(yaml) {
  return `---
${yaml}
---`;
}

// src/distill/build-note.ts
function buildNoteName(fm) {
  const project = fm.project.replace(/[^a-zA-Z0-9]+/g, "-").replace(/^-|-$/g, "").toLowerCase() || "unknown";
  let dateStr = "undated";
  if (fm.start_time) {
    const parsed = new Date(fm.start_time);
    if (!isNaN(parsed.getTime())) {
      dateStr = parsed.toISOString().slice(0, 10);
    }
  }
  const shortId = fm.session_id.slice(0, 8);
  return `${project}--${dateStr}--${shortId}.md`;
}
function buildLayer0Note(fm) {
  var _a, _b, _c, _d;
  const parts = [];
  parts.push(wrapFrontmatter(serializeFrontmatter(fm)));
  parts.push("");
  parts.push("## Stats");
  parts.push("");
  parts.push("| Metric | Value |");
  parts.push("|---|---|");
  if (fm.duration_min !== void 0) {
    parts.push(`| Duration | ${formatDuration2(fm.duration_min)} |`);
  }
  if (fm.cost_usd !== void 0) {
    parts.push(`| Cost | $${fm.cost_usd.toFixed(2)} |`);
  }
  if (fm.user_turns !== void 0 || fm.assistant_turns !== void 0) {
    const user = (_a = fm.user_turns) != null ? _a : 0;
    const assistant = (_b = fm.assistant_turns) != null ? _b : 0;
    parts.push(`| Turns | ${user} user / ${assistant} assistant |`);
  }
  if (fm.tools_used && fm.tools_used.length > 0) {
    parts.push(`| Tools | ${fm.tools_used.join(", ")} |`);
  }
  if (fm.input_tokens !== void 0 || fm.output_tokens !== void 0) {
    const input = (_c = fm.input_tokens) != null ? _c : 0;
    const output = (_d = fm.output_tokens) != null ? _d : 0;
    const total = Math.round((input + output) / 1e3);
    parts.push(`| Tokens | ${total}k total |`);
  }
  parts.push("");
  return parts.join("\n");
}
function formatDuration2(minutes) {
  if (minutes < 1) {
    return `${Math.round(minutes * 60)} sec`;
  }
  if (minutes < 60) {
    return `${Math.round(minutes)} min`;
  }
  const hours = Math.floor(minutes / 60);
  const mins = Math.round(minutes % 60);
  return mins > 0 ? `${hours}h ${mins}m` : `${hours}h`;
}
function mergeNoteContent(layer0Content, llmContent) {
  const layer0Fm = extractFrontmatterSection(layer0Content);
  const llmFm = extractFrontmatterSection(llmContent);
  if (!llmFm || !llmContent.includes("## Summary")) {
    return layer0Content;
  }
  if (!layer0Fm) {
    return llmContent;
  }
  const layer0Fields = parseFrontmatter(layer0Fm);
  const llmFields = parseFrontmatter(llmFm);
  const merged = mergeFrontmatterFields(layer0Fields, llmFields);
  const llmBody = extractBody(llmContent);
  return `---
${merged}
---
${llmBody}`;
}
function parseFrontmatter(yaml) {
  var _a, _b;
  const fields = /* @__PURE__ */ new Map();
  const lines = yaml.split("\n");
  let currentKey = null;
  let currentArray = null;
  for (const line of lines) {
    if (line.match(/^\s+-\s+/)) {
      const value = line.replace(/^\s+-\s+/, "").trim();
      if (currentArray && currentKey) {
        currentArray.push(unquote(value));
      }
      continue;
    }
    if (currentArray && currentKey) {
      fields.set(currentKey, currentArray);
      currentArray = null;
      currentKey = null;
    }
    const match = line.match(/^([a-z_]+):\s*(.*)?$/);
    if (match) {
      const key = match[1];
      const value = (_b = (_a = match[2]) == null ? void 0 : _a.trim()) != null ? _b : "";
      if (value === "" || value.startsWith("[")) {
        if (value.startsWith("[") && value.endsWith("]")) {
          const items = value.slice(1, -1).split(",").map((s) => s.trim());
          fields.set(key, items);
        } else {
          currentKey = key;
          currentArray = [];
        }
      } else {
        fields.set(key, unquote(value));
      }
    }
  }
  if (currentArray && currentKey) {
    fields.set(currentKey, currentArray);
  }
  return fields;
}
function unquote(value) {
  if (value.startsWith('"') && value.endsWith('"') || value.startsWith("'") && value.endsWith("'")) {
    return value.slice(1, -1).replace(/\\"/g, '"').replace(/\\n/g, "\n");
  }
  return value;
}
function mergeFrontmatterFields(layer0, llm) {
  const merged = /* @__PURE__ */ new Map();
  for (const [key, value] of layer0) {
    merged.set(key, value);
  }
  const llmOnlyFields = ["session_type"];
  for (const key of llmOnlyFields) {
    if (llm.has(key)) {
      merged.set(key, llm.get(key));
    }
  }
  const mergeArrayFields = ["tags", "files_touched"];
  for (const key of mergeArrayFields) {
    const layer0Val = layer0.get(key);
    const llmVal = llm.get(key);
    if (layer0Val && llmVal) {
      const layer0Arr = Array.isArray(layer0Val) ? layer0Val : [layer0Val];
      const llmArr = Array.isArray(llmVal) ? llmVal : [llmVal];
      const combined = [.../* @__PURE__ */ new Set([...layer0Arr, ...llmArr])].sort();
      merged.set(key, combined);
    } else if (llmVal) {
      merged.set(key, llmVal);
    }
  }
  return serializeMergedFrontmatter(merged);
}
function serializeMergedFrontmatter(fields) {
  const lines = [];
  const orderedKeys = [
    "tags",
    "session_id",
    "schema_version",
    "project",
    "cwd",
    "branch",
    "model",
    "title",
    "start_time",
    "duration_min",
    "cost_usd",
    "context_tokens",
    "input_tokens",
    "output_tokens",
    "cache_read_tokens",
    "user_turns",
    "assistant_turns",
    "tools_used",
    "files_touched",
    "error_count",
    "session_type",
    "source_path",
    "obsidian_uri"
  ];
  const output = /* @__PURE__ */ new Set();
  for (const key of orderedKeys) {
    if (!fields.has(key)) continue;
    output.add(key);
    const value = fields.get(key);
    lines.push(formatField(key, value));
    const sectionEnds = ["schema_version", "duration_min", "cache_read_tokens", "assistant_turns", "session_type"];
    const isProjectSectionEnd = key === "title" || key === "model" && !fields.has("title");
    if (sectionEnds.includes(key) || isProjectSectionEnd) {
      lines.push("");
    }
  }
  for (const [key, value] of fields) {
    if (output.has(key)) continue;
    lines.push(formatField(key, value));
  }
  return lines.join("\n");
}
function formatField(key, value) {
  if (Array.isArray(value)) {
    if (key === "tools_used") {
      return `${key}: [${value.join(", ")}]`;
    }
    return `${key}:
${value.map((v) => `  - ${quoteIfNeeded(v)}`).join("\n")}`;
  }
  return `${key}: ${quoteIfNeeded(value)}`;
}
function quoteIfNeeded(value) {
  if (typeof value !== "string") return String(value);
  if (/^-?\d+(\.\d+)?$/.test(value)) return value;
  const needsQuote = value === "" || value.startsWith(" ") || value.endsWith(" ") || /[:#[\]{}|>&*!?'"\n\r\t\\]/.test(value) || ["true", "false", "null", "yes", "no", "on", "off"].includes(value.toLowerCase());
  if (!needsQuote) return value;
  const escaped = value.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\n/g, "\\n");
  return `"${escaped}"`;
}
function extractFrontmatterSection(content) {
  const match = content.match(/^---\n([\s\S]*?)\n---/);
  return match ? match[1] : null;
}
function extractBody(content) {
  const match = content.match(/^---\n[\s\S]*?\n---\n?([\s\S]*)$/);
  return match ? match[1] : content;
}

// src/distill/find-existing.ts
var import_obsidian18 = require("obsidian");
function findExistingNote(app, distillFolder, sessionId) {
  const folder = app.vault.getAbstractFileByPath((0, import_obsidian18.normalizePath)(distillFolder));
  if (!folder || !(folder instanceof import_obsidian18.TFolder)) {
    return null;
  }
  for (const file of folder.children) {
    if (!(file instanceof import_obsidian18.TFile) || file.extension !== "md") {
      continue;
    }
    const cache = app.metadataCache.getFileCache(file);
    const frontmatter = cache == null ? void 0 : cache.frontmatter;
    if (frontmatter && frontmatter["session_id"] === sessionId) {
      return file.path;
    }
  }
  return null;
}
async function readExistingNote(app, path5) {
  const file = app.vault.getAbstractFileByPath(path5);
  if (!(file instanceof import_obsidian18.TFile)) {
    return null;
  }
  return app.vault.read(file);
}

// src/distill/distill-session.ts
async function distillSession(app, session, distillFolder, options = {}) {
  try {
    const adapter = app.vault.adapter;
    const vaultRoot = adapter.basePath;
    const frontmatter = extractFrontmatter(session, vaultRoot);
    const existingPath = findExistingNote(app, distillFolder, frontmatter.session_id);
    let noteContent;
    let updated = false;
    if (existingPath && !options.force) {
      const existingContent = await readExistingNote(app, existingPath);
      if (existingContent) {
        const newContent = buildLayer0Note(frontmatter);
        noteContent = mergeNoteContent(newContent, existingContent);
        updated = true;
      } else {
        noteContent = buildLayer0Note(frontmatter);
      }
    } else {
      noteContent = buildLayer0Note(frontmatter);
    }
    if (options.dryRun) {
      return {
        success: true,
        content: noteContent,
        updated
      };
    }
    await ensureFolder(app, distillFolder);
    const noteName = buildNoteName(frontmatter);
    const notePath = (0, import_obsidian19.normalizePath)(`${distillFolder}/${noteName}`);
    const existingFile = app.vault.getAbstractFileByPath(notePath);
    if (existingFile instanceof import_obsidian19.TFile) {
      await app.vault.modify(existingFile, noteContent);
      updated = true;
    } else {
      await app.vault.create(notePath, noteContent);
    }
    return {
      success: true,
      notePath,
      updated
    };
  } catch (err) {
    return {
      success: false,
      updated: false,
      error: err instanceof Error ? err.message : String(err)
    };
  }
}
async function mergeWithClipboardContent(app, session, clipboardContent, distillFolder) {
  try {
    if (!clipboardContent.includes("---") || !clipboardContent.includes("## Summary")) {
      return {
        success: false,
        updated: false,
        error: "Clipboard does not contain a valid /distill output (missing frontmatter or ## Summary)"
      };
    }
    const adapter = app.vault.adapter;
    const vaultRoot = adapter.basePath;
    const layer0Frontmatter = extractFrontmatter(session, vaultRoot);
    const layer0Note = buildLayer0Note(layer0Frontmatter);
    const mergedContent = mergeNoteContent(layer0Note, clipboardContent);
    await ensureFolder(app, distillFolder);
    const noteName = buildNoteName(layer0Frontmatter);
    const notePath = (0, import_obsidian19.normalizePath)(`${distillFolder}/${noteName}`);
    const existingFile = app.vault.getAbstractFileByPath(notePath);
    if (existingFile instanceof import_obsidian19.TFile) {
      await app.vault.modify(existingFile, mergedContent);
    } else {
      await app.vault.create(notePath, mergedContent);
    }
    return {
      success: true,
      notePath,
      updated: existingFile instanceof import_obsidian19.TFile
    };
  } catch (err) {
    return {
      success: false,
      updated: false,
      error: err instanceof Error ? err.message : String(err)
    };
  }
}
async function ensureFolder(app, folderPath) {
  const normalized = (0, import_obsidian19.normalizePath)(folderPath);
  const existing = app.vault.getAbstractFileByPath(normalized);
  if (existing) {
    if (!(existing instanceof import_obsidian19.TFolder)) {
      throw new Error(`${folderPath} exists but is not a folder`);
    }
    return;
  }
  await app.vault.createFolder(normalized);
}

// src/distill/bases-templates.ts
var import_obsidian20 = require("obsidian");
var BASES_TEMPLATES = [
  {
    name: "Session Dashboard",
    fileName: "Session Dashboard.base",
    content: `filters:
  file.hasTag("claude-session")
formulas:
  cost_display: 'if(cost_usd, "$" + cost_usd.toFixed(2), "")'
  tokens_k: '((input_tokens + output_tokens) / 1000).round(0)'
views:
  - type: table
    name: "All Sessions"
    order: [project, file.name, formula.cost_display, formula.tokens_k, duration_min, model, session_type]
    summaries:
      cost_usd: Sum
      duration_min: Sum
`
  },
  {
    name: "Cost Tracker",
    fileName: "Cost Tracker.base",
    content: `filters:
  file.hasTag("claude-session")
formulas:
  cost_display: 'if(cost_usd, "$" + cost_usd.toFixed(2), "")'
views:
  - type: table
    name: "By Project"
    order: [project, formula.cost_display, input_tokens, output_tokens, cache_read_tokens, model]
    groupBy:
      property: project
      direction: ASC
    summaries:
      cost_usd: Sum
`
  },
  {
    name: "Recent Sessions",
    fileName: "Recent Sessions.base",
    content: `filters:
  and:
    - file.hasTag("claude-session")
    - '(now() - date(start_time)).days <= 7'
views:
  - type: table
    name: "Last 7 Days"
    order: [project, file.name, start_time, duration_min, cost_usd, session_type]
`
  },
  {
    name: "Error Patterns",
    fileName: "Error Patterns.base",
    content: `filters:
  and:
    - file.hasTag("claude-session")
    - 'error_count > 0'
views:
  - type: table
    name: "Sessions with Errors"
    order: [project, file.name, error_count, start_time, session_type]
`
  }
];
async function installBasesTemplates(app, basesFolder, force = false) {
  const result = {
    installed: [],
    skipped: [],
    failed: []
  };
  const normalized = (0, import_obsidian20.normalizePath)(basesFolder);
  const existing = app.vault.getAbstractFileByPath(normalized);
  if (!existing) {
    await app.vault.createFolder(normalized);
  } else if (!(existing instanceof import_obsidian20.TFolder)) {
    return {
      installed: [],
      skipped: [],
      failed: [{ name: basesFolder, error: "Path exists but is not a folder" }]
    };
  }
  for (const template of BASES_TEMPLATES) {
    const filePath = (0, import_obsidian20.normalizePath)(`${basesFolder}/${template.fileName}`);
    try {
      const existingFile = app.vault.getAbstractFileByPath(filePath);
      if (existingFile && !force) {
        result.skipped.push(template.name);
        continue;
      }
      if (existingFile) {
        await app.vault.adapter.write(filePath, template.content);
      } else {
        await app.vault.create(filePath, template.content);
      }
      result.installed.push(template.name);
    } catch (err) {
      result.failed.push({
        name: template.name,
        error: err instanceof Error ? err.message : String(err)
      });
    }
  }
  return result;
}

// src/main.ts
var ClaudeSessionsPlugin = class extends import_obsidian21.Plugin {
  constructor() {
    super(...arguments);
    this.settings = DEFAULT_SETTINGS;
  }
  async onload() {
    (0, import_obsidian21.addIcon)("claude-sparkle", '<g transform="translate(50,50)" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"><line y1="-12" y2="-44"/><line y1="-12" y2="-44" transform="rotate(45)"/><line y1="-12" y2="-44" transform="rotate(90)"/><line y1="-12" y2="-44" transform="rotate(135)"/><line y1="-12" y2="-44" transform="rotate(180)"/><line y1="-12" y2="-44" transform="rotate(225)"/><line y1="-12" y2="-44" transform="rotate(270)"/><line y1="-12" y2="-44" transform="rotate(315)"/></g>');
    await this.loadSettings();
    Logger.init(this.settings);
    const adapter = this.app.vault.adapter;
    this.sessionIndex = new SessionIndex(adapter.basePath, this.app.vault.configDir);
    this.api = buildAPI(this);
    this.registerView(VIEW_TYPE_TIMELINE, (leaf) => {
      return new TimelineView(leaf, this.settings);
    });
    this.registerView(VIEW_TYPE_SEARCH, (leaf) => {
      return new SearchView(leaf, this);
    });
    this.registerEvent(
      this.app.workspace.on("active-leaf-change", (leaf) => {
        const searchLeaves = this.app.workspace.getLeavesOfType(VIEW_TYPE_SEARCH);
        for (const sl of searchLeaves) {
          if (sl.view instanceof SearchView) {
            sl.view.onActiveLeafChanged(leaf);
          }
        }
      })
    );
    this.registerEvent(
      this.app.workspace.on("layout-change", () => {
        const searchLeaves = this.app.workspace.getLeavesOfType(VIEW_TYPE_SEARCH);
        for (const sl of searchLeaves) {
          if (sl.view instanceof SearchView) {
            sl.view.onLayoutChanged();
          }
        }
      })
    );
    this.addSettingTab(new SettingsTab(this.app, this));
    this.addCommand({
      id: "browse-sessions",
      name: "Browse sessions",
      callback: async () => {
        new import_obsidian21.Notice("Scanning session directories...");
        const result = await scanSessionDirs(this);
        if (result.entries.length === 0) {
          new import_obsidian21.Notice("No sessions found. Check your session directories in settings.");
          return;
        }
        if (result.updated === result.total) {
          new import_obsidian21.Notice(`Indexed ${result.total} sessions.`);
        } else {
          new import_obsidian21.Notice(`Found ${result.total} sessions (${result.updated} updated).`);
        }
        new SessionBrowserModal(this.app, this, result.entries).open();
      }
    });
    this.addCommand({
      id: "search-sessions",
      name: "Search sessions",
      callback: async () => {
        await this.revealSearchView("cross-session");
      }
    });
    this.addCommand({
      id: "import-file",
      name: "Import session file",
      callback: () => {
        new FilePickerModal(this.app, this).open();
      }
    });
    this.addCommand({
      id: "export-markdown",
      name: "Export session to Markdown",
      checkCallback: (checking) => {
        if (!this.getActiveTimelineView()) return false;
        if (checking) return true;
        void this.exportActiveSession();
        return true;
      }
    });
    this.addCommand({
      id: "export-html",
      name: "Export session to HTML",
      checkCallback: (checking) => {
        if (!this.getActiveTimelineView()) return false;
        if (checking) return true;
        void this.exportActiveSessionHTML();
        return true;
      }
    });
    this.addCommand({
      id: "expand-all",
      name: "Expand all turns",
      checkCallback: (checking) => {
        const view = this.getActiveTimelineView();
        if (!view) return false;
        if (checking) return true;
        view.expandAll();
        return true;
      }
    });
    this.addCommand({
      id: "collapse-all",
      name: "Collapse all turns",
      checkCallback: (checking) => {
        const view = this.getActiveTimelineView();
        if (!view) return false;
        if (checking) return true;
        view.collapseAll();
        return true;
      }
    });
    this.addCommand({
      id: "expand-all-blocks",
      name: "Expand all blocks (tools, thinking, summary)",
      checkCallback: (checking) => {
        const view = this.getActiveTimelineView();
        if (!view) return false;
        if (checking) return true;
        view.expandAllBlocks();
        return true;
      }
    });
    this.addCommand({
      id: "collapse-all-blocks",
      name: "Collapse all blocks (tools, thinking, summary)",
      checkCallback: (checking) => {
        const view = this.getActiveTimelineView();
        if (!view) return false;
        if (checking) return true;
        view.collapseAllBlocks();
        return true;
      }
    });
    this.addCommand({
      id: "refresh-session",
      name: "Refresh session",
      checkCallback: (checking) => {
        const view = this.getActiveTimelineView();
        if (!view) return false;
        if (checking) return true;
        void view.reloadSession();
        return true;
      }
    });
    this.addCommand({
      id: "toggle-live-watch",
      name: "Toggle live watch",
      checkCallback: (checking) => {
        const view = this.getActiveTimelineView();
        if (!view) return false;
        if (checking) return true;
        view.toggleWatch();
        return true;
      }
    });
    this.addCommand({
      id: "search-in-session",
      name: "Search in session",
      checkCallback: (checking) => {
        const view = this.getActiveTimelineView();
        const session = view == null ? void 0 : view.getSession();
        if (!view || !(session == null ? void 0 : session.rawPath)) return false;
        if (checking) return true;
        void this.revealSearchView("in-session");
        return true;
      }
    });
    this.addCommand({
      id: "copy-resume",
      name: "Copy resume to clipboard",
      checkCallback: (checking) => {
        var _a;
        const view = this.getActiveTimelineView();
        const id = (_a = view == null ? void 0 : view.getSession()) == null ? void 0 : _a.metadata.id;
        if (!id) return false;
        if (checking) return true;
        void navigator.clipboard.writeText(`claude --resume ${id}`);
        new import_obsidian21.Notice("Copied resume command");
        return true;
      }
    });
    this.addCommand({
      id: "distill-session",
      name: "Distill session to note",
      checkCallback: (checking) => {
        const view = this.getActiveTimelineView();
        const session = view == null ? void 0 : view.getSession();
        if (!session) return false;
        if (checking) return true;
        void this.distillActiveSession();
        return true;
      }
    });
    this.addCommand({
      id: "merge-distill-clipboard",
      name: "Merge /distill output from clipboard",
      checkCallback: (checking) => {
        const view = this.getActiveTimelineView();
        const session = view == null ? void 0 : view.getSession();
        if (!session) return false;
        if (checking) return true;
        void this.mergeDistillFromClipboard();
        return true;
      }
    });
    this.addCommand({
      id: "install-bases-templates",
      name: "Install bases dashboard templates",
      callback: async () => {
        const result = await installBasesTemplates(
          this.app,
          this.settings.basesFolder
        );
        if (result.installed.length > 0) {
          new import_obsidian21.Notice(`Installed: ${result.installed.join(", ")}`);
        }
        if (result.skipped.length > 0) {
          new import_obsidian21.Notice(`Skipped (already exist): ${result.skipped.join(", ")}`);
        }
        if (result.failed.length > 0) {
          new import_obsidian21.Notice(`Failed: ${result.failed.map((f) => f.name).join(", ")}`);
        }
      }
    });
    this.registerObsidianProtocolHandler("claude-sessions", async (params) => {
      const p = params;
      const sessionPath = p["session"];
      if (!sessionPath) {
        new import_obsidian21.Notice("Missing session parameter.");
        return;
      }
      const turnIndex = p["turn"] ? parseInt(p["turn"], 10) : void 0;
      await this.openSessionByPath(sessionPath, turnIndex);
    });
  }
  async openSessionByPath(sessionPath, turnIndex) {
    try {
      const filePath = expandHome(sessionPath);
      const content = await readFileContent(filePath);
      const parser = detectParser(content);
      if (!parser) {
        new import_obsidian21.Notice("Could not detect session format.");
        return;
      }
      const session = parser.parse(content, filePath);
      await resolveSubAgentSessions(session, readFileContent, listDirectoryFiles);
      await this.openSession(session, turnIndex);
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      new import_obsidian21.Notice(`Failed to load session: ${msg}`);
    }
  }
  onunload() {
    const leaves = this.app.workspace.getLeavesOfType(VIEW_TYPE_TIMELINE);
    for (const leaf of leaves) {
      if (leaf.view instanceof TimelineView) {
        leaf.view.stopWatching();
      }
    }
  }
  async loadSettings() {
    this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
  }
  async saveSettings() {
    await this.saveData(this.settings);
  }
  async openSession(session, turnIndex, highlight) {
    var _a;
    let leaf;
    if (session.rawPath) {
      for (const l of this.app.workspace.getLeavesOfType(VIEW_TYPE_TIMELINE)) {
        if (l.view instanceof TimelineView && ((_a = l.view.getSession()) == null ? void 0 : _a.rawPath) === session.rawPath) {
          leaf = l;
          break;
        }
      }
    }
    if (leaf) {
      void this.app.workspace.revealLeaf(leaf);
    } else {
      leaf = this.app.workspace.getLeaf("tab");
      await leaf.setViewState({
        type: VIEW_TYPE_TIMELINE,
        active: true
      });
    }
    const view = leaf.view;
    if (view instanceof TimelineView) {
      view.loadSession(session);
      if (turnIndex !== void 0) {
        window.requestAnimationFrame(() => {
          if (highlight) {
            view.navigateToMatch(turnIndex, highlight.contentBlockIndex, highlight.text, highlight.occurrenceInBlock);
          } else {
            view.scrollToTurn(turnIndex);
          }
        });
      }
    }
  }
  async revealSearchView(mode) {
    const existing = this.app.workspace.getLeavesOfType(VIEW_TYPE_SEARCH);
    let leaf;
    if (existing.length > 0) {
      leaf = existing[0];
    } else {
      leaf = this.app.workspace.getRightLeaf(false);
      await leaf.setViewState({ type: VIEW_TYPE_SEARCH, active: true });
    }
    void this.app.workspace.revealLeaf(leaf);
    const view = leaf.view;
    view.setMode(mode);
    return view;
  }
  updateTimelineViews() {
    const leaves = this.app.workspace.getLeavesOfType(VIEW_TYPE_TIMELINE);
    for (const leaf of leaves) {
      const view = leaf.view;
      if (view instanceof TimelineView) {
        view.updateSettings(this.settings);
      }
    }
  }
  /** Update content width on all open timeline views without re-rendering. */
  updateTimelineWidth() {
    const leaves = this.app.workspace.getLeavesOfType(VIEW_TYPE_TIMELINE);
    for (const leaf of leaves) {
      const view = leaf.view;
      if (view instanceof TimelineView) {
        view.applyMaxWidth();
      }
    }
  }
  getActiveTimelineView() {
    const leaf = this.app.workspace.getActiveViewOfType(TimelineView);
    return leaf;
  }
  async exportActiveSessionHTML() {
    const view = this.getActiveTimelineView();
    if (!view) {
      new import_obsidian21.Notice("No active session to export.");
      return;
    }
    const session = view.getSession();
    const timelineEl = view.getTimelineEl();
    if (!session || !timelineEl) {
      new import_obsidian21.Notice("No session loaded.");
      return;
    }
    try {
      await exportToHTML(timelineEl, session, this.settings);
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      new import_obsidian21.Notice(`HTML export failed: ${msg}`);
    }
  }
  async exportActiveSession() {
    const view = this.getActiveTimelineView();
    if (!view) {
      new import_obsidian21.Notice("No active session to export.");
      return;
    }
    const session = view.getSession();
    if (!session) {
      new import_obsidian21.Notice("No session loaded.");
      return;
    }
    try {
      const mdPath = await exportToMarkdown(this.app, session, this.settings);
      new import_obsidian21.Notice(`Exported to ${mdPath}`, 5e3);
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      new import_obsidian21.Notice(`Export failed: ${msg}`);
    }
  }
  async distillActiveSession() {
    var _a;
    const view = this.getActiveTimelineView();
    const session = view == null ? void 0 : view.getSession();
    if (!session) {
      new import_obsidian21.Notice("No active session to distill.");
      return;
    }
    try {
      const result = await distillSession(this.app, session, this.settings.distillFolder);
      if (!result.success) {
        new import_obsidian21.Notice(`Distill failed: ${(_a = result.error) != null ? _a : "Unknown error"}`);
        return;
      }
      if (!result.updated) {
        new import_obsidian21.Notice(`Created: ${result.notePath}`);
      } else {
        new import_obsidian21.Notice(`Updated: ${result.notePath}`);
      }
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      new import_obsidian21.Notice(`Distill failed: ${msg}`);
    }
  }
  async mergeDistillFromClipboard() {
    var _a;
    const view = this.getActiveTimelineView();
    const session = view == null ? void 0 : view.getSession();
    if (!session) {
      new import_obsidian21.Notice("No active session.");
      return;
    }
    try {
      const clipboardText = await navigator.clipboard.readText();
      if (!clipboardText.trim()) {
        new import_obsidian21.Notice("Clipboard is empty.");
        return;
      }
      const result = await mergeWithClipboardContent(
        this.app,
        session,
        clipboardText,
        this.settings.distillFolder
      );
      if (!result.success) {
        new import_obsidian21.Notice(`Merge failed: ${(_a = result.error) != null ? _a : "Unknown error"}`);
        return;
      }
      if (!result.updated) {
        new import_obsidian21.Notice(`Created with merged content: ${result.notePath}`);
      } else {
        new import_obsidian21.Notice(`Merged into: ${result.notePath}`);
      }
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      new import_obsidian21.Notice(`Merge failed: ${msg}`);
    }
  }
};

/* nosourcemap */